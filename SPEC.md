---
status: active
last-updated: 2026-09-08
owner:
---

# Spec, Agent Nekko

> **This is the source of truth for the project.** It describes *what* we're building and *why*, vision, users, journeys, the feature set, and what success looks like. It is **not** about stack or technical design (that's `TASKS.md`). It is a **living artifact**: every prompt that adds or changes a feature updates this file so it always describes the system as it actually is and intends to be. The verbatim origin ask lives in [original-prompt.md](../../obsurdian/projects/agent-nekko/original-prompt.md).

## Vision

**Agent Nekko** (formerly Kotrain, briefly Nekkos, and originally Open Paw) is your local agent, backed by frontier intelligence. It is an open-source assistant for coding, cowork, and repeatable work that blends models running on your hardware with frontier providers. Use its own desktop/workbench UI or bring its capabilities into Claude Code, Cursor, Codex, OpenClaw, and other compatible tools through the CLI and MCP. Local-only work can run offline once its models and dependencies are installed; frontier calls and network integrations still require connectivity and may send their selected context to the configured provider.

The new primary domain is **agentnekko.com**. **nekkoagent.com** is the backup domain and should permanently redirect to the primary with paths and query strings preserved. The code repository and project folders are to become **agent-nekko**; the CLI command is **agent-nekko**. This is a staged cutover, not a claim that the domains, GitHub repository, Vercel project, or npm package have already moved. Existing `kotrain` commands, settings, data, package imports, updater feeds, and relay connections must remain compatible until their migrations are verified.

The personality is a capable, approachable agent nearby, with a restrained secret-agent influence rather than spy parody. Clear task status, quiet confidence, and generous whitespace take precedence over mission jargon, decorative surveillance metaphors, or a heavy space theme.

Quality bar: on par with Zed / Cursor / Warp / OpenClaw, generous whitespace, minimal chrome, dark/light themes, fast.

The persona/mascot is **Nekko** (formerly Aphelion), a 2D hand-drawn outline cat with a subtle secret-agent look: slim sunglasses, a discreet earpiece and short coiled wire, and a folded collar with a small tie. The reflective helmet treatment is gone. Her full body is a slender stick-cat silhouette with long line-art limbs and a narrow torso, not the older rounded astronaut/loaf body. At rest she leans one shoulder against a short local representation of the app edge, ankles crossed with one paw relaxed, like an agent waiting for the next assignment. The edge is part of the drawing only; the full rail divider remains hidden while collapsed. Sunglasses lift above closed eyes while sleeping or stretching so expressions remain readable. The miniature working icon simplifies the same head details for small sizes, and the portrait includes the collar without clipping. Website hero and social artwork reuse the app's exact leaning pose. The website favicon and packaged desktop/PWA/installer icons use the exact miniature secret-agent head on the dark-green tile, replacing the old orbit mark.

The ink and paper follow each theme, with ginger ears/tail and ink-filled glasses/tie. The full mascot retains its frame-by-frame "line boil" wobble and the activity system shipped in PR #140 (T124). Every state uses the same slender proportions: leaning while idle, pushing away from the edge and spinning when the user returns, making a long limber stretch during active pauses, standing and bracing against the edge to inspect a bug while the model works, and slumping against the edge after sustained inactivity. Pose timing, reduced-motion handling, offscreen pause behavior, and right-facing placement remain unchanged.

## Why It Exists

People who want to run AI models locally are stuck choosing between two kinds of tool, each blind in its own way:

- **Local chat UIs (LM Studio, Ollama front-ends)** are *chat-only*, a great model runner with a chat box, but with no awareness of your files or projects. The model can talk about your work but can't actually do it.
- **Terminal CLIs (Claude Code, aider)** have agentic power but are *blind*, you can't see what changed without `git diff`, and editing means leaving the tool.

Kotrain exists to collapse that gap: easy local-model setup **and** a model that works inside your real codebases, with everything it does visible.

### Positioning (carry into website + README)

**AI help on your computer. For coding and everyday work.** Lead with what people can do: get help writing and coding, automate repetitive tasks, and choose AI running on their computer or an online service. Explain that a model is the AI they choose, and local AI runs on their own computer. Do not require readers to understand "frontier", "harness", "runtime", "CLI", or "MCP" before they understand the benefit. Technical setup details remain available farther down the page with short explanations.

The five main marketing headings are **Use AI on your computer or online**, **Choose the right AI for the job**, **Automate repetitive work**, **Manage your AI models in one place**, and **Control your computer by voice** (visibly Planned). Section titles describe concrete actions rather than agent/spy metaphors. Agent Nekko complements existing tools instead of requiring people to abandon their preferred interface; its differentiator remains combining available local compute with online models. Simplifying the language must preserve setup requirements, paid-provider costs, privacy boundaries, and the distinction between available and planned features.

### Messaging pillars

1. **Frontier and local, working together.** Use Agent Nekko as your primary harness/UI, or connect through its CLI and MCP from compatible tools such as Claude Code, Cursor, Codex, and OpenClaw. Bring your own frontier providers and local model servers.
2. **The right model for the task.** Help users select models for a prompt and delegate concrete work to local or cheaper models while reserving frontier capability for demanding planning. Local inference can avoid per-token API charges but still uses hardware, power, memory, and time. Explain routing decisions and never silently cross an offline boundary.
3. **Automation in one place.** Manage scheduled tasks, watchers, and workflows alongside interactive agent work. Reusable procedures support agent, skill, shell, and nested workflow steps with bounded retries and visible outcomes.
4. **Simpler model management.** Discover and connect local runtimes, manage supported model lifecycle actions from the same app, and mix managed and existing servers as support grows. Installing and supervising an inference runtime so a separate server app is unnecessary is a product goal, not a claim that an inference engine is already bundled.
5. **Control your PC offline, with safeguards.** Tell Nekko what you want by text or voice and let it perform supported local computer actions within permissions you choose. One-click guided setup selects a compatible local intent LLM, text-to-speech (TTS), optional speech-to-text (STT), and required runtimes based on the machine and each component's minimum requirements. The experience includes model/setup explanations, action previews, sensitive-action confirmations, and an immediate stop control. This is a major planned product capability, not available in the current release.

### Roadmap: beyond one machine

These are aspirations, not generally available capabilities or launch promises:

- **Team hardware and distributed work:** opt-in workers pool available machines for model inference, agent tasks, and workflows, with permissions, capacity reporting, isolation, cancellation, and clear ownership.
- **Local task runners:** expand today's local shell/agent workflow foundation into a practical alternative for jobs otherwise consuming hosted CI credits. GitHub Actions workflow compatibility and untrusted pull-request execution require their own design and security review; neither is implied by local shell steps.
- **Team workflows:** shared definitions, run coordination, access controls, and auditable outcomes across a team.
- **Optional cloud runners:** explore only if users want them. A paid service must remain optional and must not weaken the local-first experience.

Existing cloud prototypes and older branding elsewhere in this spec describe implementation history or parallel work, not a requirement to launch a hosted product before the local Agent Nekko experience.

## Who It's For

- **Developers who want to use local models for real work**: people running LM Studio / Ollama / vLLM who are tired of a chat box that can't touch their files, and want an agent that reads, edits, and runs in their codebases.
- **Claude Code / aider / terminal-CLI users** who want the same agentic power but with an IDE-like surface (file tree, diffs, inline edit, visible context).
- **Privacy-conscious / offline users** who want everything to stay on their machine, and, with Cloud, a zero-data-retention path that still keeps inference local.
- **People who want an approachable local PC assistant** without assembling an intent LLM, speech models, runtimes, and hardware settings themselves. Voice is optional; typed control and visible confirmations must remain first-class.
- **People who want to reach their home/office model from their phone** (the headline Cloud feature).

## User Journeys & Experiences

### Editions, same engine + UI, different runtime

Kotrain ships the **same engine + same React UI** in several runtimes; from the user's point of view it's the same product wherever it runs (only the host transport differs, see `TASKS.md` and [spec-web-and-hosted.md](spec-web-and-hosted.md)):

- **Desktop** (Electron), the primary experience, available today (installers on GitHub Releases).
- **Self-hosted web**: `npx kotrain` or `npm run web` starts a local server that serves the same UI in a browser; offline-first.
- **Docker**: `docker compose up`, workspaces as mounted volumes, local models reachable via `host.docker.internal`.
- **Kotrain Cloud** (paid), managed hosting with subscriptions, an always-available **Zero-Data-Retention** mode, cloud chat-history + file management, and **phone connectivity to your locally-running model** via a secure relay.

### Setting up a local model

A user opens the app and goes to **Models**. Auto-discovery probes common localhost ports (Ollama 11434, LM Studio 1234, vLLM 8000, plus `127.0.0.1` to catch IPv4-only servers). They add or confirm a server, test the connection (green Connected / Offline), and the model list populates. Cloud providers are added the same way, grouped into **Local** vs **Cloud** sections. Favorites pin to the top of the picker.

### First-run setup (planned onboarding wizard)

The first launch opens a calm, full-screen setup wizard over the app; every step can be skipped (a per-step **Skip**, plus **Skip setup**), and it never auto-shows again once dismissed (Settings can replay it). Three skippable steps:

1. **Theme.** System-match, Light, Dark, and a set of elegant tech presets, each shown as a circle filled with a gradient of its palette so the choice is visual, not a word list. Clicking previews live.
2. **Providers.** Two groups: **online** (Claude and ChatGPT lead with one-click *subscription* sign-in so a Pro/Max or Plus/Pro plan works without metered API fees, API keys remain the secondary path, plus OpenRouter and a generic OpenAI-compatible endpoint) and **local** (auto-discovery surfaces running Ollama / LM Studio / vLLM servers for one-click add, and an **Agent Nekko** card offers the app's own managed local-model path).
3. **Integrations.** Two groups: **use Nekko inside other tools**, which installs the app as an MCP subagent into detected agent CLIs (Claude Code, Codex, Cursor) with one click, and **connect your apps** (GitHub, Linear, Slack, Discord, Jira, Teams, Gmail, Drive) so workflows and context can reach them.

### Signing in with a subscription (planned)

A user with a **Claude Pro/Max** or **ChatGPT Plus/Pro/Business** subscription clicks **Sign in with Claude** / **Sign in with ChatGPT** on the Models page (or in onboarding), approves in the browser tab that opens, and lands back on a connected provider marked **Subscription**, no API key, no per-token billing. The flow uses the same public OAuth sign-in the vendor's own CLI uses; the honest caveat shown next to the button is that the vendor's terms are written around their own apps, so the subscription path is offered as-is and the API-key path remains fully supported. Headless or remote setups get a paste-the-code fallback, and anyone already signed in to the Claude Code or Codex CLI can **import** that session in one click instead of repeating the browser flow. Sign out revokes the local token; a failed refresh prompts re-auth rather than silently falling back.

### One-click setup for safe offline PC control (planned)

**The experience:** a user who does not want to assemble an AI stack opens **Set up offline PC control**. Nekko inspects their machine, explains which models and runtimes fit, and shows the download size, disk/memory budget, supported actions, and permissions required. One **Set up** action then downloads or imports, verifies, installs, configures, and checks the complete local stack. Separate operating-system permission dialogs and license acceptance still require the user's approval; one click never means silently granting access or bypassing security controls.

The stack includes a local **intent LLM** that turns a request into a bounded, structured action plan, **TTS** for spoken responses, **STT** when voice input is enabled, and any supported runtime or computer-control adapter those components require. Text input and captions remain available; push-to-talk is the initial voice path rather than always-on listening. Audio and captured screen content are local and ephemeral by default. Local spoken intent is a request to evaluate, not permission to run arbitrary generated shell commands.

**Hardware-aware selection:** check OS/version, CPU architecture and supported instruction/backend features, total and available RAM, GPU/backend/driver compatibility, dedicated VRAM versus unified memory, and available installation disk space. Compare those facts with versioned, verified model/runtime requirements, supported languages, and required intent/STT/TTS capabilities. Fit the whole concurrently loaded stack, including runtime overhead, context/KV memory, and headroom for the user's normal applications. Do not double-count unified memory, assume separate GPUs form one pool, or equate a small download with a usable runtime footprint. Distinguish minimum compatibility from a recommendation expected to feel responsive. Validate with a short on-device warm-up and benchmark; present measured readiness rather than inventing performance claims.

If the recommended stack does not fit, offer a smaller supported model, reduced concurrency, or a tested CPU-only path with its trade-offs. If none meets the minimum requirements, say exactly what is missing and keep setup blocked. Unknown hardware support is **unverified**, not a pass. Never silently install an incompatible model or switch to a cloud provider. Users can review or override recommendations only within supported combinations and keep using an existing compatible runtime rather than being forced to replace it.

**Computer control:** supported local actions should include opening an approved application, switching to an approved window, adjusting a supported local setting, and entering text into a confirmed target. Scope access by app/window and action, prefer semantic/accessibility APIs over blind coordinates, and show the intended action before execution. Recheck the target immediately before acting and stop if focus, permissions, or observed state changed. Ambiguous voice recognition or low-confidence intent triggers clarification, not a guess. Treat text seen in applications, documents, and web pages as untrusted content, never as authorization to override the user's request.

**Safety contract:** a deterministic action broker validates the model's structured proposal against granted capabilities. No unrestricted shell or elevated permissions by default. Sending messages, purchases, destructive changes, credential/security changes, and other sensitive actions need explicit, action-specific confirmation; a model response or background audio cannot approve itself. Provide an always-visible stop button and keyboard shortcut, interruptible action boundaries, bounded execution, clear failure/partial-completion reporting, and a local activity log with sensitive content redacted. Undo is offered only for actions with a real, verified inverse; do not promise universal rollback. Revoking a permission stops related queued actions. These controls reduce risk but are not a guarantee that a model can never make a mistake.

**Offline means end to end:** initial provisioning needs internet access for approved downloads unless the user imports a verified offline bundle. Once ready, intent recognition, speech input, speech output, and supported PC actions run locally with no account, cloud fallback, remote telemetry, or network access required. Online actions are unavailable in this mode. Verify the experience with external network access blocked, not merely by pointing chat at localhost. A local server capable of proxying to the cloud is not automatically a verified offline runtime.

**Availability and boundaries:** current hardware telemetry, file/shell tools, model discovery, and guardrails are reusable foundations, not an implemented voice/desktop-control stack. The current chat **Offline** toggle disables all tools; this feature needs its own explicit local-only action policy and must not be implemented by weakening that existing toggle. Build a platform/action support matrix for macOS, Windows, and Linux and expose unsupported OS capabilities honestly. Automatic provisioning, model requirements/recommendations, voice processing, native computer-control adapters, and end-to-end safety/offline verification are still to build.

### Delegating frontier planning to a local worker

The lead agent can delegate a scoped task through `spawn_agent` with optional `provider_id` and `model_id` targets. Leaving both out preserves the parent's model. Changing provider requires a known exact model ID; the host checks the configured, enabled target and its model list before creating the child. Unknown, unavailable, disabled, or known non-chat models produce an error, never a silent fallback to a different or paid model. The child appears in the existing nested session UI with its selected provider/model and inherits the parent's execution policy and disabled tools. Ask mode and the ask-everything sandbox require approval before starting a sub-agent or calling an MCP tool, so indirect execution cannot bypass that boundary.

This first implementation is **explicit cross-provider delegation**, not automatic price/capability/resource optimization. The lead receives enabled provider IDs and labels, not credentials, and must ask for a known model rather than invent an ID. An opt-in local-worker default and a routing-decision UI remain planned. Incognito delegation is blocked because child sessions are persisted; it must not quietly retain an incognito task.

The existing **Offline** chat mode is deliberately narrower than normal local-agent work: it disables tools and connectors. The host now also rejects cloud or non-loopback provider endpoints before making a request, prevents unoffered tool execution, skips connector fetching in offline context previews, and suppresses hidden post-turn spec refreshes. This is endpoint-policy enforcement, not an operating-system network sandbox; users still control whether their local inference server proxies requests elsewhere. Broader independent model actions, including explicit spec generation, need the same privacy audit before claiming application-wide network isolation.

### Working in a codebase

The user wires one or more **folders** into a chat (multi-root), choosing one primary folder and any number of supporting folders in the Context Inspector's Folders section. The workspace gets indexed (file tree + code outline + search), with index search and workspace memory spanning the selected folders while the agent's working directory remains the primary. They talk to the model in a unified **Chat** surface, messages render as speech bubbles, and reasoning appears above each answer as a collapsed summary such as "Thought for 3s" (expandable to inspect the reasoning). A multi-step turn's working steps (reasoning, tool calls, and inter-tool narration) fold into a single collapsible **activity group** ("Worked on 6 steps · read_file, grep") so a long run reads as one expandable line instead of a wall of "Used `<tool>`" rows; only the final answer stays a bubble. Risky commands trigger an approval prompt. Each user prompt reveals a subtle on-hover action row (its timestamp, Copy, Edit & resend, and **Reset here**, which rewinds the chat to that message and re-runs it), replacing the old single Regenerate button. If a local model returns an empty response mid-loop, the reply retries once with a nudge and, failing that, surfaces a clear marker instead of stalling silently. If a model instead **degenerates into repeating itself**, the reply is cut off the moment the output collapses into a cycle, keeping everything it wrote before the loop and saying plainly what happened and what usually helps, rather than streaming the same sentence for minutes. A live subtext under the conversation tracks the in-flight reply (elapsed, tokens/sec, tokens generated) and keeps a last-reply summary when idle. **Tokens/sec is decode throughput**, output tokens over the time the model actually spent generating them, summed across the reply's steps: it deliberately excludes prompt processing, tool runs, and approval waits, so it matches the figure the local runtime reports rather than being dragged down by however long the turn happened to take (Ollama's own `eval_duration` is used where it's offered; other providers are timed from first token to the usage report). Single-digit rates keep a decimal, since 8.6 and 9.4 tok/s are different answers to "is this model fast enough". The UI says **reply** for a chat exchange and **iteration** for one cycle of a long-running run; "turn" is gone from the surface. Markdown renders on both sides of the conversation, so a dashed list typed into the composer comes back as real bullets rather than raw dashes. The composer accepts pasted images and has a **+** menu for a photo, file, or folder; attachments sit in a thumbnail strip inside the composer card, and right-clicking any chat image offers Copy or Save. Images are sent to vision-capable models as multimodal messages, with larger previews and a full-size lightbox on click. Invoking a `/skill` adds a compact usage chip while keeping the user's own input separate; the full skill instructions still go to the model. Above the composer, a two-row instrument strip separates **how this agent runs** (mode, tools, and the Offline / Incognito privacy switches on the right) from **which brain it uses** (model, thinking, effort, plus Automate), and the context readout counts the draft as you type it, in the strip and in the Context Inspector alike. Per-chat control modes (Ask / Guardrails / YOLO, plus Offline and Incognito) govern how much it confirms.

### Editing, reviewing & previewing, without leaving the app

Files open as panes right in the workbench: a dedicated **Files** side-pane lists every workspace root and can dock left or right (left by default), collapse, and resize; its tree opens files as tabs alongside chats and terminals. Markdown is rendered or code opens in a mono editor, with an integrated browser pane for the live preview beside the code. When the agent edits files, a **Changes** panel shows a line diff and the user keeps or reverts at line/file/all granularity. Reviewing AI-made changes, browsing a preview, and writing better prompts (via the always-on prompt analyzer) all happen in one calm window. *(Planned next:)* the user drops a **+ comment** on a line and either queues it for the next prompt or runs it immediately, turning in-place review notes into agent work.

### Designing: draw it or describe it (Design tab)

The **Design** tab is where designs get made, a Claude Design-style surface with two ways in, both ending as a working prototype:

1. **Draw it.** A pen-and-touch-first sketch canvas (built to be used from an iPad or iPhone via the web edition: pointer events, Apple Pencil friendly, palm-safe `touch-action`). The user sketches boxes, arrows, and labels, adds an optional note ("mobile app, dark theme"), and hits **Generate prototype**: a vision model turns the sketch into a polished, self-contained HTML code prototype.
2. **Describe it.** Write a prompt ("a landing page for a sushi delivery startup") and watch a design pop up. Starter idea chips help the first prompt along.

Either way the concept lands on the board as a card and is **mirrored into the workspace as a real HTML file** (`kotrain-designs/`), so the user, or any agent, keeps iterating on it as code. Selecting a concept opens a side sheet with a **Refine** box ("make the hero darker", "add a pricing section") that regenerates the design in place, follow-up by follow-up: the user stays the designer, the model just drafts fast.

The board still holds **live snapshots** of the app's real pages. The user pins **notes** on a page or leaves a **comment** that they Add-to-prompt or Run-now. As the agent reworks the UI, the snapshots refresh live and an "updating" badge marks the page being changed; clicking it jumps to the agent doing the work. The board makes "what does my app look like, and what should change" a visual, direct-manipulation surface.

### Seeing exactly what the model gets (Context Inspector)

A toggleable right-side panel makes context legible. **Sources** lists the folders wired in, attached files, the chat's `SPEC.md` (with Build-from-chat / Live controls), and **Guidelines**: detected guideline files (AGENTS.md/CLAUDE.md) plus memory entries, each with its own enable/disable control. Guidelines sits above the Spec panel, while **Breakdown** is a live view of exactly what's entering the prompt on the next reply (files, guidelines, memory snippets, connector data, index snippets), each item toggleable/pinnable with token counts, plus a context-window headroom bar.

### Living spec from a conversation

Any chat can synthesize a `SPEC.md` in its workspace from the conversation (using the session's own provider/model, one-shot). With **Live** on, the spec is rebuilt after every turn so it tracks design changes and new features as the conversation evolves.

### Training a model while you watch (Training tab)

The user opens **Training**, says what the model is for ("classify support tickets by urgency"), points at a dataset (Hugging Face, Kaggle, a local path, or a URL) and optionally a base model, and hits Start. That's the whole simple path; an **Expert levers** panel exposes the full configuration (framework, epochs, batch size, learning rate, metric + direction, experiment/time budgets, free-form constraints) for people who want control. A **data-scientist agent** then does the real work in a dedicated chat session, and the dashboard shows its progression live: stat tiles (best score, experiments, success rate, emergent niches, self-repairs, runtime), a **current champion** card, and the **idea maze**, a growing tree of every idea the agent tried, color-coded (tried / got better / best so far / running) with the path to the leader highlighted. The user can **guide the agent mid-run**: hints, new training approaches, or corrections fold into its next turn. Starting a run drops the user straight onto its live dashboard, where a "now" strip spells out what's happening (from "starting up…" through the currently-running experiment), and an **Artifacts card** makes the output concrete: it lists exactly what the run built (the trained model plus each harness file), where each lives, and how to use it, with one-click open in the OS file manager. The distinctive twist: a run can also produce the **harness for the model's use case**, an agent file (AGENTS.md), a skill, and/or a spec, so the output isn't just weights, it's a ready-to-use capability.

### Automating repeatable work (Workflows tab)

Some work isn't a conversation, it's a **procedure that should run the same way every time**, often without anyone watching. The **Workflows** tab is that surface, modelled on GitHub Actions but with the steps a coding agent actually needs: a step is an **agent prompt**, an installed **skill**, another **workflow**, or a **shell command**.

The part that makes it more than a script runner is that steps are **routed rather than merely sequenced**. Every step declares where the run goes when it succeeds and when it fails, and a route can point **back at an earlier step**, so the natural shape of real work is expressible: build, then verify, and when verify finds problems, go back to build and try again. Loops are bounded (a step can be re-entered a fixed number of times) so a workflow that never converges still stops and says so, rather than running forever. Steps also carry **retries**, a **timeout**, and a **continue-on-error** flag. Agent steps report their own outcome with a PASS/FAIL token, which is what lets an agent step be routed the way a shell step's exit code is; a run's agent steps all share one chat session, so a verify step sees what the build step actually did.

A workflow starts from whatever fires it: **by hand**, on a **schedule** (a cron expression or a plain interval), from the **CLI** (`kotrain workflow run <name>`, or `trigger <command>` to fire everything listening for it), from **Slack** (a channel, optionally filtered to messages containing a word), or from a **git provider event**, a pull request opened, updated, closed, merged, commented on or reviewed, a push, a tag, an issue, filtered by repo and branch. Provider events are **normalized across GitHub, GitLab, Bitbucket and other**, so a workflow reacts to "a PR opened" without caring whose vocabulary it was. One workflow can carry several triggers.

The tab is built for an install that accumulates **hundreds** of these. A left rail filters by search (which reaches into step bodies, not just names), enabled state, trigger kind, and category, and the list groups either **by category** ("what do I have?") or **by event listener** ("what happens when a PR opens?"), with a workflow appearing under every event it listens for. A row shows its state, step count, triggers, and how the last run went; expanding it reveals the **step canvas**, a node graph in the same visual language as the Skills tab, where failure routes are drawn in the danger color and a loop back to an earlier step dips under the row, so a retry loop is something you see rather than something you read. The same canvas doubles as the live run view, each node wearing that run's outcome, beside the attempt-by-attempt run log (with a link into any step's transcript) and that workflow's run history. Workflows are created from **templates** (a build/verify loop, a PR reviewer, a nightly maintenance sweep, or empty), edited in a dialog whose canvas redraws as you type and which flags steps nothing routes to and validates a cron expression against its next fire, duplicated, enabled/disabled, and deleted.

### Handing the agent a long-running goal

The user gives the agent **something to get finished** ("ship the CSV import end to end: parser, validation, tests, docs"), optionally with context and constraints, and the agent works it **plan-first** in three phases: **Plan** (it studies the goal and the workspace, then writes an ordered execution plan of concrete, verifiable steps), **Execute** (it works the plan step by step, marking each step active and then done, with a one-line outcome note, only once verified), and **Iterate** (when reality disagrees, a step fails, new work surfaces, it revises the plan and keeps going until every step is done and the goal is verifiably met). This is deliberately **not a model-testing surface** (that's the Training tab); it's a "work until finished" surface for real work.

The dashboard reads as **mission control, not a chat**: a big **"working for"** clock, live status plus a **phase chip** (planning / executing / verifying / finished), and the **live plan checklist** front and center, each step with its status glyph, note, and a progress bar, alongside a current-step card, goal-shaped stat tiles (plan progress, iterations, attempts, runtime), a **Mission control** card that surfaces the budgets the loop enforces (iteration budget, time budget), and a prominent activity feed of plan milestones, failures, and self-repairs. Course-correcting is a full-width **"Steer the mission"** console (deliberately one nudge at a time, folded into the next iteration, not a back-and-forth chat); the underlying agent transcript is still reachable via a low-key **Transcript** link. Under the hood a goal is **not just one ever-growing chat**: each iteration runs on a **bounded context window** (the last few turns; the brief re-injects the plan every turn), and the loop is **guaranteed to terminate**, the host enforces a turn budget and any time budget, and **detects a stall** (no measurable plan/experiment progress plus a repeating answer) to pause the run and ask for a hint rather than looping for thousands of iterations. Runs survive restarts and resume automatically; resuming a budget-capped run grants a fresh leg.

### Driving your home model from your phone (remote control)

The user opens **Settings → Remote access**, hits Enable (the managed relay is prefilled; a self-hosted relay URL works the same), and taps **Pair a device**: a QR appears carrying a **single-use pairing code that expires in 10 minutes**. They scan it from the phone (Kotrain app or any browser) and the phone shows up under **Paired devices** with a live connection dot. From anywhere, the phone now runs the full app against the home machine: start and manage chats, watch and steer training runs and goals, approve risky commands, browse the Command Center, with a **bottom tab bar** making the key surfaces thumb-reachable. Inference and tool calls execute locally under the machine's guardrails/sandbox.

Security is layered: the local agent dials out to the relay (no inbound ports), every payload is end-to-end encrypted (the relay routes only ciphertext, so relayed local-model use is inherently zero-data-retention), and on top of the transport every device must pass a handshake against the machine's **device registry**: unknown devices are denied unless they present a live pairing code, each device can be **renamed or revoked** (revocation kicks it mid-connection), and **Rotate secret** is the cryptographic kill switch that unpairs everything. Remote access survives restarts; the agent reconnects on boot. Full guide: [docs/REMOTE.md](docs/REMOTE.md).

## What Success Looks Like

- **Offline PC control target (planned):** on a supported machine, a non-expert can review and approve one setup plan, complete required OS permission prompts, and use the selected intent/speech stack for approved local actions with external network access blocked. Model choices meet verified whole-stack minimum requirements, sensitive actions require confirmation, and stop/revocation reliably prevents subsequent actions. Unsupported hardware or capabilities are explained rather than hidden by cloud fallback.

- **"Connect + use a local model" just works**: one-click local setup, and the model can actually read/edit/run in a real codebase end-to-end (verified repeatedly against a live LM Studio gemma reasoning model: streaming reasoning, single + multi-step tool loops, index-grounded context).
- **The unique features land**: users can *see* what the model is given (Context Inspector) and trust it on risky commands (guardrails), the two differentiators from the original ask.
- **Same app everywhere**: desktop, one-command web, Docker, and (paid) hosted, with a byte-identical UI.
- **Polished enough to stand next to Zed/Cursor/Warp**: whitespace, themes, speed, and a charming mascot.
- **Shipped and installable**: public MIT repo with downloadable installers for win/mac/linux, auto-updates, and a marketing site with downloads. macOS builds are signed with a Nekko Labs Developer ID certificate and notarized by Apple, so a download installs with no Gatekeeper workaround; Windows signing (SignPath Foundation) is still pending.

## Feature Set

> Living catalog of capabilities, grouped by area, marked `[shipped]` / `[in progress]` / `[planned]`. Capability-level descriptions; the task-level breakdown (with stable IDs and history) lives in [TASKS.md](TASKS.md) Part 2, and the full technical design for the web/Docker/Cloud editions lives in [spec-web-and-hosted.md](spec-web-and-hosted.md).

### Models & providers
- **Provider support** `[shipped]`, Anthropic (native), OpenAI-compatible streaming SSE (covers OpenAI/OpenRouter/LM Studio/vLLM), Ollama native (list/pull/ps/load/unload), with OpenRouter presets. Base-URL `/v1` normalization for bare `host:port` entries.
- **Local-model auto-discovery** `[shipped]`, scans localhost + `127.0.0.1` on common ports; Test-connection in the add form; cards auto-check and show Connected/Offline; Local vs Cloud grouping.
- **Subscription sign-in** `[planned]`, one-click OAuth sign-in that lets a **Claude Pro/Max** or **ChatGPT Plus/Pro/Business** subscription drive the app instead of a metered API key: browser authorize + loopback callback on desktop, a paste-the-code fallback for headless/remote, and one-click **import from the Claude Code / Codex CLI** for anyone already signed in there. Tokens refresh silently, are stored mode-0600 in the data dir, never enter settings export, and a lapsed session asks for re-auth rather than failing mid-chat. Subscription is the encouraged path for anyone holding one (no per-token fees); API keys stay fully supported as the quiet alternative, and provider cards badge which kind of auth they use. **Caveat the UI states plainly**: these flows use the vendor's own first-party CLI OAuth client, which vendor terms scope to their own apps, so availability is at the vendor's discretion and can change.
- **Models page** `[shipped]`, server cards (add/connect ollama/lmstudio/vllm/cloud), model list, model favorites, and per-provider server management. Each local server card shows which models are **loaded** in memory (Ollama via `/api/ps`, LM Studio via its native `/api/v0/models` state, vLLM = always resident), auto-refreshing while connected. Per-model **load/unload** is available for Ollama (native HTTP) and for LM Studio (via its `lms` CLI, for a local instance with the CLI installed). A **Stop server** action terminates the server process (killing whatever listens on its port); it stays the unload path for vLLM and for a remote/CLI-less LM Studio, which have no per-model unload. It appears **only on a local provider that is actually connected**, since an offline server has no process to stop and a greyed-out button just invites a click that can't work. A model's **load** chip is visibly interactive (accent tint and ring on hover), and a loaded one reports "loaded" at rest and offers "unload" under the cursor, so one chip carries both the state and the action. Usage analytics (tokens over time, per model/provider) now live in the Command Center.
- **Resource monitors** `[shipped]`, four live monitors, **CPU** load, **memory** use, **GPU** utilization, and **VRAM**, surfaced in two places that never fight for the same corner. A quiet always-on **monitor chip** sits bottom-right on **every tab** (one segment per enabled monitor, plus a VRAM micro-bar); hovering it opens the panel where each monitor is switched on or off and a per-GPU breakdown is shown. The chat's context panel carries the **full monitoring section** at its foot (CPU with core count and processor name, memory used/total with %, GPU utilization, VRAM used/total with % and per-device used/free). While that section is on screen the chip **warps into it**, shrinking, blurring and fading along a path that ends on the section (which answers with a one-shot glow), and flies back out when the panel closes, so the same numbers are never stacked on top of each other. **Switching a monitor off stops the work behind it**: the renderer polls only the probes the enabled monitors need, so turning GPU/VRAM off spawns no GPU probe and turning CPU/memory off samples no CPU times; with all four off the chip reads "Monitors off" and nothing is polled at all. CPU/memory come from `os` (portable, no native module, load measured as a delta between samples like a task manager). The GPU probe is **per platform**: `nvidia-smi` on Windows and Linux, and on **macOS** the accelerator driver's own counters read out of the IO registry (`ioreg -c IOAccelerator`), which needs no admin rights, unlike `powermetrics`. A Mac therefore reports its real GPU ("Apple M1 Max (32-core GPU)", live utilization) instead of the old "No NVIDIA GPU detected". Because Apple Silicon has **no dedicated VRAM**, the memory meter there renames itself **GPU memory**, measures the driver's current allocation against the unified pool, and says "unified with system RAM" so nobody reads it as a discrete card's VRAM; the dock names the probes it used (`os + ioreg`, `os + nvidia-smi`). A machine whose GPU can't be read says so plainly rather than naming a vendor it never checked for. Preferences persist in settings (`AppSettings.monitors`); one shared sampler feeds both surfaces so nothing is probed twice per tick.
- **Reasoning-model streaming** `[shipped]`, `reasoning_content` deltas (OpenAI-compatible) and `message.thinking` (Ollama) render into a collapsed, persisted "Thought for Ns" summary above each assistant answer; the full reasoning remains expandable.
- **Thinking toggle** `[shipped]`, for reasoning-capable models the metrics bar's "thinking" indicator becomes a per-chat **on/off toggle** (`session.thinking`) that turns reasoning on or off for that chat. It maps to each server's native knob, Ollama `think`, OpenAI-compatible `chat_template_kwargs.enable_thinking` (Qwen3 and friends), and is only sent to local servers that understand it. Non-reasoning models keep the passive "streamed reasoning this turn" indicator.

### Chat & cowork (unified surface)
- **Workbench (multi-pane)** `[shipped]`, the Chat surface (reached from the **Agent** tab, which wears the Aphelion cat avatar in the rail; the rail header is a plain "Kotrain" wordmark with no logo mark) is a Warp/Devin-style workbench: a project-grouped left sidebar with collapsible folder sections (animated chevron + item-count badge when collapsed, accent-highlighted active chat, spawned sub-agents nested under their parent), a tabbed center, and side-by-side split columns (up to 3) so many agents and terminals run at once. Project-less chats sit in a **General** group kept at the **top** of the sidebar (hidden when empty), and a **new chat auto-files under the project it's about** the first time it runs, inferred from its attached folders and first prompt (a confident, non-ambiguous match only; a general chat stays under General). Each chat pane is independent, with its own provider/model picker, streaming, and Context Inspector, so agents run in parallel. The chat itself opens on a friendly empty state and a **unified composer card**: textarea, attach menu, Queue, and the **send control is Aphelion herself** (the cat avatar, no button fill, lifting on hover) in one rounded surface with a whole-card focus ring. A chat that references a project shows subtle **tags** on its sidebar row (extra projects it touches, free-form tags) and a project chip on its center tab, so tabs from different projects are tellable apart.
- **In-chat pull requests** `[shipped]`, a chat "has" a PR when a GitHub PR URL shows up in its transcript (the agent runs `gh pr create`, the URL lands in tool output, or the user pastes one). Those PRs surface as **cards inline in the chat** and as **badges** on the chat's sidebar row and header (open count, "PR ready" when approved + green, merged count). An open PR's card keeps its info (repo/number, state, diff counts, checks) on the left and parks a **modern, right-aligned action cluster**: **Approve** (a solid green gradient primary), **Decline** (a quiet red ghost), **Review** (a bordered secondary that opens the diff), a subtle violet **Merge**, and a compact "open on GitHub" arrow, each behind a one-click confirm; the info-left / actions-right split keeps the card slim. A merged PR's card becomes a purple, star-dusted "successfully merged 🎉" banner. **Review** opens the PR's diff in a workbench side pane (Devin-style, per-file, syntax-tinted). Live state, the diff, and the actions run through the **`gh` CLI**, falling back to the **GitHub REST API** with the GitHub connector's token when `gh` isn't installed; nothing about PRs is tracked or stored by Kotrain beyond what the transcript already contains.
- **Hover-expanding nav rail** `[shipped]`, the left icon rail expands over the content on hover (64 → 200px, springy width + label fade/slide animation) to reveal each destination's name, and collapses back when the pointer leaves; keyboard focus (Tab) expands it too. Its right divider is absent while collapsed for a quieter edge and appears only with the expanded rail, so it never crosses the mascot at rest.
- **Experimental surfaces, opt-in** `[shipped 2026-09-08]`, a new **Settings → Experimental** card holds a toggle per in-progress surface, keeping the sidebar to the stable set by default. **Model training**, **Design board**, and **Memory** each hide their nav destination (rail, phone bottom bar, and the command palette's navigation entries) until switched on; turning one off hides the tab again, and a surface you're standing on lands you on the Command Center instead of a dead end. The flags persist in settings (`AppSettings.experimental`) and apply across every edition, desktop, web, and Docker share the gate.
- **Terminals** `[shipped]`, persistent shell sessions rendered as Warp-style command **blocks** (command + streamed output + exit code + duration). Backed by one long-lived host shell per terminal (PowerShell on Windows, `$SHELL`/bash elsewhere) so cwd/env persist across commands; marker-delimited blocks capture status with no native PTY (honors the no-native-modules rule). Command history (↑/↑), best-effort interrupt, scrollback restore on tab reattach. New-terminal shortcut (Ctrl/⌘+`, the terminal key from VS Code, Cursor, and Zed; Ctrl/⌘+J still works).
- **Sub-agents & orchestration** `[shipped]`, a `spawn_agent` tool lets an agent delegate a scoped sub-task to a fresh child session (own context, same project, inherits the parent's tool-execution mode). An **orchestration strategy** (Settings) governs delegation: `solo` (no sub-agents, `spawn_agent` is withheld), `balanced` (delegate only clearly separable/heavy work, default), or `swarm` (act as orchestrator, proactively decompose into parallel sub-agents). The strategy shapes the system prompt and tool availability; **max nesting depth** and an advisory **parallel** count are configurable. Children stream their own events, and the Command Center renders the full **swarm as a recursive tree** under each agent (live status, descendant counts, click-to-open) and reports the final answer back as the tool result.
- **Streaming chat** `[shipped]`, markdown + code rendering, persisted collapsed reasoning summaries, expandable "Used `<tool>` tool" cards, approval prompts, and speech-bubble messages. A turn's **concluding text always surfaces as the answer bubble**, even when the run ends on a tool-calling step (cut short by the step budget, an abort, or a model that concludes in the same message as its final tool call), so the wrap-up no longer folds into the collapsed activity group and vanishes. **Markdown covers both sides of the conversation**, the user's own messages render it too, since people type dashed lists, `code`, and links into the composer and expect them formatted: bullet and numbered lists (nested by indentation, and recognised even when the run is glued to the sentence above it, no blank line needed), headings, block quotes, rules, pipe tables, fenced code, and inline bold / italic / strikethrough / code / links plus bare-URL autolinking. Single newlines stay line breaks, and snake_case identifiers or `2 * 3 * 4` arithmetic are never mistaken for emphasis.
- **Composer attachments & the + menu** `[shipped]`, paste images directly into the composer or use the **+** menu to add photos, session files, or project folders. The + menu's **Skill** row **expands its skills as a side flyout on hover** (no click needed): every skill this chat can run (built-ins plus installed marketplace skills, starred ones first, each with its description) plus an **Add skill** row that opens the Skills tab. Picking one arms it in the composer exactly as `/name` does, so skills are discoverable by hovering rather than only by knowing to type a slash; a short close-delay lets the pointer cross to the flyout, and Escape closes the flyout before it closes the menu. Pasted and selected images travel with the user message as data URLs and are encoded for multimodal requests by OpenAI-compatible, Anthropic, and Ollama providers. Attachments **live inside the composer card**, a thumbnail strip across its top separated from the textarea by a hairline, so they can never cover the controls above it. Any chat image, thumbnail or full-size preview, has a **right-click menu**: *Copy image* (put on the clipboard as PNG) and *Save image…* (download it). Escape dismisses that menu without closing the preview behind it.
- **Saved drafts & ready-to-type composer** `[shipped]`, an unsent message is never lost. The workbench only mounts the pane you're looking at, so the composer's text **and its pending image attachments** are parked per chat and restored when you come back, whether you switched to another tab, left the Chat view, or quit and reopened the app. Sending or queueing clears the parked draft. Opening a chat also puts the cursor straight in the composer (after any restored text), so a new agent is ready to type into the moment it appears.
- **Metrics bar** `[shipped]`, context used/total with an **expanded by-source token tooltip** (a segmented usage bar plus each source's token count and share of the window, a **Free space** row, and the total, Claude-Code style), tokens/sec, a thinking on/off toggle (reasoning models) or indicator, estimated cost, compact provider/model switching, and an effort cycle (low/normal/high → sampling temperature). The model picker and **⚡ Automate** control live here rather than in the chat header, with the picker immediately to the left of effort.
- **Model auto-mode, with profiles** `[shipped, profiles 2026-07-28]`, a per-chat **✨ Auto (pick best)** option in the model picker, plus an **Auto profile** chip beside it that says how hard Auto leans on capability: **Cheap** (always the smallest capable model, whatever you ask), **Normal** (reads the prompt: complex/coding work gets the strongest model the provider offers, a quick question gets a small fast one), **Quality** (always the strongest available). Favorited models break ties. Auto never picks a model that can't hold a conversation, speech recognition, embeddings, rerankers, OCR, and image models are filtered out, because a local server lists those from the same endpoint as its chat models. The strip shows a live **"→ <model>"** of what the next message will actually run on (with the prompt empty as well as typed) and explains the choice on hover. Persisted per chat (`session.autoModel`, `session.autoQuality`).
- **No silent model guess** `[shipped 2026-07-28]`, a chat without a model **asks** instead of inheriting one. The empty state reads "Pick a model to get started" with a button that opens the picker, a nudge above the composer says the chat needs a model, and the model chip itself reads "Choose a model" in accent. Kotrain no longer defaults to a provider's first model on connect, which used to silently select whatever a local server happened to list first (LM Studio's first entry is often `whisper-large-v3`, which can't answer a chat turn).
- **Flat grouped model picker** `[shipped]`, the composer's model menu lists **every provider's models in one flat list, grouped by provider** (no nested provider dropdown): a **★ Starred** section pinned on top, a star toggle on every row (persisted to the shared `favoriteModels` setting, same as the Models page), a filter box when the list is long, and implicit provider switching, picking a model from another provider switches the chat's provider in the same click. The pick is **remembered on the chat** (provider and model both), so it survives switching tabs, closing the pane, or restarting. The menu opens rightwards from the chip's own left edge, so it stays inside the pane instead of hanging over the sidebar. The strip above the composer **never wraps**: the model chip is the flexible member and truncates first, so a long local-model name can't push the controls onto a second line. If a chat has no usable model (its provider is offline or exposes none), sending **says so** rather than doing nothing, and keeps the draft.
- **Instrument strip, two rows and three control shapes** `[shipped]`, the settings above the composer are grouped by what they answer instead of reading as one long run of identical pills. **Row one is how this agent runs**: Mode, Tools, and **MCP** (the MCP-servers menu, see MCP client) on the left, and the two privacy switches (Offline, Incognito) parked on the right behind a hairline divider. **Row two is which brain it uses**: model, Thinking, Effort, with the Automate action on the right. Three deliberately different shapes carry the difference, **value pickers** are bordered fields with a muted label and a caret, **on/off switches** are quiet until hovered and show their state with color and a lit dot rather than a filled pill (Offline lights accent, Incognito plain ink, so only one accent state exists in the strip), and **actions** are plain icon buttons. Nothing wraps down to 640px wide.
- **Runaway-output backstops** `[shipped 2026-08-13]`, three things now bound a single response, because a local model that degenerates had nothing stopping it (one observed reply streamed 108,790 characters of reasoning, the same sentence 2,534 times, over nine minutes, and locked the window up). An **output cap** is sent to every provider as its native limit (`max_tokens` / `num_predict`; default 8,192, 256–200,000 in Settings → Agent loop), where openai-compat and Ollama requests previously carried none at all and ran until they filled their own context window. A **repetition guard** watches each stream and cuts it off the moment the output collapses into a cycle, cancelling the request, keeping the useful head of the reply, and ending the turn instead of spending the rest of the step budget on the same loop. And streamed deltas are **batched into frames** rather than committing React state per token, with the live buffers capped, so a fast or runaway reply can't freeze the transcript.
- **Runs survive being cut off** `[shipped]`, a long agent run is checkpointed as it goes, so an interruption costs you the last step rather than all of them. Every completed step (the assistant message and each tool result) is **written to disk the moment it lands**, before the event even reaches the UI, so a run killed mid-flight by a timeout, a dropped connection, a crash, or a quit leaves its finished work on disk instead of only in memory. A reply whose stream breaks **keeps what the model had already written**, marked as cut off, rather than vanishing behind an error (a tool call it was still emitting is dropped: it never ran and its arguments may be truncated). Pressing **Stop** goes through the same path, so a stopped reply keeps its partial answer and reads as "Reply stopped", not as a failure. The failure notice then offers **Resume** as the primary action: the transcript is left exactly as it is, any tool call that never got to run is answered with a stand-in saying so (which is what makes an interrupted transcript valid to send again), and the model is told to continue from where it stopped and not repeat the work above. **Start over** remains beside it as the honest name for the destructive option, which re-answers the prompt from scratch and discards the run. (Before, a timeout on a long chat reported only "Reply failed", and the single **Retry** button truncated the chat back to the last user message, throwing away every step it had completed.)
- **Tool-step budget (no more lost replies)** `[shipped]`, the agent loop is bounded by a **step budget** (default 80 tool round trips per reply, 5–400, set in Settings → Agent loop) that exists to catch a loop going nowhere. Reaching it no longer discards the work: the loop takes one final pass with the tools **withheld**, so the model answers with what it found and the next steps it would take, and the reply lands with an honest note naming the limit and how to continue. (Before, a long exploration hit a hard 12-step cap and the whole reply failed with "Reached max tool iterations".) The agent's operating principles also make it **diagnose blocked operations instead of retrying blindly**: an empty/unauthorized/not-found result (an empty `gh`/API response, a 401/403/404, "permission denied", an auth prompt) makes it stop and name the likely cause, a private or inaccessible repository, missing/expired credentials or `gh` auth, a wrong remote, or a rate limit, and say how to fix it, and it **ends every turn with an honest wrap-up** (what it did, what actually happened including anything blocked or unverified, and the concrete next step) rather than claiming a task is done when it isn't.
- **Chat control modes** `[shipped]`, per-chat Ask / Guardrails / YOLO, plus Offline (local-only, no tools/connectors/internet) and Incognito (no transcript/memory persistence); in-composer Tools popover to enable/disable builtins + MCP tools.
- **Competitor-parity chat features** `[shipped]`, export to Markdown, regenerate last response, edit & resend, per-message + code-block Copy, slash-command palette (`/`), @-mention files, chat-list search + rename, conversation tags, per-chat ⋯ menu (Pin/Rename/Delete).
- **Command Center dashboard** `[shipped]`, a monitor-style operations board (reworked 2026-07-25 from the old card-heavy kanban layout). Under the header sits a one-line **monitor strip** of vitals (agents working · automations active · terminals live · tokens today · est. spend), quiet tabular numbers between hairlines rather than stat tiles. Then three scannable row groups: **Now** (every in-flight run as a rich row: live status + elapsed working time, agent-type badge, model, message/token counts, last-answer snippet, the queued prompts, and the **recursive sub-agent swarm tree**, with Stop/Open in hand; live automation fires appear here too; compact fleet-by-type counts sit in the section header), **Automations** (every scheduled / recurring / background task as a schedule row: kind glyph, cadence, live "running now" / **"next in Xm" countdown** / paused / error state, run count + last-run time + last-result snippet, with Run-now / Pause-Resume / Delete on hover), and **Sessions** (all other chats as compact rows: title, model, msgs, tokens, age; capped with Show-all). A **Terminals** row group lists shells with live/exited state. Analytics fold into a single segmented **Insights** section at the bottom (Optimize tips / Cost / Usage / Services tabs, one visible at a time). Per-GPU VRAM moved out of the page into the global **monitor chip** (see Resource monitors). Every visual metric shows a **friendly skeleton placeholder** when there's no data yet.
- **Agent types & fleet view** `[shipped]`, every running or scheduled agent is given a **type** derived from what it's doing, the skill in play, the task kind/condition, and the prompt, so a team can tell a **code review bot** from a **production monitor** from a **research agent** or **build agent** at a glance. Types show as coloured badges on agent cards and task cards, and a **Fleet** panel at the top of the Command Center groups everything in flight by type with live counts. This turns the dashboard into a team-wide monitor: what's running, what kind of work it is, and where to step in. Classification is a pure, unit-tested heuristic (`classifyAgent`/`classifySession` in `shared/agent-types.ts`).
- **Per-agent queues** `[shipped]`, each chat agent has a **run-queue**: line up follow-up prompts (from the composer's **Queue** button, even while a turn is streaming) and the agent works through them one after another when the current turn finishes. Queued items show on the composer and on the agent's Command Center card, each removable. Combined with scheduled / recurring / background tasks, the Command Center becomes a single place to see everything each agent has done, is doing, and has queued next.
- **Optimization insights** `[shipped]`, an **Optimize** panel on the Command Center turns your own usage into prioritized, actionable tips (warnings first, then by estimated saving): route light chats to a connected local model, use a smaller model for short chats (with a concrete cheaper alternative), prune context when input dwarfs output, the single biggest cost driver, and pinning your go-to models. Pure heuristics over the existing usage summary + sessions + providers, no extra model calls; shows a total potential-savings figure.
- **Cost breakdowns** `[shipped]`, a Command Center **Cost** panel: this-month **actual** spend + a month-end **projection**, an **all-time** total, a **daily-spend chart**, a **per-agent breakdown** (accurate to the model each session used), and a collapsible **token-pricing reference** (USD / 1M tokens, published list prices). Local models are $0; everything is labelled an estimate. Computed in the host from the usage log (`totalCost` / `bySessionCost` / per-day `cost`).
- **Automation tasks** `[shipped]`, **scheduled** (run once at a time), **recurring** (every interval), and **background** (long-running agents that stay alive **forever** or **until a condition** the agent judges met) tasks, shown in the Command Center's **Tasks & scheduled work** board (cadence, status, run count, last result; Run-now / Pause-Resume / Open-chat / Delete; live updates). A host **scheduler** fires due tasks through the normal agent loop, reusing one chat session per task so background agents keep context. Created from a chat via the **⚡ Automate** menu (pre-filled with the chat's project/model/draft).
- **Skills in the `/` menu** `[shipped]`, the composer's `/` menu lists **skills**, standard capabilities any agent can run (research, plan, review, security-review, simplify, test, explain, fix, commit, pr), above the user's saved prompts. Invoking a skill attaches it as a compact usage chip in the composer and in the sent message instead of dumping its template into the text box; the full instructions are still expanded into the prompt sent to the model. **`goal` is a highlighted (★) skill**: `/goal <condition>` starts a long-running **background agent** that works until the condition is met (a real "work until done" loop), surfaced in the Tasks board.
- **Skills tab & workflow visualizer** `[shipped]`, a dedicated **Skills** view in the left rail. Skills are grouped by category (Research & planning, Code quality, Delivery, Automation) in a searchable list; selecting one shows its description, the tools it uses, and a **workflow visualizer** that renders the skill's steps as an n8n / Make-style node graph (trigger → context → agent → tool → decision/loop → output) on a dotted canvas, with colour-coded node types, bezier connectors, branch labels, and dashed return arrows for loops (e.g. `/goal`'s "goal met? → no → keep working" cycle, `/research`'s fan-out to parallel searches). A **Use in chat** button adds the skill as a usage chip in a chat composer. The graph layout is a pure, unit-tested layered algorithm in `shared/skills.ts` (`layoutWorkflow`), so each skill carries a real `workflow` definition rather than ad-hoc art. The canvas is also a **teaching surface**: an explainer panel beside it walks the steps in run order ("How this skill runs"), and clicking any node, on the canvas or in the list, explains that step: what it does, what its node kind means (trigger / context / agent / tool / decision / loop / output), and where its branches and loops go ("yes → Done", "no: loops back to Do the work").

- **Vaizer skills hub** `[shipped]`, the Marketplace has a **Vaizer** shelf connected to the public skills marketplace ([nekko-labs/vaizer](https://github.com/nekko-labs/vaizer), browsable at vaizer.app/skills). An optional integration in the Kotrain-apps spirit: the shelf renders **fully offline** from a bundled catalog snapshot (or the last cached fetch), and only touches the network when the user clicks **Refresh** (pulls the live catalog) or installs (fetches the skill's real, current SKILL.md so file-based installs get the verbatim skill, not a summary). Skills carry Vaizer's **trust tiers** (🟣 official, built + reviewed by Nekko Labs; 🟢 community, audit before use, with an in-app caution note) and install one-click into Kotrain, Claude Code, or Codex like any marketplace skill; those installs snapshot the skill onto the install record so they stay runnable offline and after catalog changes. (Shipped against `nekko-labs/nekko-dojo-skills` as a "Kotrain Dojo" shelf; that repo folded into the Vaizer repo on 2026-08-02 and the shelf was renamed with it.)
- **Skills marketplace** `[shipped]`, the Skills tab has a **Marketplace** with three shelves: **Installed** (what you added, and where each copy lives), **Nekko Labs** (first-party skills: review-council, spec-sync, changelog, standup, dep-audit, a11y-audit), and **Popular online** (well-known ecosystem skills, Anthropic's official skills, superpowers, and community packs, ranked by public star/install metrics; the catalog ships as a curated offline-first snapshot). A skill can be **installed into Kotrain** (it joins the composer's `/` menu and the Skills library) or **exported to other agent apps** that read the SKILL.md convention, Claude Code (`~/.claude/skills`) and Codex (`~/.codex/skills`), with per-target availability detection, safe no-clobber installs, and one-click removal. Every marketplace skill renders in the same workflow visualizer (bespoke graphs for the flagship skills, a derived graph otherwise) plus a "what it tells the agent" instructions card.

### Files, editor & visual surfaces (IDE-like, "stay in the app")
- **Built-in file viewer/editor** `[shipped, editing pass 2026-07-28]`, clicking a file (or a spec/plan/tasks row, or a tree entry) opens it as a workbench **pane**, splittable side-by-side with chats and terminals, never handed off to the OS. Markdown renders as a **document** (a real heading scale, reflowed paragraphs, tables, task-list checkboxes, embedded HTML reduced to its text, images and relative links as chips that open the real file) with a Source/Preview toggle. Other text opens in a lightweight mono editor that behaves like an editor: **auto-save** on an idle pause (toggleable, remembered, flushed on tab switch and on quit), **undo/redo** with its own history (Ctrl+Z / Ctrl+Shift+Z, coalescing a typing run into one step), **copy / cut / paste** actions beside the shortcuts, Tab-to-indent, Ctrl/Cmd-S, a save-state readout (Saving shortly → Saved), and binary/oversized-file guards. Fixes the old dead-click where `.md` files silently failed to open (`shell.openPath` with no registered handler).
- **Integrated browser pane** `[shipped]`, a Chromium `<webview>` pane with a URL bar (back / forward / reload / open-external) for previewing a local dev server or docs right next to the code that drives them (code-left, preview-right).
- **File explorer, inside the right panel** `[shipped, reworked 2026-07-28]`, the file tree lives in the right-hand panel's **Folders** section rather than in a side-pane of its own: each project folder is an accordion, and expanding one reveals its lazy-loaded VS Code–style tree with color-coded file-type icons. Clicking a file opens it in a tab and edits in-app. Choosing what the agent can see and browsing what's there are the same act, so they're the same control. (Superseded the separate dockable **Files** side-pane, which duplicated the Folders list one pane over.) Not a full IDE, no language servers, debugger, or Monaco.
- **Diff & approval (Devin-style)** `[shipped]`, every file the agent touches this session is snapshotted on first write; a **Changes** panel (a `Δ N` button in the chat header) shows a client-side line diff and lets you **keep / revert** at **line**, **file**, or **all-files** granularity, live-updating as the agent edits. Writes still happen immediately (the tool loop is never gated); "reject" reverts.
- **Hoverable Context Inspector** `[shipped]`, every Context Inspector section and source explains itself on hover (what it is, why it's included, how to control it), turning the provenance panel into a teaching surface.
- **Prompt analyzer** `[shipped]`, a live, fully client-side (no LLM, zero-latency, offline) composer analyzer that identifies the parts of a prompt (role / task / context / examples / output-format / constraints / tone / variables…), underlines weak spots inline, gives an **A–F health score**, and recommends a model tier (fast / balanced / frontier), a marketable always-on coaching surface. Missing-part chips are **click-to-fill**: clicking **+ Role / + Task / + Context / + Examples / + Format / + Constraints** inserts a starter snippet matched to what the prompt is about (code / writing / data, question vs. task) via instant offline text matching, role at the top of the draft, everything else appended. An opt-in LLM "Improve prompt" rewrite (before/after diff) and model-generated part fills are deferred.
- **Inline editor comments** `[shipped]`, in the file editor, a **+** appears on each line (a scroll-synced gutter); clicking it opens an inline comment box. The comment is captured with its file + line context and the agent picks it up via one of two actions: **Add to prompt** (queues the comment into the composer for the next turn, so several annotations batch into one ask) or **Run now** (dispatches it immediately to the active chat). A line keeps a gutter marker (persisted in `comments.json`) until resolved; a bottom dock lists a line's comments with re-send / resolve. Lets you review code in place and turn margin notes into agent work without leaving the file.
- **Design board (Figma-style)** `[shipped]`, a **Design** tab that lays out the app's UI pages as snapshots on a zoomable board, like a Figma canvas. Live-page snapshots are **read-only live previews**, each card is a scaled, non-interactive `<iframe>` of the page (so it mirrors the app as it actually renders, no capture infra). Click a card to attach **persistent notes** (saved in `design.json`, survive across sessions) or **comments** that feed the prompt, the same **Add to prompt / Run now** actions as inline editor comments. As the agent edits the UI, previews **reload so you watch the design update live**; a per-page **"updating" indicator** flags an in-flight change, and clicking it jumps to the driving agent's chat pane. A visual companion to the file/diff surfaces: see the app as pages, not just files.
- **Design studio (sketch → prototype, prompt → design)** `[shipped]`, the Design tab's creation layer, a Claude Design-style loop. **Draw it**: a pointer-events sketch canvas (Apple Pencil / touch friendly, `touch-action: none`, DPR-sharp, pen colors/sizes/eraser/undo) whose drawing plus an optional note goes to a vision model and comes back as a working prototype. **Describe it**: a prompt studio (with starter idea chips) that generates a design one-shot. Concepts render on the board via iframe `srcdoc`, are **mirrored into the workspace as real files** (`kotrain-designs/<slug>.html`), and carry a **Refine** box in their side sheet that regenerates the prototype from a follow-up instruction while keeping the rest intact. Generation runs host-side (`design:generate`) with the default provider/model, one-shot, no agent loop; concept comments route to the agent with the prototype's file path so agent iteration works on the real file.

### Model training & goals (the data-scientist agent)
- **Training tab** `[shipped]`, train a model in a simple UI (purpose + dataset + base model) with full expert levers (framework, epochs, batch, lr, metric + direction, experiment/time budgets, free-form constraints). Datasets from Hugging Face, Kaggle, local paths, or URLs; base models from Hugging Face or local. A data-scientist agent runs the actual experiments in a dedicated chat session (hidden from the regular chat boards); the dashboard shows a one-line explainer of what a run does, a live stat tiles row (best score, experiments, success rate, emergent niches, self-repairs, runtime, turns), a current-champion card, and the **idea maze** (every idea tried, growing over time, path-to-leader highlighted, click a node for details). A **live "now" strip** calls out what the run is doing right this moment (an explicit "starting up…" state the instant you hit start, then the currently-running experiment plus the latest activity line), and starting a run takes you straight to its dashboard. An **Artifacts card** shows exactly what the run produced (the trained model plus each harness file), where each lives, and how to use it, with every path and the run's output folder openable in the OS file manager. Run controls are grouped as one cluster (Start · Pause · Stop) set apart from Open chat / Delete. **Harness output**: a run can produce an agent file / skill / spec beside the model for its use case. The form also takes an optional **data-scientist model** override (the model running the experiments, distinct from the model being trained; App default otherwise).
- **Goal runs (plan-first)** `[shipped; the Goals tab became Workflows 2026-08-13]`, long-running goal solving for real work, not model testing: the agent **plans first** (an ordered execution plan of concrete, verifiable steps, written before any execution), **executes** step by step (statuses + one-line outcome notes, done only when verified), and **iterates** (revises the plan when reality disagrees) until the goal is finished. Dashboard built for hours/days legibility: "working for" clock, phase chip (planning/executing/verifying/finished), the live plan checklist with progress bar, current-step card, goal-shaped stat tiles, and the activity feed. Runs auto-resume after a host restart. The dedicated Goals **tab** is gone (that nav slot is now Workflows, which covers repeatable procedures rather than one-off goals); goal runs are listed alongside training runs in the **Training** tab so any that already exist stay reachable, and `/goal` in the composer still starts a background goal task.
- **Mid-run guidance** `[shipped]`, both tabs accept hints, new approaches, or new-data pointers at any time; unconsumed hints queue as chips and fold into the agent's next turn.
- **report_experiment tool** `[shipped]`, run-driven sessions get an extra builtin tool the agent uses to register/update experiments (id, parent lineage, approach family, status incl. self-repair, score, metric, note); the host tracks the leader and replies with the current standing so the agent always knows what to beat.
- **report_artifact tool** `[shipped]`, run-driven sessions also get a tool to register the concrete outputs a run produces (kind: model / agent-file / skill / spec / dataset / code / report / other, plus a title, workspace-relative path, and how-to-use note). Upserts by path; the host logs the first sighting as a milestone. This drives the dashboard's **Artifacts card** so a finished run answers "what did I get, and where is it?" at a glance instead of leaving it buried in the log. The agent brief instructs the model to call it the moment each artifact (the trained model + every harness file) exists on disk, and to keep everything under the run's `kotrain-training/<slug>` (or `kotrain-goal/<slug>`) output folder.
- **update_plan tool** `[shipped]`, goal-run sessions also get a plan tool: replace=true writes/rewrites the ordered plan, upserts keep step statuses (pending/active/done/skipped) and notes current. The host logs step completions as milestones, drives the goal-run checklist live, and echoes the plan back so the agent always knows each step's id.

### Workflows (procedural automation)
- **Workflows tab** `[shipped 2026-08-13, replaced the Goals tab]`, GitHub-Actions-style automation where a step is an **agent prompt**, an installed **skill**, another **workflow**, or a **shell command**. Steps are **routed, not just sequenced**: each declares where the run goes on success and on failure, and a route may point back at an earlier step, so build → verify → back to build is a first-class shape (bounded by a per-step re-entry cap so a non-converging workflow still terminates and says why). Steps carry retries, a timeout, and continue-on-error. Agent steps end with a **PASS/FAIL token** so they can be routed like a shell exit code, and every agent step in a run shares one chat session so later steps see earlier work. Each run records every attempt (step, attempt number, outcome, output/error, and a link to the step's transcript); history is capped per workflow, and a run interrupted by a quit or crash is written off as interrupted on the next boot rather than looking stuck.
- **Triggers** `[shipped]`, per workflow, any number of: **manual**, **schedule** (a 5-field cron expression, parsed in shared so the editor validates it and previews its next fire, or a plain every-N-minutes interval), **CLI** (`kotrain workflow run <name>`; `kotrain workflow trigger <command>` fires everything listening for that command), **Slack** (channel + optional keyword), and **git provider** events (PR opened / updated / closed / merged / commented / reviewed, push, tag, issue opened / commented) filtered by repo and branch globs. Provider events are **normalized across GitHub, GitLab, Bitbucket and other**, so a workflow reacts to "a PR opened" regardless of whose vocabulary the webhook used. Every transport enters through one host door (`workflow:event`), which in the web edition is reachable as an HTTP endpoint for a Slack app or a webhook, so all of them match by the same rules.
- **Built for hundreds** `[shipped]`, the list filters by search (reaching into step bodies, not just names), enabled state, trigger kind, and category, and groups either **by category** or **by event listener**, a workflow appearing under every event it listens for. Groups collapse and cap their rows with a "show more", so a few hundred workflows stay navigable.
- **Step canvas** `[shipped]`, expanding a row (or opening the editor) draws the step graph as a node canvas in the same layered-node visual language as the Skills tab (the layout code is now shared by both). Failure routes are drawn in the danger color, loop-backs dip under the row as dashed edges, and terminal Done / Failed nodes close the graph. With a run selected the same canvas shows that run's per-step outcome and attempt count, so the shape of a workflow and how it last behaved are one picture.
- **Editing** `[shipped]`, create from a **template** (build/verify loop, PR reviewer, nightly maintenance sweep, or empty) or from scratch; the editor redraws the canvas as you type, warns about steps nothing routes to, and re-points any route left dangling by a deleted step. Workflows can be duplicated (arriving disabled, so two don't answer the same trigger by accident), enabled/disabled, and deleted (which takes the run history with it).
- **Integration triggers** `[planned]`, beyond manual/schedule/CLI, a workflow can fire from the user's connected apps: **connector triggers** poll the configured Linear, Jira, Slack, Discord, or Teams account on an interval for new matching events (polling is the default because a desktop app behind NAT can't receive inbound hooks), and a **webhook trigger** gives each workflow its own secreted URL (`POST /api/hooks/<slug>`) on the server/Docker editions, with an opt-in loopback listener on desktop. Every external event still enters through the single `workflow:event` door, so matching rules stay identical however the event arrived.
- **Action steps** `[planned]`, a fifth step kind that pushes results back out through the integrations: post a Slack or Teams message, comment on or create Linear/Jira issues, comment on a PR, set a commit status. Fields interpolate run context (`{{trigger.*}}`, `{{steps.x.output}}`, `{{run.status}}`), and the editor offers real pickers (channel, issue, repo) instead of raw payloads.
- **Local CI runner** `[planned]`, a workflow template that turns a machine into a private runner for git-host events: on `push`/`pr_opened` it checks out the ref, runs the repo's build/test steps locally, then reports the outcome back as a **commit status** (`agent-nekko/<workflow>`) and, on failure, a PR comment with the output tail. This is how a team spends their own hardware instead of hosted-runner minutes; it deliberately is *not* an implementation of the GitHub Actions runner protocol, and untrusted-PR execution needs its own review before enablement (see Scope Boundaries).

### Context, guardrails & memory (the differentiators)
- **Context Inspector** `[shipped, restructured 2026-07-28]`, the right-hand panel, now **two stacked sections split by a draggable divider**, the way VS Code stacks its sidebar views:
  - **Folders** on top: each project folder is an accordion carrying what the agent may see (primary / supporting / excluded, promote-to-primary, remove) and expanding into that folder's **file tree**, so browsing and opening a file happens where you choose the context. Folders support one primary plus supporting roots, and the selection drives the chat's multi-root context.
  - **Context** below: Sources (attached files, SPEC.md, guideline/memory links) + Breakdown (live per-source token view, toggle/pin, headroom bar). The former Project context section is now **Guidelines**, above the Spec panel, with each guideline and memory entry individually enableable or disableable.
  - The split **gives and takes ground**: with every folder collapsed, Folders is content-height and Context owns the panel; opening a tree hands Folders the height you dragged it to, capped so Context always keeps a usable minimum, and the height you asked for is remembered rather than clamped away, so freeing space (collapsing the resource meters) returns it. The **Resources** dock at the foot is collapsible for the same reason, keeping its headline numbers in the header when closed.
  - The panel belongs to the **workbench**, not to a chat pane, so opening a file from the explorer doesn't take the explorer away with it; it follows the active chat and says so when there isn't one. Toggled from the workbench sidebar or Ctrl/⌘+\.
  - Prefs persist per session and actually shape the prompt. **Both readouts move as you type**: the composer's context gauge and this panel's total, bar, and breakdown all count the unsent draft as a **Your message** source, so the cost of what you're about to send is visible before you send it rather than only after the reply lands.
- **Spec-driven development** `[shipped]`, first-class Kiro-style spec workflow built from the conversation. Pick a **methodology** (per-chat + global default): `spec-tasks` (**Spec → Tasks**, spec.md → tasks.md with no separate plan), `kotrain` (**Spec → Plan → Tasks**, spec.md → plan.md → tasks.md), `kiro` (requirements.md → design.md → tasks.md), or `lean` (single spec.md). Spec → Tasks is the default, while the other methodologies remain available. Each artifact has its own **Build/Update** button and is **chained** where the methodology provides earlier artifacts as context. When a chat has multiple context folders, the Spec panel **follows the primary folder** chosen in the Folders section right above it, and its own project select overrides that until the choice above moves again, so spec and tasks can be viewed, built, and toggled per workspace without ever showing one project's docs while the pane says another is primary. A "Build all from chat" button runs the whole pipeline. The **tasks artifact renders as an interactive checklist** whose toggles write `[ ]`/`[x]` back to the file, with a progress bar. Lives in the Context Inspector's Spec panel. A **Live** toggle keeps the primary spec rebuilt after every reply.
- **Guardrails** `[shipped]`, risky-command classifier (destructive fs, force push, curl|sh, registry, privilege escalation) + allow/ask/deny policy engine with a default ruleset; settings UI with a guardrails JSON editor.
- **Memory** `[shipped]`, markdown memory store (global + per-workspace) with CRUD and a manager UI.

### Workspaces, indexing & connectors
- **Workspaces** `[shipped]`, multi-root folder add/remove/activate, chats bound to one primary workspace plus optional supporting workspaces so index + per-project memory engage across every selected folder while tools keep the primary as their working directory.
- **Indexer** `[shipped]`, workspace file tree, regex-based code outline, search; index-grounded context merged across a chat's selected primary and supporting folders.
- **Projects page** `[removed 2026-07-18]`, the dedicated Projects view (folder add/remove, index status, file browser) was retired to keep the rail focused. Folders are added from a chat's **+ → Folder** menu or the Context Inspector; files are browsed in the right panel's Folders accordions; the index keeps powering `@` file mentions and context under the hood.
- **Connectors** `[shipped]`, GitHub/Linear/Slack/Discord (token-based, real REST), Jira (site URL + email + API token) and Microsoft Teams (incoming webhook for posting, optional Graph token for reads) `[planned]`, Gmail/Drive (paste an OAuth access token; one-click OAuth flow still pending Google client creds); connector snippets fed into context. **Official brand icons** (Simple Icons, CC0, in brand colours) and **validate-on-connect**, Connect stores the token and immediately does a real fetch, surfacing a bad token as an inline error instead of silently "connecting"; each card links to where to get its token.
- **Nekko inside other agent tools** `[planned]`, one-click install of the app as an MCP subagent into detected agent CLIs (Claude Code, Codex, Cursor): the app is already an MCP server (`kotrain mcp`), so connecting writes that server into the other tool's config (a backup is made first), and a copyable config snippet covers tools we can't write to. From then on Claude Code or Codex can hand Nekko scoped tasks, which run under this app's guardrails on this machine's models.

### Tooling & integrations
- **MCP client** `[shipped]`, connect stdio MCP servers **or streamable-HTTP endpoints** (a URL + optional bearer token, added via "+ Add URL" in Settings); their tools merge into the agent loop namespaced `mcp__<id>__<tool>`, with per-chat toggles and live status. Verified against `@modelcontextprotocol/server-filesystem` (stdio) and a live Hypergate gateway (HTTP). An **MCP menu sits in the composer's instrument strip, just right of Tools**: it lists every configured server with a live status dot (connected / offline / checking), its name and transport, and an **enable/disable switch** (server enablement is global, so turning one on offers its tools to every chat); it probes live status on open, and offers **Add MCP server** (jumps to Settings) plus a recommendation to manage servers securely with the **Hypergate** app.
- **Hypergate integration** `[shipped]`, Kotrain pairs with [Hypergate](https://hypergate.app), the Nekko Labs MCP server runtime and manager, in one click from either side, and then runs it as a tab in this window.
  - **Detect.** Kotrain probes the local `hypergated` daemon at startup (host-side, so it works in every edition) and shows a card in Settings → MCP servers naming its version, its managed-server count, and its port. With no daemon running, a quiet **Get Hypergate** hint makes the companion discoverable without nagging.
  - **Connect (from Kotrain).** One click claims this install's own scoped agent token from the daemon, saves the aggregated gateway as a single MCP entry (every server Hypergate manages, one URL), brings its tools online in every chat, and opens the Hypergate tab. Re-connecting replaces that entry rather than adding a second, and upgrades the pre-rename `kotrain-mcp` row.
  - **Connect (from Hypergate).** Hypergate's **Connect Kotrain** button opens `kotrain://hypergate/connect?port=N`, which brings Kotrain forward (launching it if it was closed) and runs the same flow. The link names a port and carries no credential: Kotrain reads the daemon's identity back over loopback and asks the user before connecting, because any local program can open a URL and a gateway's tools reach every chat.
  - **Scoped, not borrowed.** The token is Hypergate's own per-agent credential rather than the master gateway token, so the pairing is visible and governable from both sides: Hypergate's Agents list shows Kotrain online, with its own per-server permissions, usage history, and revocation.
  - **Hypergate as a tab.** The manager's UI runs in a workbench pane (`hypergate` pane kind) with the daemon's live version, server count, and connected agent in its header, so adding or auditing MCP servers never means leaving Kotrain. It says so and offers to reconnect when the daemon stops.
- **`kotrain` CLI + MCP server** `[shipped]`, run the host in-process or against a running server over HTTP+WS; `kotrain status|sessions|chat|mcp|watch`; `kotrain mcp` exposes Kotrain as an MCP *server* (chat/list/new/get/status) so other agents can trigger agents + swarms. Bundled into the npx package.

### Mascot, polish & onboarding
- **Aphelion mascot** `[shipped]`, a 2D hand-drawn **outline astronaut cat**: every pose is a single ink stroke (`var(--ink)`) on the theme's paper (`var(--paper)`), no color fills except the ginger tail and inner ears, so she rides the theme tokens and works on light and dark chrome without per-theme tuning. A turbulence **line boil** re-seeds a few times a second so the strokes wobble like frame-by-frame ink even when the pose is still; it switches off under reduced motion and while the document is hidden. The body and helmet carry a paper fill purely for occlusion (nearer shapes hide the lines behind them, the inked-animation trick). The activity poses are unchanged: lying (with tucked front-paw rolls peeking under the chest), waking stand, stretching, bug watch, sleeping. `AphelionAvatar` (the Agent tab, the send control, login heroes) and `MiniAphelion` (inline spinner, heavier stroke for 18px legibility) share the outline dome; the boil stays exclusive to the rail mascot so the composer stays calm. (The filled astronaut illustration this replaced is described in TASKS T114/T124.)
- **Onboarding wizard** `[planned]`, the first-run setup described under "First-run setup": a full-screen, every-step-skippable wizard covering theme, providers, and integrations, replayable from Settings, identical on every edition because it shares the renderer.
- **Theme presets** `[planned]`, System / Light / Dark plus a set of elegant tech palettes (e.g. Nebula, Terminal, Nord, Solar, Ember), each rendered as a gradient swatch circle of its colors and previewed live on click. Presets extend the existing token theming (mode + accent + a second accent, plus deeper surface overrides where a palette needs them) and appear in Settings → Appearance too.
- **Polish** `[shipped]`, welcome/empty states, command palette (Ctrl+K), keyboard shortcuts, toasts, onboarding banner, persistent Context Inspector prefs, app icon, boot smoke test. Shortcuts are **discoverable, not folklore**: new agent (Ctrl/⌘+N) and new terminal (Ctrl/⌘+`) show their key hint next to the matching item in the workbench **+** menu, on the empty-state buttons, in tab-strip tooltips, and in the command palette. Every chord and its printed label come from one definition, so they can't drift apart, and the labels render as ⌘ on macOS.
- **Refined design system** `[shipped]`, a Warp/Linear-grade visual pass: cleaner cooler neutrals with more breathing room, soft elevation shadows on cards, a **secondary color** (`--accent-2`) paired with the accent into a **brand gradient** (the Command Center wordmark, primary buttons, the rail logo, and the active-nav indicator), focus rings, and tighter heading tracking. All theme tokens (incl. `--accent-soft`) track the user's chosen accent. Approachable for non-technical users while keeping the Aphelion mascot.
- **Border beam** `[shipped]`, the border-beam motion pattern in brand colors: while the agent is working, a violet→cyan beam laps the chat composer's **border only** (the earlier interior color wash was removed, so the surface stays still as you type); the marketing site's hero window mock and download button carry the same beam (the button's speeds up on hover). Static ring without `@property` support; still under reduced motion.
- **App icon: the orbit mark** `[shipped]`, an abstract mark rather than the mascot, so it survives being 16 pixels tall: a comet running a tilted orbit, its tapered trail fading violet into cyan (the brand gradient) over a deep-space squircle with a nebula wash and a scatter of stars. Drawn as vector in `apps/desktop/scripts/make-icons.cjs` and rasterized by Electron itself (`npx electron apps/desktop/scripts/make-icons.cjs`), which writes `icon.svg`, `icon.png` (512), a true multi-size `icon.ico` (16/24/32/48/64/128/256), the web/PWA `icon-512.png`, and the two NSIS installer bitmaps. Small sizes are drawn with their own settings (no stars, heavier stroke, larger body) instead of being downscaled from the large one. This retires the pre-rebrand orange pixel-cat icon, and the installer art that still read **"Open Paw"**.
- **Fresh color palette** `[shipped]`, the default accent is a modern **indigo-violet** (`#6d5efc`) paired with a **cyan** secondary into a violet→cyan brand gradient, the current developer-tool look (Linear/Vercel-era), replacing the original salmon/orange. Neutrals were cooled to match. Existing installs are **migrated** off the old default automatically (users who picked their own accent keep it); the accent is still fully recolorable in Settings. Applied across the app, the marketing website, and the branded installer art. The Aphelion mascot stays a ginger cat, and lives in the UI rather than in the app icon (see the orbit mark).
- **One title bar, no menu bar** `[shipped]`, the desktop window used to stack three strips above the app: an OS title bar, a File/Edit/View/Window menu, and then the UI. The menu is gone (it only duplicated shortcuts the app already owns, and macOS keeps its own, which lives in the system bar rather than the window), and the title bar is now the app's: a 38px strip in `--paper` carrying the Kotrain wordmark, with the **native window buttons drawn into it** — Window Controls Overlay on Windows and Linux, painted in the app's own background and repainted whenever the theme changes, and macOS traffic lights inset onto the same line. The strip is the drag region, so dragging it moves the window and double-clicking maximises. The wordmark moved out of the rail, which only ever showed it on hover, so the rail starts on its first destination and the app is a row shorter overall. Reload (Ctrl/⌘+R) and devtools (F12) were the only menu items worth keeping and are bound directly. Nothing renders in the web or phone builds, which have chrome of their own.
- **Multi-language UI (i18n)** `[shipped]`, system-language detection + English fallback; en/es/fr/de/pt/ja/zh.
- **Data & privacy controls** `[shipped]`, delete chats by window, Reset configs, Delete everything; settings export/import.

### Distribution & platforms
- **Marketing website** `[shipped]`, static site (kotrain.com) rebuilt in an immersive deep-space style inspired by liquidink.design's GitHub concept: drifting starfield canvas, nebula gradients, a giant KOTRAIN display hero ("Train your models. Run your agents. Own your machine."), glass panels, numbered chapters with scroll reveals, the astronaut Aphelion drifting in zero-g with a spark lapping it in the hero, plus the app-window mock, positioning cards, and edition cards. Dark-only by design; reduced-motion safe; still no-framework hand-crafted HTML/CSS/JS. The narrative leads with the **local-models spotlight** (chapter 01), then **"The harness is the other half of the model"** (chapter 02): the argument that a model you run yourself competes on real work through the scaffolding around it, laid out as a three-group spec-sheet manifest (what it sees / what it can reach / when it's wrong) rather than a card grid. The download chapter is a **smart download button**: it detects the visitor's OS and architecture, resolves the real asset URL from the GitHub Releases API (with the releases page as the offline/rate-limited fallback), and a caret opens a menu of every other platform and format (installer, portable, distro package), keyboard navigable and marking the build we picked for you. The **hero CTA is that same button**, so the first click downloads instead of scrolling to a second button, and the GitHub link beside it is a quiet outline pill. Both share **one button shape with hypergate.app**, the sibling product: a fully rounded single-line pill, dark ink on the brand gradient, the version in a mono chip behind a hairline divider, and an equal-height outline pill next to it (format and architecture move to the link's tooltip and the caption under the button). Below the download button, terminal people get the **same build as a one-liner in their own shell's syntax** (PowerShell `iwr`, `curl` + `open` on macOS, `curl` + `chmod` on Linux) with a copy button; it appears only once the real asset URL resolves, so the command is never a guess.
- **Desktop packaging & release** `[shipped]`, electron-builder (win/mac/linux), GitHub Actions release workflow, branded NSIS installer + Windows uninstaller, **macOS Developer ID signing + Apple notarization** (hardened runtime, entitlements, stapled ticket, and a `codesign`/`stapler`/`spctl` check that fails the release rather than the user's machine; ad-hoc signing survives only as the fallback that keeps unsigned local builds launchable), and `electron-updater` against published GitHub releases. The desktop title bar's version pill is the complete update experience: it becomes **Check for updates** on hover or keyboard focus, keeps fast checks legible for at least 500 ms, confirms `latest`, offers **Download & install**, **Download only**, or **Skip** for a newer version, shows real download progress, stages **Install & restart**, stays visible while restarting, reports **Updated to vX.Y.Z** on the next launch, and exposes retry/details on failure. Skipping suppresses only that release and a manual check restores it. Settings uses the same live state and actions, while the existing first-run auto-check consent remains in place. Web keeps the same-build comparison and refresh fallback instead of desktop-only install controls. Released through v0.6.0, which is the first build actually signed with the Nekko Labs Developer ID certificate and notarized by Apple.
- **Self-hosted web edition** `[shipped]`, `npm run web` / `npx kotrain` serves the same UI; localhost bind by default, token to expose.
- **Docker edition** `[shipped]`, multi-stage image, compose with volume workspaces + `host.docker.internal`, non-root, GHCR publish.
- **Native phone apps (Capacitor)** `[shipped]`, wraps the shared renderer; relay pairing (paste link + QR, device identity, revoked/expired feedback), local notifications; Android APK + iOS simulator builds verified on CI.
- **Remote push (relay-mediated)** `[shipped]`, APNs + FCM, dependency-free, content-free "a run finished" notifications, tokens registered **per device** and dropped on revocation; real delivery pending creds + device.
- **Phone remote control** `[shipped]`, run and manage chats, training, and goals from a paired phone. Relay v2 (per-device connections, unicast routing, rate/size limits), agent-side **device registry** with 10-minute single-use pairing codes, rename/revoke (live kick), and secret rotation; Remote access settings card (device list, expiring pair QR); phone **bottom tab bar**; remote access persists across restarts. Managed relay live at `wss://kotrain-relay.fly.dev` (free during beta; entitlement gate built for the Cloud launch); self-hosting is one Docker command (`ghcr.io/nekko-labs/kotrain-relay`, Compose/Coolify/Fly documented). Verified end to end through the live managed relay (enrollment, single-use codes, remote device management, revocation). See [docs/REMOTE.md](docs/REMOTE.md).

### Kotrain Cloud (paid, hosted)
- **Cloud foundation** `[shipped]`, multi-account hosted edition wrapping the same engine + renderer per account; email+password auth, bearer tokens, per-account data isolation, entitlements (free/pro/team) gated server-side. The OSS app never license-checks.
- **Phone↔local-model relay** `[shipped]`, see "Phone remote control" above: relay v2 + device registry + revocation + managed instance on Fly.io. The relay carries an **entitlement gate** (`KOTRAIN_RELAY_AUTHZ_URL` → the cloud's `/api/relay/authorize`, which checks the account and returns the plan's device allowance) so the managed relay becomes a paid Cloud perk at launch while self-hosting stays free; the gate is off during beta.
- **Payments (Stripe)** `[shipped]`, Checkout (upgrade to Pro/Team) + Customer Portal (manage/cancel) + signature-verified webhooks that drive an account's plan (and therefore its entitlements). Hand-rolled against the Stripe REST API (no SDK dependency) and gated on `STRIPE_SECRET_KEY`, so the cloud server runs fully without a Stripe account; real charges light up once keys are configured.
- **ZDR + cloud history/files** `[planned]`, retention modes, encrypted-at-rest sync engine, ZDR badges + audit log.
- **Managed connectors** `[planned]`, pre-registered OAuth apps for Gmail/Drive/Slack/Discord.

## Scope Boundaries

- **No native node modules**: keeps Electron rebuild pain away; ripgrep/git are spawned via child_process with JS fallbacks. (Technical constraint; see `TASKS.md`.)
- **Not a full IDE**: the file editor is a lightweight mono textarea (+ rendered markdown), not Monaco/CodeMirror; no language servers, debugger, multi-cursor, or refactoring tooling. The browser pane is a preview/utility surface, not a hardened general web browser. The Design board (planned) shows snapshots, not an editable vector canvas.
- **Cloud never runs your shell/filesystem tools on our servers**: inference and tool calls always execute on *your* paired machine. The cloud is a sync + relay + billing layer, not a remote executor.
- **The OSS app never checks a license**: paid features are gated server-side in Cloud only; the open-source editions are fully functional and free.
- **The relay is a dumb pipe**: it authenticates/routes and forwards ciphertext; it does not inspect or store message bodies.
- Pricing tiers (Free OSS / Pro / Team) are drafts pending cost modeling, see [spec-web-and-hosted.md](spec-web-and-hosted.md) §6.
- **Workflows don't execute untrusted code from strangers**: a git-triggered workflow that checks out a pull request runs that contributor's code with this machine's tool access. Local CI runner templates must only auto-fire on trusted refs (own branches, collaborators' PRs); running arbitrary fork PRs is an explicit opt-in with sandboxed (yolo-off, approval-gated) tool policy, not a default.
- **Subscription sign-in rides first-party OAuth clients**: the Claude and ChatGPT subscription flows use the public OAuth client of the vendor's own CLI. Vendor terms are written around their own apps, so the feature is offered as-is, clearly labelled, with the API-key path unaffected. If a vendor cuts it off, the app degrades to API keys, not a broken product.

## Open Questions

- Hosting/runtime for Cloud: **the relay now runs on Fly.io** (`kotrain-relay.fly.dev`, Nekko Labs org, single machine since rooms are per-machine memory); the Cloud app's host is still open (Fly.io is the natural default now). Relay scaling beyond one machine needs sticky routing (fly-replay by room) when it matters.
- E2E key-management UX (recovery vs zero-knowledge trade-offs).
- Mobile: PWA first, or native shells later? (Native Capacitor shells now exist; PWA also works.)
- Exact ZDR boundary for cloud-configured (non-local) providers without ZDR tiers, likely "not allowed in ZDR workspaces."
- Do desktop + local web share one data dir by default, or separate? (Lean: share, so the web UI sees your desktop sessions.)
- "Agent Nekko" as a local provider in onboarding presumes managed in-app inference (the AN9 track). Until that lands, the onboarding card deep-links to the Models page's local setup; it should become a real one-click managed-model install when AN9 exists.
- Microsoft Teams reads need Microsoft Graph OAuth, which requires an app registration; v1 supports posting via incoming webhook and reads via a pasted token. Whether to register a first-party Nekko Graph app is open.
- Full Gmail/Drive OAuth flow (needs Google client creds); macOS signing + notarization is **wired in CI and waiting only on the `nekko-labs` org secrets being uploaded** (the certificate exists; GitHub secrets are write-only so it must be exported and set by hand, see [docs/signing.md](docs/signing.md)); Windows Authenticode pending SignPath Foundation; npm publish of `kotrain` (needs the user's npm login), all pending user-side credentials. Provisioning details in [provisioning.md](../../obsurdian/projects/agent-nekko/provisioning.md).
