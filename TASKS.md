---
status: active
last-updated: 2026-09-07
owner:
---

# Execution Plan, Agent Nekko

> Converted from executionplan.md on 2026-06-29. ✅ = done per the prior plan; Part 1 below is the technical plan, Part 2 is the task checklist.

> **The plan + the build log, in one file.** Part 1 is the **technical plan**, how we build what [SPEC.md](SPEC.md) describes (stack, architecture, data model, conventions, design system, constraints). Part 2 is the **task list**, Now / Backlog / Shipped, recording *how every past feature was built and how future features will be built*, with the context around them. The full technical design for the web/Docker/Cloud editions lives in [spec-web-and-hosted.md](spec-web-and-hosted.md); credential/ops setup lives in [provisioning.md](../../obsurdian/projects/agent-nekko/provisioning.md), referenced here, not duplicated. (Merged from the former `plan.md` + `tasks.md`.)

---

# Part 1, Plan (how we build it)

> The **technical plan**: how we build what [SPEC.md](SPEC.md) describes. Update this whenever the technical approach changes.

## Stack (decided, do not relitigate)

- **Monorepo**: npm workspaces. **No pnpm** (broken on this machine by a corepack/yarn override up the directory tree).
- **Desktop**: Electron **43.3.0** (exact-pinned under workspaces) + Vite 7 + React 19 + TypeScript 7 + Tailwind CSS v4 + Zustand 5. `electron-vite` v5 drives the build; `electron-builder` v26 creates installers. These versions reflect the dependency upgrade already present on `main`; keep the lockfile and package manifests aligned and verify packaged main/preload bundling when upgrading again.
- **Core engine**: `packages/core`, pure TS, no Electron imports, unit-testable with Vitest.
- **Host services**: `packages/host` (`@kotrain/host`), transport-agnostic Node services + a `createHost()` facade (Phase-2 extraction; see Architecture).
- **Shared types/IPC contracts**: `packages/shared` (`@kotrain/shared`).
- **Web server**: `apps/server`, Fastify v5 (+ @fastify/static, @fastify/websocket).
- **Cloud**: `apps/cloud`, Fastify, multi-account, file-backed store (Postgres-swappable).
- **CLI**: `apps/cli` (`kotrain`), ESM, Node 22 globals (`fetch`/`WebSocket`), no deps.
- **Relay**: `apps/relay`, Fastify WS dumb pipe.
- **Mobile**: `apps/mobile`, Capacitor wrapping the shared renderer (standalone, not a root workspace).
- **Website**: `apps/website`, static hand-crafted HTML/CSS/JS (no framework, GitHub Pages), download buttons → GitHub Releases.
- **Storage**: JSON files under the app data dir; usage analytics as JSONL. **No native modules**, spawn `ripgrep`/git via child_process when available, with JS fallbacks.

## Architecture Overview

```
apps/desktop/
  src/main/        Electron main: thin IPC wiring over createHost() (windows, IPC router,
                   workspace add dialog, window-state), service logic now lives in packages/host
  src/preload/     contextBridge API (typed, from packages/shared) → window.kotrain
  src/renderer/    React app (shared verbatim across all editions; see Design System & UI/UX)
packages/core/     providers/ (anthropic, openai-compat, ollama-native, lmstudio, vllm,
                   openrouter), agent/ (loop, tools, prompts), guardrails/ (risky-command
                   classifier + policy), memory/ (markdown memory store), indexer/ (file
                   tree + outline + search), context/ (context assembly with provenance -
                   powers the Context panel), connectors/ (linear, slack, discord, gmail, gdrive)
packages/host/     services/ (settings, sessions, chat, tools, workspace, memory, usage,
                   connectors, mcp, spec, relay-agent, terminal, files, changes); host.ts
                   createHost() → KotrainApi surface + EventEmitter; dispatch.ts createDispatcher(host);
                   paths.ts (data dir, ALS)
packages/shared/   types, IPC channel contracts, defaults, e2e.ts (WebCrypto seal/open)
apps/server/       Fastify: POST /api/:channel + /api/events WS over createHost(); serves renderer;
                   relay-agent mode; esbuild bundle → npx kotrain
apps/cloud/        multi-account hosted edition (accounts, entitlements, per-account Host)
apps/relay/        dumb E2E pipe + push sender (APNs/FCM)
apps/cli/          kotrain CLI + MCP server (local in-process or remote HTTP+WS)
apps/mobile/       Capacitor shell over the shared renderer
apps/website/      marketing site (index.html, styles, mascot sprite, downloads)
```

### One codebase, multiple runtimes (the host extraction)

The keystone Phase-2 decision: service logic was extracted out of `apps/desktop/src/main` into the transport-agnostic **`packages/host`**. `createHost(opts)` returns one object implementing every `KotrainApi` method (minus the `on*` subscriptions, which become an `EventEmitter` emitting `agentEvent` / `indexProgress` / `terminalEvent` / `changesUpdated`). A shared **`createDispatcher(host)`** provides one routing table reused by every transport. Then every edition wraps the same `Host`:

- **Desktop** (`apps/desktop/src/main`): thin IPC wiring, `ipcMain.handle(channel, …) → host[method](...)`; forwards host events to `webContents.send`. Overrides only `workspaceAdd` for the native folder dialog.
- **Web/Docker** (`apps/server`): `POST /api/:channel → host[method]` + a `/api/events` WebSocket, both routed by `createDispatcher`. Serves the same built renderer.
- **Cloud** (`apps/cloud`): per-account `Host` (own data dir + event emitter), every request run inside `withDataDir()`.
- **Renderer transport adapter**: `renderer/web-client.ts` provides `window.kotrain` over fetch/WS (auto-installed by `main.tsx` when no Electron preload exists), so the React UI is **shared byte-for-byte**. It can also route over the relay (`?relay=&room=&key=`).

> **The five-touch rule (recurring):** adding any renderer↔host capability means touching, in order: `shared/ipc.ts` (channel + `KotrainApi` type) → host impl + `host.ts` interface → `dispatch.ts` → `preload/index.ts` (Electron) → `web-client.ts` (web). Keep all five in sync. (Extending `setSessionOptions` is the same shape: update the `Pick` in **three** places, `sessions.ts`, the `host.ts` interface, and `shared/ipc.ts`.)

See [spec-web-and-hosted.md](spec-web-and-hosted.md) for the full web/Docker/Cloud design, edition matrix, relay protocol, and ZDR boundary.

### Workbench panes (chat / terminal / file / browser / diff / pr / hypergate)

The workbench is a Zustand pane model (`store.ts`: `groups: WbGroup[]`, each a column of tabbed `WbPane`s shown side by side; `MAX_GROUPS = 3`). `WbPane.kind` is `'chat' | 'terminal' | 'file' | 'browser' | 'diff' | 'pr' | 'hypergate'`; `WbPane.refId` holds the file path (file/diff), URL (browser), PR URL (pr), or the Hypergate manager's URL (hypergate). `WorkbenchView.tsx` routes each kind to a component. Store openers mirror `openChatPane` (locate-or-create, focus): `openFilePane(path)`, `openBrowserPane(url?)`, `openDiffPane(sessionId)`, `openPrPane(url)`, `openHypergatePane()`. Hypergate is the one singleton pane: there is a single manager, so an existing pane is re-pointed rather than duplicated when the port changes.

## Data Model

- **Storage**: JSON files under the app data dir (desktop: `%APPDATA%/Kotrain/kotrain`; server/CLI: `~/.kotrain`, `KOTRAIN_DATA_DIR` override). Usage analytics as JSONL.
- **Settings** (`AppSettings`): providers, defaults (provider/model), guardrails ruleset, prompts (`PromptTemplate[]`), MCP servers (`McpServerConfig[]`), favorites, language, default chat mode, auto-update prefs, `specMethodology`, `orchestration`.
- **Session**: messages, `workspaceId`, `attachedPaths`, `specLinked`, `mode` (ask/guardrails/yolo), `disabledTools`, `offline`, `incognito`, `pinned`, `tags`, `title`, `modelId`, `autoModel`, `specMethodology`, `parentSessionId`, `ContextPrefs` (toggle/pin state).
- **Memory**: markdown entries, global + per-workspace.
- **Workspace**: folder roots + index (file tree, outline, search).
- **Usage**: JSONL append; `UsageSummary` aggregates byModel / bySession (drives cost estimation).
- **Changes** (diff/approval): keyed by sessionId, `{ path, original }` recorded the first time `write_file`/`edit_file` touches a path in a session; `listChanges`/`revertChange`/`acceptChange` operate over it.
- **Cloud**: file-backed `CloudStore` (`cloud.json` + per-account dirs under `accounts/<id>`) behind an interface so Postgres can drop in; `setPlan` ready for Stripe. Each account gets its own data dir, scoped via `withDataDir` (`AsyncLocalStorage` in `paths.ts`); the settings cache is keyed by data dir (`Map<dataDir, AppSettings>`).

## Integrations & APIs

- **Model providers**: Anthropic native; OpenAI-compatible SSE (OpenAI/OpenRouter/LM Studio/vLLM); Ollama native (list/pull/ps/load/unload). Base-URL `/v1` normalization for bare `host:port`.
- **File IPC** (powers file panes / explorer / editor): `readFile(path) → {content, truncated, binary}` (1 MB cap, NUL-byte binary detect), `writeFile(path, content)`, `listDir(path) → DirEntry[]` (dirs-first), host `files.ts`, honoring sandbox/jail checks where applicable.
- **Connectors**: Linear/Slack/Discord via real REST (token-based); Gmail/Drive OAuth scaffold (full flow pending Google client creds).
- **MCP**: hand-rolled JSON-RPC-2.0 client, no SDK dep, two transports: **stdio** (`shell:true` on win32 so `npx`/`.cmd` resolve) and **streamable HTTP** (`McpServerConfig.url` + `token` → plain `fetch` POSTs with `Accept: application/json, text/event-stream`, parses JSON or SSE-framed replies, echoes `mcp-session-id` for stateful servers). `initialize` + `notifications/initialized`, `tools/list`, `tools/call`. Host-side `detectKotrainMcp()` probes `localhost:7777` (`KOTRAIN_MCP_URL` override) for a KotrainMCP daemon (`mcp:kotrain` channel). Kotrain is also an MCP *server* via `kotrain mcp`.
- **Vaizer skills hub**: host `vaizer.ts` reads the public catalog of [nekko-labs/vaizer](https://github.com/nekko-labs/vaizer) (`vaizer:catalog` / `vaizer:skillMd` channels). Offline-first: bundled snapshot / `vaizer.json` cache by default, live GitHub fetch only on explicit refresh; SKILL.md fetched verbatim at install time. Non-catalog installs ride an `installSkill` payload + record snapshot.
- **Push (relay)**: APNs (HTTP/2 + ES256 JWT) and FCM (HTTP v1, service-account RS256 JWT → OAuth → messages:send), both dependency-free in `apps/relay/src/push.ts`. Tokens registered per deviceId; dropped on device revocation (`push-remove`). Config via `APNS_*` / `FCM_SERVICE_ACCOUNT` env. Credential setup: [provisioning.md](../../obsurdian/projects/agent-nekko/provisioning.md).
- **Relay v2 protocol (phone remote control)**: relay tags each device connection with a `cid` (`{type:'c', cid, data}` toward the agent; agent unicasts `{type:'d', cid, data}` and can `kick`); device auth is END-TO-END, a sealed HELLO (`deviceId` + optional one-time `pair` code) validated against the host's registry (`remote.json` via `packages/host/src/remote.ts`; pure `verifyHello` in `shared/remote.ts`). Requests before a welcome are refused; events fan out only to welcomed connections (terminal/changes/tasks/training events now ride the relay too). Relay hardening: timing-safe key compare, 10 MB frame cap, token-bucket rate limit, client/room caps, optional `KOTRAIN_RELAY_AUTHZ_URL` agent-enrollment gate (cloud `/api/relay/authorize`). Managed instance: `wss://kotrain-relay.fly.dev` (Fly.io, `apps/relay/fly.toml`, ONE machine, rooms are in-memory); self-host image `ghcr.io/nekko-labs/kotrain-relay`. Guide: [docs/REMOTE.md](docs/REMOTE.md).
- **Cloud auth**: email+password (scrypt via `node:crypto`, `salt:hash`, `timingSafeEqual`), bearer session tokens.

## Infrastructure & Deployment

- **Desktop**: electron-builder targets win (MSI/NSIS/zip), mac (dmg/zip, arm64 ad-hoc signed), linux (AppImage/deb). Release CI on `v*` tags builds all 3 OSes and publishes a draft. Auto-updates via electron-updater (GitHub feed, NSIS). To cut a release: bump version (root/desktop/server) + `git tag vX.Y.Z && git push` → CI publishes a draft → publish it.
- **Web/npx**: `npm run web` (in-repo) or `npm run bundle:web` → esbuild-bundled self-contained `kotrain` package in `apps/server/cli-dist` (server+engine inlined, fastify external) → `npx kotrain`. Publish needs the user's npm login.
- **Docker**: multi-stage `Dockerfile` (`ELECTRON_SKIP_BINARY_DOWNLOAD=1`, dev deps pruned, non-root node:20-slim) + `docker-compose.yml` (volume workspace + data, `host.docker.internal:host-gateway`, publishes to 127.0.0.1:1440); GHCR publish workflow on `v*` tags.
- **Mobile**: `.github/workflows/mobile.yml` (manual) builds Android debug APK + iOS simulator. `cap add ios/android` + native toolchains run on cloud runners (not the Windows dev box). Requires a full root `npm run build` first so workspace dists resolve.
- **Cloud**: `npm run cloud` (:4318) locally; hosted deploy target TBD (open question).
- **Local test loop**: `npm run local` builds + launches the built desktop app (electron-vite preview, no installer/Defender); `npm run web` for the browser edition.
- **Ports**: web 1440, cloud 4318. Kill a stale dev server by port (`Get-NetTCPConnection -LocalPort … | Stop-Process`), npm-wrapped node survives `pkill`.

## Design System & UI/UX

**UI map (renderer).** Left rail (icon nav, expands over the content on hover to show labels; its right divider is visible only while expanded): **Command Center** (dashboard), **Chat** → the **Workbench**, **Skills**, **Training**, **Goals**, **Design** (the design studio + snapshot board), **Models** (model server UI), **Connectors**, **Memory**, **Settings**. (The dedicated **Projects** view was removed 2026-07-18; folders are added from a chat's + menu / Context Inspector, and the index still powers `@` mentions + context.) **Mascot**: Nekko, a right-facing 2D hand-drawn outline secret-agent cat. `AgentHead` is the canonical head geometry: slim ink-filled sunglasses, discreet earpiece/coiled wire, folded collar and small tie. Glasses perch above closed eyes in sleep/stretch; the mini simplifies the details for 16-24px legibility. Theme ink/paper and ginger ears/tail remain, as does the full mascot's line-boil wobble. Website hero/favicon/social artwork reuse the same geometry rather than drifting into another style. The renderer-local state machine and exact timings still map agent work, user activity, active pauses, and AFK/visibility to bug-watching, wake-and-spin, stretching, lying, and sleeping poses (T124/T135 foundations, AN10 visual refinement).

**Workbench (the Chat surface).** A Warp/Devin-style multi-pane shell driven by a Zustand pane model (`groups: WbGroup[]`, each a tab-stack of `WbPane`s shown side by side; `MAX_GROUPS = 3`). Left sidebar groups work by project (workspace bucket + a "No project" bucket), listing chats and terminals; sub-agent sessions (`parentSessionId`) nest recursively under their parent. Center is a tab bar + split columns. Components: `WorkbenchView` (orchestrator + sidebar + tab strips), `ChatPane` (self-contained conversation, per-pane provider/model, streaming, inline Context Inspector; refactored out of the old monolithic `ChatView`), `TerminalPane` (Warp block terminal), `FilePane` (markdown preview / mono editor), `BrowserPane` (`<webview>` + URL bar), `DiffPane` (session change review). Terminals are an in-memory host service (`packages/host/terminal.ts`): one persistent shell process per terminal, per-command marker echo delimits blocks + carries the exit code (`$?`-aware on PowerShell), output streamed over a `terminalEvent` bus channel. Sub-agents: `spawn_agent` builtin tool handled in `host/chat.ts` (creates a child session, runs a nested `sendChat`, returns its last assistant message; depth-capped).

**Chat surface.** Messages render as **speech bubbles** (user right, assistant left with a tail) flowing into the composer. Reasoning streams into a Claude-Code-style collapsible **Thinking** box (dimmed, auto-collapses when the answer starts). A **metrics bar** above the composer shows context used/total (hover → token breakdown by source), tokens/sec, thinking on/off, an **effort** cycle (low/normal/high → `EFFORT_TEMPERATURE` {0.2,0.7,1.0}), and per-chat estimated cost. The composer also hosts the **prompt analyzer** (see below).

**Instrument strip (above the composer).** Two rows, split by the question each answers: row one **how this agent runs** (Mode, Tools, then a hairline divider and the Offline / Incognito privacy switches pushed right), row two **which brain it uses** (model picker, Thinking, Effort, then Automate pushed right). Three control families keep them from reading as one run of pills, and they are the vocabulary for any new strip control: **`.ctl-menu`** for a value picker (transparent bordered field, `.ctl-menu-label` for the muted name, `.ctl-caret` for the ▾), **`.ctl-toggle`** for an on/off switch (no background until hover; the on-state is color plus a lit `.ctl-dot`, never a filled pill; `.ctl-toggle-neutral` for a switch that shouldn't claim the accent, `.ctl-toggle-on` for a read-only indicator that can't carry `aria-pressed`), and a plain icon button for an action. Everything is `shrink-0` except the model chip, which truncates first, so the strip never wraps.

**Markdown (`Markdown.tsx`).** A dependency-free line scanner (not a blank-line block splitter, which mis-parses the dashed lists people actually type) shared by both sides of the transcript and the file viewer: lists nested by indentation, headings, quotes, rules, pipe tables, fenced code, inline bold/italic/strike/code/links + bare-URL autolinking, single newlines kept as `<br>`. Emphasis is stars-only with hugging delimiters so `snake_case` and `2 * 3 * 4` survive. Covered by `Markdown.test.tsx`.

**File / editor / browser surfaces (IDE-like).** `FilePane` reads via the file IPC; `.md` → toggle between rendered (`Markdown.tsx`) and source; other text → editable mono `<textarea>` with Save (Ctrl/Cmd-S) + dirty dot; binary/oversized → notice. `BrowserPane` is an Electron `<webview>` (`webviewTag: true` in the main `webPreferences`) with go/back/forward/reload/open-external. `FileTree.tsx` (`ProjectFiles`) is a collapsible per-project tree with `fileIcons.tsx` color-tinted glyphs (Linguist/Material palette), lazy children via `listDir`. `DiffPane.tsx` computes a client-side LCS line diff and renders added/removed lines with per-line keep/revert checkboxes + per-file and all-files Keep/Revert; refreshes on the `changesUpdated` event. The `SpecPanel` ↗/row click calls `openFilePane(path)` (not `openPath`), keeping an explicit "reveal in OS" affordance as fallback.

**Prompt analyzer.** `promptAnalysis.ts` (pure, renderer-side, no LLM): given the draft, returns `{ parts, findings, score: 'A'..'F', model }`. Part detection (role/task/context/examples/output-format/constraints/reasoning/tone/variables) + lint rules (vague terms, weak/passive verbs, missing role/format, ambiguous pronouns, length, filler, conflicting instructions, secret/PII leak) + a model hint (multi-step + large context → frontier; short single-shot → fast/cheap). `PromptAnalyzer.tsx` in the composer shows the grade + part checklist, expandable to grouped findings, with inline wavy underlines over flagged spans.

**Context Inspector (right panel, toggleable).** Two parts, each section self-explaining on hover (`InfoHint` group-hover popover):
- **Sources**: folders wired into the chat (active highlighted, add/remove, multi-root), attached files (native multi-file picker; `prompt` fallback on web), the chat's `SPEC.md` (highlighted + openable) with Build-from-chat / Live controls, links to detected guideline files (AGENTS.md/CLAUDE.md), and a memory count.
- **Breakdown**: live view of exactly what's entering the prompt this turn (files, guidelines, memory snippets, connector data, index snippets), each item toggleable/pinnable with token counts, plus a context-window headroom bar.

**Design tokens.** Cleaner cooler ink/paper neutrals, one warm accent (salmon ~`#ff7a59`) + a friendly `--accent-2` paired into `--brand-grad`, 8px spacing grid, rounded-xl cards with soft elevation `--shadow-sm/md`, hairline borders, focus `--ring`, Inter/system font, JetBrains Mono for code. `--accent-soft`/`--ring`/gradient all derive from `--accent` (color-mix) so they track the user's chosen accent. Dark and light themes via CSS variables + `data-theme`, with `color-scheme: light/dark` set on `:root`/`[data-theme=dark]` (and styled `select option` bg/color) to avoid white-on-white.

**Responsive / PWA.** ChatView session list + context panel become slide-over drawers on phones; header selects shrink/hide via `md:`. Installable PWA (manifest + network-first `sw.js` that never caches `/api`, registered only on http origins).

### Inline editor comments (planned), design

- **Surface**: in `FilePane`'s mono editor, a gutter **+** appears on the hovered/active line. Clicking opens an inline comment box anchored to that line.
- **Capture**: a comment carries `{ path, line, lineText, comment }`. Comments persist per session (extend the host `changes`/a new `comments.ts` store, keyed by sessionId; surfaced via a new `comments:*` IPC channel set following the five-touch rule). A small marker stays on commented lines until resolved.
- **Actions**: **Add to prompt** appends a formatted block (`> file:line, comment`) into the active pane's composer draft so several annotations batch into one ask; **Run now** dispatches immediately via `sendChat` on the pane's session with the same block as the user turn. Both reuse the existing composer/send path, no new agent plumbing.
- **Constraint**: zero new deps; gutter affordance built with the existing editor textarea + an overlay (same technique as the analyzer underline overlay).

### Design board (planned), design

- **Surface**: a new left-rail **Design** view (and/or a `WbPane.kind = 'design'`). A zoomable/pannable board lays out captured **page snapshots** as cards (Figma-canvas feel) using CSS transform pan/zoom, no new canvas/graph dep for v1.
- **Snapshots**: v1 = read-only images. Capture per page/route. For web/desktop, snapshot the rendered app (reuse the existing screenshot path / `webview` capture for the running preview); store under the app data dir keyed by route. A capture refresh is triggered on demand and when the agent reports a UI change.
- **Notes & comments**: clicking a card opens a side sheet. **Notes** persist with the board (a `design.json` per workspace). **Comments** reuse the inline-comment actions, **Add to prompt** / **Run now**, carrying the page identity as context.
- **Live update + indicator**: when an agent edits files that map to a page, mark that card with an **"updating" badge**; on the agent's `done`/`changesUpdated`, re-capture so the user watches the snapshot change. Clicking the badge calls `openChatPane(sessionId)` to jump to the driving agent.
- **Scope (v1)**: snapshots only (not an editable vector canvas, see Non-goals in spec). Page→file mapping starts heuristic (route table / manual tag) and can tighten later.

## Coding Conventions

Extends `../../knowledgebase/principles/coding.md` (which these override).

- **TypeScript everywhere**; `packages/core` is pure (no Electron) and unit-tested with Vitest.
- **Keep the build green**: `npm run build` at repo root (order: shared → core → host → desktop) before each commit; typecheck across all workspaces; core tests passing.
- **Land code via auto-merged PRs** on `nekko-labs/agent-nekko`: branch → push → `gh pr create` → `gh pr merge --squash --admin --delete-branch`. PR descriptions stay **short and plain**, a few human-readable bullets.
- **Human-readable tasks first**: checklist items read like plain product statements; technical detail goes as indented sub-bullets, never the top line.
- **Cross-platform** code (Windows-first dev). MCP/stdio spawns use `shell:true` on win32.
- **No new deps when avoidable**: diff (LCS), prompt analysis, file icons, MCP, push JWTs are all in-repo.
- **MCP server stdout hygiene**: `runMcpServer` reassigns `console.log = console.error` so stray stdout never corrupts the protocol stream.
- Autonomous build: don't ask the user; use best judgement; record open questions in `memory.md`.

## Constraints

- **No native node modules** (avoids Electron rebuild pain); **no pnpm**; **Windows-first** dev but keep code cross-platform.
- **Not a full IDE**: lightweight textarea editor (not Monaco/CodeMirror), no LSP/debugger; `<webview>` browser is a preview, not a hardened browser; Design board shows snapshots, not an editable canvas.
- The web/Docker server grants file + shell access to whoever can reach it → **bind to `127.0.0.1` by default**; exposing beyond localhost requires `--host`/`KOTRAIN_HOST=0.0.0.0` **and** a token (`KOTRAIN_TOKEN`), with a prominent banner. Guardrails + sandbox modes live in the host (not the UI), so they apply identically across editions.
- **Local Windows `dist` is blocked** by Defender quarantining `app-builder.exe` (AV false-positive), release CI runners are unaffected; don't change Defender settings unprompted.
- **Privacy invariant**: the relay sees only ciphertext + the fact "a run finished" (for push); never message content. Never read/echo a secret value into the conversation (the rotate script is no-echo by design).
- **GUI can't be exercised headless here**: interactive surfaces (webview, drag, diff line-revert math, analyzer overlay, the planned inline comments + design board) are verified by typecheck + build in this environment and need a hands-on pass on a real desktop/web run.
- **Standing rule**: confirm before first-time public exposure / new permissions/credentials unless explicitly authorized this session ([[confirm-before-public-or-permissions]]). Building tooling is fine; only *executing* the public/permission action needs the prompt.

## Key Technical Decisions

- **Host extraction (`packages/host`)**: one transport-agnostic `createHost()` + `createDispatcher()` so desktop/web/Docker/Cloud/CLI all reuse the same engine and the renderer is shared verbatim. The single biggest architectural move; everything else hangs off it.
- **Per-account isolation via `AsyncLocalStorage`**: `withDataDir(dir, fn)` scopes the data dir per request; the settings cache became `Map<dataDir, AppSettings>` (also a general correctness fix). Editions that never call `withDataDir` are byte-for-byte unaffected.
- **E2E-encrypted, outbound-only relay**: the local agent dials out (no inbound ports); keys derived from the pairing secret (PBKDF2 → AES-GCM); the relay forwards opaque ciphertext. This is what makes relayed local-model use inherently ZDR.
- **Open files in-app, not the OS**: matches the "stay in the app" goal and fixes the dead click (`openPath` silently fails with no registered `.md` handler); OS hand-off kept only as a "reveal" fallback.
- **Editor is a lightweight textarea**, not CodeMirror/Monaco, honors "simple, not a full IDE" + the small-dependency footprint; markdown gets the existing zero-dep `Markdown` renderer.
- **Browser pane uses Electron `<webview>`** for v1 (DOM-flow, simplest inside splittable panes); `WebContentsView` is more robust but needs main-process bounds syncing across split groups, deferred.
- **Diff/approve snapshots originals** on first agent write, then diffs current-vs-original; writes still happen immediately (tool loop never gated), "reject" reverts, full review/revert UX without blocking the agent.
- **Prompt analyzer is fully client-side** (regex/structural heuristics), instant, offline, free; the marketable always-on feel. LLM rewrite is a later opt-in.
- **Capacitor over React Native for mobile**: wraps the existing shared renderer (the web edition already runs over the relay in a phone browser), matching "same UI everywhere" and maximizing reuse.
- **Tags over hierarchical folders** for conversation organization (lighter, less sidebar restructuring).
- **Hand-rolled MCP & push** (JSON-RPC over stdio; APNs/FCM JWTs via `node:crypto`), dependency-free, in keeping with "no native modules" and a lean tree.

---

# Part 2, Tasks (what's built and what's next)

> The spec + plan broken into **small, reviewable, independently testable work items**, the project-level build checklist + shipped history. Capability-level descriptions live in [SPEC.md](SPEC.md); the full web/Docker/Cloud design lives in [spec-web-and-hosted.md](spec-web-and-hosted.md).

> **Status values**: `[ ]` not-started · `[~]` in-progress · `[x]` done · `[!]` blocked
>
> IDs are the original build-checklist numbers (kept stable; do not renumber). Phase groupings are preserved as sub-headings. Current narrative status lives in [memory.md](../../obsurdian/projects/agent-nekko/memory.md).

## Road to v1.0

> **What v1.0 means**: the polished, installable **OSS product** (Desktop + self-hosted web + Docker), every differentiator and IDE surface working and verified, the two new visual/editing features landed, and clean signed installs + npm publish. **Kotrain Cloud (paid)** runs as a *parallel track* (T31/T32/T34) and does **not** gate the OSS v1.0 launch.

**Done toward v1.0**: core engine + agent loop, all providers + local auto-discovery, the differentiators (Context Inspector, guardrails, memory), workbench + terminals + sub-agents + Command Center, spec-driven dev + orchestration + optimize + auto-mode + refined design system, the **IDE-surfaces wave** (file viewer/editor, browser pane, file explorer, diff/approval, hoverable inspector, prompt analyzer, T66), **inline editor comments (T72)** and the **Design board (T73)**, and all distribution editions (desktop release through **v0.6.0**, web/npx, Docker, mobile shells, relay-mediated push).

**Remaining for v1.0:**
- [ ] **GUI verification pass** of the IDE-surfaces wave (T66) with a model connected, webview browsing, the diff line-revert math, the analyzer overlay. (The file editor + tree + the new comment/design surfaces are now hands-on verified over the web edition; the rest is best exercised with a live model.)
- [ ] Pending user-side credentials/infra (below): remaining Windows signing, CLI npm publishing, full Gmail/Drive OAuth, real APNs/FCM delivery, and wiki push. macOS signing/notarization shipped in v0.6.0 (T125). The older T116 domain/project rename is superseded by the Agent Nekko cutover AN3-AN5 below. Keep `wss://kotrain-relay.fly.dev` and old update artifacts stable until installed-client migration is verified; do not destroy relay apps as part of a branding sweep.
- [ ] Cut the **v1.0 release** once the above land (bump versions + tag).

## Now / In Progress

### Subscription sign-in, first-run onboarding, and workflow automations (added 2026-09-08)

Philip's three initiatives: (1) support Claude and ChatGPT **subscriptions** via one-click OAuth sign-in, with API keys as the secondary path; (2) a skippable **first-run onboarding wizard** (theme presets → providers → integrations incl. installing Nekko as a subagent inside Claude Code / Codex / Cursor); (3) expand **workflows into real automations**: integration-backed triggers, action steps that act through connectors, and a local CI-runner mode for git-host events. Full implementation plans: [subscription sign-in](docs/superpowers/plans/2026-09-08-subscription-signin.md), [onboarding](docs/superpowers/plans/2026-09-08-onboarding.md), [workflow automations](docs/superpowers/plans/2026-09-08-workflow-automations.md). Workspace feature folders: `obsurdian/projects/agent-nekko/features/{subscription-signin,onboarding,workflow-automations}`. Ships as a stacked PR series (order below). **Boundary carried into all of it**: subscription OAuth uses the vendors' first-party CLI clients (off-label, plainly labelled, API keys unaffected if cut off), and git-triggered runs never execute untrusted PR code unattended (see SPEC Scope Boundaries).

- [x] **T137**, Host OAuth core: PKCE service + loopback callback + paste-code fallback + 0600 token store (`tokens.json`, excluded from settings export), `oauth:*` channels, token refresh + injection at chat time. PR `feat/oauth-core`. · merged #167 · Done: 2026-09-08
- [x] **T138**, Claude subscription sign-in: `AnthropicProvider` OAuth mode (Bearer + `oauth-2025-04-20` beta header + required system prefix), `SubscriptionSignIn` component, Models page subscription-first CTA, `claude setup-token` paste + Claude Code CLI import. PR `feat/claude-signin`. · merged #168 · Done: 2026-09-08
- [x] **T139**, ChatGPT subscription sign-in: new `ChatGptProvider` speaking the Responses API (`chatgpt.com/backend-api/codex/responses`), localhost:1455 callback, Codex CLI `auth.json` import for headless. PR `feat/chatgpt-signin`. · merged #169 · Done: 2026-09-08
- [x] **T140**, Subscription UX polish: Subscription/API-key badges, sign-out/re-auth, $0 subscription labelling in cost surfaces, `docs/SUBSCRIPTIONS.md`. PR `feat/subscription-ux`. · merged #170 · Done: 2026-09-08
- [x] **T141**, Theme presets: `ThemePreset` model + `accent2`/`themePreset` settings, `[data-preset]` token blocks, gradient-swatch picker shared by onboarding + Settings → Appearance. PR `feat/theme-presets`. · merged #171 · Done: 2026-09-08
- [x] **T142**, Onboarding wizard shell + theme step: `settings.onboarding` flag (versioned), full-screen every-step-skippable wizard, Replay setup in Settings. PR `feat/onboarding-shell`. · merged #172 · Done: 2026-09-08
- [x] **T143**, Onboarding providers step: subscription-first online group + local auto-discovery group + Agent Nekko managed-model card (deep-links until AN7/AN9 land managed inference). PR `feat/onboarding-providers`. · merged #173 · Done: 2026-09-08
- [x] **T144**, Onboarding integrations step + new connectors: one-click MCP-subagent install into Claude Code / Codex / Cursor configs (backup first, manual snippet fallback), new `jira` + `teams` connector kinds. PR `feat/onboarding-integrations`. · merged #174 · Done: 2026-09-08
- [x] **T145**, Workflow action steps + integration triggers: `action` step kind over a `WORKFLOW_ACTIONS` registry (slack/teams post, linear/jira create+comment, github/gitlab comment+commit-status, generic webhook post) with `{{...}}` run-context templating; `connector` + `webhook` trigger kinds, host pollers with per-trigger cursors, `POST /api/hooks/:slug` on the server edition + opt-in desktop loopback. PRs `feat/workflow-actions` + `feat/workflow-triggers`. · merged #175 + #176 · Done: 2026-09-08
- [x] **T146**, Local CI runner: git-event-triggered workflow template that checks out the ref, runs build/test locally, and reports back as a commit status + failure comment; `docs/git-runner.md`. Restricted to trusted refs by default (SPEC scope boundary). PR `feat/git-runner`. Aligns with AN8 (workflow/runner expansion). · merged #177 · Done: 2026-09-08
### Subscription limits + pricing visibility (added 2026-09-08)

Follow-on to the subscription stack: surface real plan state (5-hour + weekly reset windows, per-model scoped limits) and cost (per-model price estimates, running session total). Plan: [usage-visibility](docs/superpowers/plans/2026-09-08-usage-visibility.md).

- [x] **T147**, Usage-limits data path: normalized `SubscriptionLimits` (Claude `anthropic-ratelimit-unified-*` response headers + `/api/oauth/usage`; ChatGPT `GET /wham/usage`), host `LimitsService` (header capture + throttled poll), `limits:get` + `limitsUpdated` IPC. PR `feat/usage-limits`. · merged #179 · Done: 2026-09-08
- [x] **T148**, Limits + pricing UI: chat-header limits chip + popover (all windows + reset times + plan/credits), provider-card breakdown, per-model price estimate in model pickers ("Included in plan" + muted equiv. for subs, `$` for metered), running session-cost total. `docs/USAGE-LIMITS.md`. PR `feat/usage-limits-ui`. · merged #180 · Done: 2026-09-08

### Agent Nekko rebrand and next product wave (added 2026-09-07)

Product name: **Agent Nekko**. Mascot: **Nekko**. Primary: **agentnekko.com**; backup: **nekkoagent.com**. Repo/folder target: **agent-nekko**; CLI: **agent-nekko**. See [positioning](SPEC.md#positioning-carry-into-website--readme), [pillars](SPEC.md#messaging-pillars), and [roadmap](SPEC.md#roadmap-beyond-one-machine). Do not sweep historical Shipped entries or past release assets.

- [x] **AN1: Initial marketing and product identity implementation.** Website, social image, outline favicon, metadata, README, visible desktop/mobile branding, installer artwork, and mascot names updated. Four pillars are distinct; runtime installation and automatic routing are not overstated; team/distributed/cloud runners are roadmap. T124/T135 activity/outline design preserved. Matching desktop/mobile website and light/dark Settings captures plus motion recordings saved in temporary storage. Website checked at six widths with ten download scenarios. Native mobile and packaged-upgrade verification remain AN5 gates, not claimed done. Implemented locally: 2026-09-07; not deployed.
- [x] **AN2: Compatible CLI/MCP entry points.** Added `agent-nekko` beside legacy `kotrain` and `nekkos`; MCP discovery lists 23 `agent-nekko_*` tools while legacy `kotrain_*` calls still dispatch. Preserved published package name and `kotrain/run` import. Packed-tarball smoke installs and executes all three real binaries and compares both MCP prefixes. CLI tests/typecheck and package smoke pass. Implemented locally: 2026-09-07; npm publication remains AN5.
- [x] **AN3: GitHub and local folder cutover.** Done 2026-09-07. `nekko-labs/kotrain` renamed to `nekko-labs/agent-nekko` via `gh api`; GitHub auto-redirects old web/git/API URLs, so installed clients on the old update feed keep working while the old name stays unclaimed (do not create a new `kotrain` repo). Branch protection (required `build` check), all workflows, releases, and the Vercel Git integration survived the rename (deployments now show `nekkolabs/agent-nekko`). Checkout remote repointed; repository/feed/package metadata and doc/site links updated in PR #162. Local folders renamed: `code/kotrain` to `code/agent-nekko` and `obsurdian/projects/kotrain` to `obsurdian/projects/agent-nekko`, with workspace pointers updated. Old release filenames and update manifests remain immutable; no old packages, data, or relay apps deleted. **Remaining user-side item:** the npm Trusted Publisher on npmjs.com is bound to `nekko-labs/kotrain` + `release.yml` and must be re-pointed to `nekko-labs/agent-nekko` (or `NPM_TOKEN` set) before the next CLI publish.
- [~] **AN4: Vercel and domains.** DNS/domain pointing done by Philip 2026-09-07: `agentnekko.com` serves 200 and `nekkoagent.com` 308-redirects to the primary; `kotrain.com` still serves. Remaining: the Vercel project is still named `nekkos`; inspect its ID, team, Git linkage (repo was renamed to `nekko-labs/agent-nekko`, verify the integration followed), settings, and domain assignments before renaming it to `agent-nekko`. Confirm `www.agentnekko.com` and `www.nekkoagent.com` are also attached and TLS-valid. Keep old domains/relay available through migration.
- [ ] **AN5: Package, installer, and persistent-identity migration.** `npm view kotrain version` still returns 0.6.0 and `agent-nekko` returns 404 as of 2026-09-07. Establish npm ownership/trusted publishing before changing the package name or advertising `npx agent-nekko`. Stage internal workspace/env/storage identifiers separately with new-first, legacy-fallback migration tests. Preserve the Electron app ID and existing userData during the display-name change. Verify signed installers, upgrades, protocol handlers, icon/installer artwork, GHCR image aliases, and mobile bundle/store IDs before changing persistent identity. Never weaken signing or CI controls to unblock release.
- [x] **AN6a: Explicit frontier-to-local delegation.** `spawn_agent` now accepts optional `provider_id`/`model_id`; default inheritance remains unchanged. Explicit targets require enabled configured providers and verified exact chat-model IDs. Invalid/unavailable targets fail before creating a child and never silently fall back. Children inherit effective execution mode and disabled tools; Ask/ask-everything now approve both sub-agent starts and MCP calls before indirect execution. Incognito delegation is blocked rather than persisted. Host-side offline checks require a local-compatible loopback endpoint; unoffered tools cannot run, offline context previews skip connectors, and hidden spec refreshes are suppressed. Added 50 focused mocked-provider tests, including the context-preview failure reproduced before its fix. Implemented locally: 2026-09-07. See [experience and limits](SPEC.md#delegating-frontier-planning-to-a-local-worker).
- [ ] **AN6b: Explainable routing policy and wider privacy audit.** Add opt-in local-worker defaults, automatic task/capability/resource-aware selection, and visible routing reasons. Validate against live frontier/local providers and real CLI/MCP clients. Audit explicit spec generation and other independent provider actions for the same offline policy; configured loopback validation is not a network sandbox. Add cancellation propagation and a real concurrency/resource scheduler before promising parallel fleet execution.
- [~] **AN7: Managed inference runtime.** Decomposed into four phases after the 2026-09-09 design session: **A** control plane, **B** fit planner, **C** model catalog + downloads, **D** bundled engine. A and B shipped as T149/T150 below; C and D get their own specs. Runtime downloads and license/telemetry implications still require explicit consent, and the separate-inference-server requirement is **not** removed: phase D is what would remove it. Design: [managed inference runtime](docs/superpowers/specs/2026-09-09-managed-inference-runtime-design.md), plan: [managed runtimes](docs/superpowers/plans/2026-09-09-managed-runtimes.md).
- [x] **T149**, **Runtime control plane (AN7 phase A).** Local model servers are now started, stopped, and driven rather than merely observed. New `packages/host/src/runtimes/` puts all three behind one `RuntimeAdapter` whose **`RuntimeCapabilities`** record (not its kind) tells the UI what to offer, which is why there is no runtime-kind branching anywhere in the renderer and why phase D's bundled engine will need no UI change. **Ollama** is supervised as a child process we own (`supervisor.ts`: PID ownership, 60s health-poll budget, a 200-line log ring buffer, SIGTERM then SIGKILL, and a `process.on('exit')` sweep so quitting never leaves an orphaned server holding VRAM); it loads via `/api/generate` with `num_ctx`/`num_gpu`/`keep_alive` and reads geometry from `/api/show`'s architecture-prefixed `model_info` map (matched on the key suffix, so a new architecture needs no code change). **LM Studio** drives its real `lms server start|stop` lifecycle plus `lms load --context-length --gpu --ttl`, keeping T99's lesson that `lms` exits 0 on "Model Not Found"; its weights come from `lms ls --json`, the only place LM Studio publishes a size. **vLLM is connect-existing only**: its adapter has *no* `start` method at all, so it cannot be started even by mistake, and its `/metrics` Prometheus text is parsed for KV-cache pressure and queue depth. **Ownership is the behavioural change**: stopping a process Agent Nekko did not start now asks first and names it, where T98 killed whatever held the port silently; the old `servers.ts` port-killer remains only as the confirmed-force path. Six new IPC channels through the full five-file chain. 30 host tests. · [spec](SPEC.md#models--providers) · Done: 2026-09-09
- [x] **T150**, **VRAM fit planner (AN7 phase B).** New pure `packages/core/src/capacity/` answers "will this fit, where will it run, and why" before the load: weights + KV cache (`2 x layers x kvHeads x headDim x context x bytesPerElement x parallelSlots`) + overhead against one pool, yielding a verdict, estimated CPU-resident layers, structured reasons carrying their own numbers, and suggestions that are **re-planned before being offered** so a chip never proposes something that would not help. Three honesty rules are enforced by tests, not convention: **`unknown` is a first-class verdict** (never infer capacity from a model name), **unified memory is never summed with system RAM** (a 64 GB Mac is not 128 GB), and **discrete GPUs are not pooled**. Where a server reports less than the planner needs, the answer is **partial rather than absent**: LM Studio's real weights are still shown and a will-not-load verdict is still certain when the weights alone overflow. Reconciliation closes the loop, measured residency after a load feeds a per-runtime EMA calibration record (`runtime-calibration.json`, keyed by runtime version, absurd residuals discarded), so the overhead constant improves instead of staying an unaudited guess. UI: runtime cards replace the standalone Stop-server button, with a power toggle, copy/edit address, resident models showing their real GPU/CPU split, and a fit drawer whose context slider drives a live segmented bar with a marker at the device's edge. 38 planner tests + 23 verdict-copy tests. **Verified live** against the real LM Studio on this Mac (10 models, 219 GB): full stop/start cycle through the API, real weights (37.0 GB for hermes-4-70b) projected against the M1 Max's 49.7 GB free with the unified pool counted once, vLLM correctly refusing to start, and Ollama correctly reporting not-installed rather than offering a dead button. · [spec](SPEC.md#models--providers) · [docs](docs/local-models.md) · Done: 2026-09-09
- [ ] **AN8: Workflow and runner expansion.** Continue from the existing local workflow engine. Separate single-machine task-runner polish from later authenticated team workers, resource-aware distributed scheduling, shared permissions/audit, and possible optional cloud runners. Untrusted repository events must not gain unattended host execution. Define verification for each slice before implementation.

### AN10: Secret-agent mascot and plain-language marketing (added 2026-09-07)

- [x] **AN10a: Refine Nekko's character without changing activity behavior.** Shared `AgentHead` adds slim sunglasses, a folded collar/tie, and discreet earpiece/wire; sleeping/stretching glasses lift to preserve visible closed eyes. Removed the helmet reflection. The full mascot is now a consistently slender, line-only stick cat: default leans against a short local edge with crossed ankles and relaxed paw; wake pushes off, stretch extends horizontally, working peeks/braces, and sleep slumps against the edge. The permanent rail divider remains hidden. Retained ginger accents, exports, five-state machine timing, line boil, reduced-motion and hidden-page behavior. Portrait bounds expand for accessories; mini artwork simplifies them at production sizes. Sixty-nine desktop tests include body geometry, all poses, six portrait widths, three mini sizes, exact timing boundaries, themes, and bounds. Website hero uses the exact app default-pose geometry and social artwork the same leaning body. Website favicon plus packaged desktop/PWA/installer icons use the canonical mini head on the new green palette, replacing the orbit mark. Done locally: 2026-09-07; not released.
- [x] **AN10b: Explain the product before the technology.** Hero now reads "AI help on your computer. For coding and everyday work." Replaced abstract section/card/nav headings with direct actions, explained model/local/CLI/MCP where needed, and simplified supporting copy. Updated website metadata, JSON-LD, llms.txt, and README. Preserved all five pillars, the user-added Planned PC-control card, hardware/setup/safety/privacy caveats, and current release links/commands. Before/after desktop/mobile screenshots and six-second GIFs are in temporary evidence storage. Six-width layout, ten-download-scenario regression, anchor/asset/metadata checks, and exact SVG parity pass. Done locally: 2026-09-07; not deployed.

### AN9: Hardware-aware one-click offline PC control (added 2026-09-07)

Fifth major product pillar, not a variation of chat model routing. See the [planned experience and safety contract](SPEC.md#one-click-setup-for-safe-offline-pc-control-planned). Reuse `host/system.ts`, `host/gpu.ts`, provider discovery, and existing approvals where appropriate. Current telemetry and shell/file tools do not constitute an offline voice/desktop-control stack. AN7 supplies the runtime lifecycle foundation; specific models and runtime dependencies are not selected until their requirements, licenses, and supported platforms are verified.

- [x] **AN9a: Product definition and honest marketing.** Added the fifth pillar, one-click journey, intent/TTS/optional-STT stack, hardware-fit requirements, scoped actions, permission gates, stop controls, and offline acceptance criteria to SPEC/README. Marketing gives this major capability its own visibly Planned card rather than claiming it already ships. Done locally: 2026-09-07.
- [ ] **AN9b: Requirements catalog and machine readiness advisor.** First implementation slice: a pure, testable evaluator over hardware facts and a curated versioned catalog. Gather OS/version, architecture/CPU features, backend/driver support, system RAM, per-device VRAM or unified memory, and free disk. Define intent/STT/TTS role capabilities, language support, quantization, artifact identity/hash/license, installation footprint, and measured runtime/KV/overhead budgets. Assess the complete concurrent stack with OS/app headroom. Output compatible/recommended/insufficient/unverified plus specific reasons; never infer a guarantee from a model name or missing probe. Tests cover CPU-only, unsupported backend/architecture, low memory/disk, unified memory without double-counting, non-poolable multiple GPUs, unknown facts, and combined models that individually fit but together do not.
- [ ] **AN9c: Resumable one-click provisioning.** Build an inspect → propose → consent → acquire → verify → install/configure → health-check → benchmark → ready state machine on AN7. Show proposed components, download/storage budget, existing-runtime reuse, and permissions before setup. Use pinned trusted artifacts/checksums, resumable downloads and verified offline import, process ownership, clear cancellation/partial-failure handling, and restart-safe progress. Never delete user-managed runtimes, bypass OS permission prompts, or fall back to cloud. Prove idempotent repeated setup, corrupt/interrupted downloads, exhausted disk, unavailable backends, failed health checks, and recovery without losing existing configuration.
- [ ] **AN9d: Local intent and speech pipeline.** Structured intent schemas validated independently of the LLM, local TTS, optional push-to-talk STT, text/caption fallback, explicit mic state, and interruption/stop priority. Keep audio ephemeral by default, with no always-on capture or cloud speech fallback. Select tested model/runtime/language pairs through AN9b; measure combined latency/resource use on the target machine. Test ambiguous/noisy speech, unsupported language, denied microphone, missing models, low resources, feedback from the app's own TTS, and cancellation mid-transcription/synthesis.
- [ ] **AN9e: Permission-scoped computer-control broker.** Define the platform/action matrix before native implementation. Use typed allowlisted actions, approved apps/windows, semantic OS/accessibility adapters, focused-target revalidation, and visible previews. Keep this separate from unrestricted shell execution and the current no-tools Offline chat toggle. Enforce independent action-specific confirmations for sensitive operations, bounded steps, local redacted audit, revocation, always-visible stop plus shortcut, and verified undo only where supported. Test screen/document prompt injection, wrong/stale targets, uncertain intent, permission revocation, repeated instructions, denied confirmation, partial completion, and stop before the next action. OS privileges are always granted explicitly by the user.
- [ ] **AN9f: Setup/control UI and offline release gates.** Integrate machine assessment, recommended stack/reasons, component progress, permission setup, readiness, and the control/stop/activity surface. Test the supported macOS/Windows/Linux combinations in a fresh environment and on an existing installation. Block outbound network access after setup and verify intent, STT, TTS, and approved local actions still work with no attempted cloud fallback or required telemetry; do not treat a proxy-capable localhost URL alone as evidence. Cover keyboard-only/text input, accessibility, captions, denied permissions, restart, low-resource alternatives, and representative user tasks. Publish measured minimum versus recommended requirements only after these tests.

**Verification checkpoint, 2026-09-07:** combined-tree `npm run typecheck`, `npm run build:web`, `npm test` (including `test:brand` redirect/identity regressions), `npm run test:cli-package`, and `git diff --check` passed. Browser verification covers six widths and ten release/download scenarios. Desktop tests include fresh-install profile creation, existing data preservation, 25 mascot geometry/behavior cases (69 desktop tests total), and seven locale labels. Native installer upgrade tests and native mobile validation are still outstanding. These changes are local on `feat/agent-nekko-rebrand`, not a published release. Vercel login, DNS/TLS, package publishing, and coordinated remote/folder cutovers remain blocked or pending above.

- [x] **T125**, **macOS Developer ID signing + notarization, live as of v0.6.0.** The release workflow now signs, hardens, notarizes, staples, and verifies the macOS build, so a release `.dmg` now installs with no `xattr` workaround. Kotrain is packaged by **electron-builder**, which does all of this internally, so this deliberately does *not* copy Hypergate's hand-rolled `codesign`/`productbuild`/`productsign`/`notarytool` pipeline (that exists only because Hypergate ships a Rust + Node-SEA `.pkg`), and no `Developer ID Installer` certificate is needed: `.dmg`/`.zip` are signed with the Application cert. `hardenedRuntime` went `false` → `true` with new `build/entitlements.mac.plist` (`allow-jit` + `allow-unsigned-executable-memory` for V8, `disable-library-validation` because `@lydell/node-pty`'s prebuilt `.node` is dlopen'd from outside the asar, `allow-dyld-environment-variables` for spawning shells/agents/MCP servers); `notarize` stays `false` in the committed config and the workflow passes `-c.mac.notarize=true`, so a local `npm run dist` never demands an Apple API key. `scripts/after-pack.cjs` keeps ad-hoc signing as the unsigned-build fallback but early-returns when `CSC_LINK`/`CSC_NAME` is set so it can't fight the real signature. Enabling the hardened runtime surfaced a **latent crash**: `QrScanner.tsx` calls `getUserMedia` with no `NSCameraUsageDescription`, and a missing usage string kills the process rather than denying a prompt, so that plus `device.camera` were added. CI fails closed on a partially configured secret set (a signed-but-unnotarized app is still Gatekeeper-blocked while looking fine) and verifies every signed build with `codesign --verify --deep --strict`, `stapler validate`, and `spctl --assess` (note the ticket staples to the **`.app`, not the `.dmg`**). Verified locally against the real `Developer ID Application: Nekko Labs LLC (3HM5598S99)` identity. New `docs/signing.md`; README's "Kotrain is damaged" section rewritten and kept only as a pre-v0.6.0 note. **Unblocked 2026-08-14**: Philip set the five `nekko-labs` org secrets by hand (they are write-only, so Hypergate's could not be copied, and the `.p12` export needed his keychain password + org-admin). Two things bit on the way in, both worth knowing before the certificate rolls. **(1)** A first attempt had `MACOS_SIGNING_CERTS_P12` set and the other four missing, and the fail-closed validation caught it exactly as designed. **(2)** The second attempt failed at `security import` with `MAC verification failed during PKCS12 import`, because `gh secret set` without `--body` reads its value from **stdin**, so pasting the five commands as a block made `MACOS_CERT_PASSWORD` swallow the *next command's text* as its value. electron-builder echoes its `-P` argument on failure, which is how the command text was visible in the log. Set interactive secrets one command at a time, or with `read -rs "P?prompt: "` piped to `--body` (note `read -p` is bash-only; Philip's shell is zsh). A local `openssl pkcs12 -info` check is misleading here: OpenSSL 3 rejects Keychain's `RC2-40-CBC` payload with an `unsupported` algorithm error unless `-legacy` is passed, which looks like a bad password but is not, and the MAC verifying is the part that actually proves the password. First notarized build verified in CI: `valid on disk`, satisfies its Designated Requirement, `flags=0x10000(runtime)`, `Authority=Developer ID Application: Nekko Labs LLC (3HM5598S99)`, `stapler` validated, and `spctl` **accepted / source=Notarized Developer ID**. · [spec](SPEC.md#distribution--platforms) · Added: 2026-08-07 · Done: 2026-08-14

## Backlog / Planned

### Workbench resizable splits (86)
- [ ] **T86**, Resizable workbench columns. The workbench already splits into up to 3 columns (`MAX_GROUPS = 3`), but each column gets an equal `flex: 1` share. Add a drag handle between adjacent groups so the user can resize column widths freely (like VS Code / Warp / Cursor). The handle lives between rendered `<PaneGroupView>`s, uses pointer events for the drag gesture, clamps each column to a min/max width, and persists the per-group `flexGrow` or pixel widths so splits survive a reload. No new deps; reuse the same pointer-event resize pattern already used by the context panel's Folders/Context divider (`startResize` in `ContextInspector`). *Planned: 2026-07-14.*

### Chat surface follow-ups
- [ ] **T104**, Re-extract the chat console components. PR #98 had extracted ChatPane's transcript pieces into `components/agent-console/` (ActivityGroup, ApprovalBar, MessageBubble, TurnFooter, transcript.ts), but PR #99's fluidity rework of the same components lived inline and superseded that snapshot, so the merge of main into #99 removed the module (dead code; the extraction is preserved in git history at a5bf815). Redo the extraction over the reworked inline versions in `ChatPane.tsx` (ModelPicker, toStreamBlocks, ActivityGroup, TurnStatus, ReasoningBlock, MessageBubble, ToolCard, ApprovalBar). *Planned: 2026-07-26.*
- [ ] **T105**, Model-generated prompt-part fills. The analyzer's click-to-fill chips (T103) use deterministic text matching; when a provider is reachable, optionally ask a small/fast model to draft the missing part from the prompt's actual content instead. Needs a lightweight one-shot completion IPC (none exists; `sendChat` always runs the full agent loop). Keep the text-match path as the offline fallback. *Planned: 2026-07-26.*

### Phase 3, Kotrain Cloud (paid, hosted), parallel track, does not gate OSS v1.0
- [ ] **T32**, ZDR mode (always available) + cloud chat-history + cloud file management; encrypted-at-rest sync engine; ZDR badges + audit log.
- [ ] **T34**, Managed connectors: pre-registered OAuth apps for Gmail/Drive/Slack/Discord.

### Pending user-side credentials / infra
- [ ] Full Gmail/Drive OAuth flow (needs Google client creds).
- [ ] Complete Windows signing provisioning and native installer verification. macOS Developer ID signing and notarization are already live (T125); keep their validation gates intact.
- [ ] Publish `kotrain` / `kotrain` to npm so `npx kotrain …` works (needs the user's npm login; bundle built + verified locally).
- [ ] Push the GitHub wiki pages after a manual first-page creation (staged in `docs/wiki/`).
- [ ] Real APNs/FCM delivery (needs creds + a device); Cloud hosted deploy target. See [provisioning.md](../../obsurdian/projects/agent-nekko/provisioning.md).
- [ ] Stripe billing keys to enable live charges (`STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_PRO`, `STRIPE_PRICE_TEAM`, `CLOUD_PUBLIC_URL`); integration is built + tested, just unconfigured.

## Shipped

> Append as work lands; bug fixes folded into the feature they harden. Verified throughout against a live LM Studio gemma reasoning model (streaming reasoning, single + multi-step tool loops, index-grounded context).

### Latest, Experimental feature toggles (2026-09-08)
- [x] **T136**, **A Settings → Experimental section gates in-progress surfaces behind per-feature switches.** New optional `AppSettings.experimental` (`ExperimentalFlags` in `packages/shared`) carries a flag per gated destination; flags default off and absent means hidden, so a new flag never surprises an existing install. The gate lives in the renderer store as `viewEnabled(view, settings)` and is applied at every nav surface: the desktop rail and the phone bottom bar filter their lists, `setView` refuses a hidden destination (lands on Command Center instead), a `useEffect` in `App` redirects if the open surface's flag is switched off, and the command palette only lists "Go to Memory" while the flag is on. Three toggles ship: **Model training**, **Design board**, and **Memory**. The web edition gets it free since it shares the renderer; verified over `npm run web` by flipping flags through the real HTTP API and by CDP-clicking the actual toggle in headless Edge (nav went 10 → 9 items live, flag persisted to `settings.json`). `npm run build:web` + `npm run typecheck` green across all workspaces. · [spec](SPEC.md#chat--cowork-unified-surface) · Done: 2026-09-08

### Latest, the mascot goes hand-drawn outline (2026-08-28)
- [x] **T135**, **Aphelion redrawn as a 2D hand-drawn outline cat with a line boil.** Philip: *"Instead of a realistic cat mascot, I'm thinking an animation 2d handdrawn, outline cat would be awesome."* `Mascot.tsx` redrawn from the filled astronaut illustration (cream suit, translucent glass, six hand-tuned colors that only worked on dark chrome) to pure ink outline: every stroke is `var(--ink)` on `var(--paper)`, so both themes come free; the ginger tail and inner ears stay as the one spot color and the sparkles moved to ginger + `var(--accent)`. The five activity poses and their state machine are untouched (same silhouettes, ported path-for-path), and the resting loaf gained **tucked front paws**, two little paper-filled rolls peeking under the chest, plus a soft haunch line. The signature move is the **line boil**: an SVG `feTurbulence`/`feDisplacementMap` filter whose seed re-seeds discretely at ~9fps, so the ink wobbles like traditional frame-by-frame animation even while a pose holds still. The filter is applied via a CSS class (`.aphelion-boil`), not a filter attribute, so reduced motion and a hidden document switch it off entirely; the body and helmet carry a paper fill purely so nearer shapes occlude the lines behind them (the inked-animation overlap trick). `AphelionAvatar` and `MiniAphelion` share the outline dome (the mini at a heavier stroke for 18px legibility); the boil stays exclusive to the rail mascot so the composer's send control stays calm. Prototyped and pose-verified in a standalone harness (light + dark screenshots, a gif cycling all five poses); captures come from the harness rather than the running app because another session's dev server owned the checkout. Typecheck and 40 desktop tests green. · [spec](SPEC.md#vision) · Done: 2026-08-28

### Latest, Workflows + runaway-output backstops (2026-08-13)
- [x] **T127**, **A looping model can no longer hang the app.** (Numbered T126 while in flight; renumbered at merge because the Hypergate task below landed on that number first, per the shared-checkout note.) Philip: *"for some reason it crashes and loops."* A qwen3.6-27b reply in LM Studio streamed **108,790 characters of reasoning, the same sentence 2,534 times, over nine minutes** (session `s_msqwff3v`, message 114), and the window locked up. Nothing in the stack was stopping it, in three separate places:
  - **No output cap was ever sent.** `openai-compat` and Ollama requests carried no `max_tokens` / `num_predict` at all, so a degenerate stream ran until it filled its own context window; Anthropic's was hardcoded at 4096. `ChatRequest.maxOutputTokens` now reaches every provider as its native knob, from a new `maxOutputTokens` setting (default `MAX_OUTPUT_TOKENS_DEFAULT` 8,192, range 256–200,000, in Settings → Agent loop beside the step budget).
  - **No repetition guard.** New `core/src/agent/runaway.ts` watches a stream's trailing window with two amortized tests (line collapse: many non-blank lines, ≤3 distinct; probe recurrence: the last 160 chars appearing 6+ times, which also catches cycles with no line breaks). It trips within ~2.5k characters on the observed case. The loop wires one guard per channel (text and reasoning are independent), and on a trip **cancels the request** through its own `AbortController` chained to the caller's signal, rather than abandoning the iterator and leaving the server streaming. The reply keeps its useful head (4k chars), drops the cycle, discards the half-emitted tool call, and **ends the turn** instead of spending the rest of the step budget looping; the message says what happened and that it's usually the model rather than the prompt.
  - **A React state commit per token.** `setLiveText`/`setLiveReasoning` ran once per streamed delta, re-rendering the whole transcript per token. Deltas now batch on a 50ms timer (a timer, not `requestAnimationFrame`, so a backgrounded window still flushes) with the live buffers capped at 40k chars, and the pending buffer is cleared on turn start, turn end, and unmount so a late flush can't duplicate a reply.
  - **Verified**: 7 new guard tests (including the exact 2,534-line failure, and negative cases for long varied prose, generated code with repeated shapes, and short repetition), 3 new loop tests (a never-ending generator ends promptly, cancels the stream, doesn't run the next iteration's tool call; the output cap reaches the provider), 185 core + 29 host + 36 desktop tests green, typecheck across the workspaces. · [spec](SPEC.md#context-guardrails--memory) · Done: 2026-08-13
- [x] **T128**, **The Goals tab is now Workflows: GitHub-Actions-style automation with routed steps.** A step is an agent **prompt**, an installed **skill**, another **workflow**, or a **shell** command.
  - **Routing, not just sequence.** `packages/shared/src/workflows.ts` models a step's `onSuccess`/`onFailure` as a transition (`next` / `end` / `fail` / `goto step`), so build → verify → *back to build* is expressible; `MAX_STEP_LOOPS` (10) bounds re-entry so a workflow that never converges still terminates with a message naming the step. Steps carry retries, a timeout, and continue-on-error (GitHub's semantics: the failure is still recorded, it just doesn't stop the run).
  - **Agent steps report their own outcome.** `stepOutcomeInstruction` appends a PASS/FAIL token request and `readStepOutcome` reads the last one, so an agent step routes the way a shell exit code does. All of a run's agent steps share **one session**, so verify sees what build did.
  - **Engine** (`packages/host/src/workflows.ts`): CRUD over `workflows.json`, runs in `workflow-runs.json` (capped at 20 per workflow), one run per workflow at a time, cancel that aborts the in-flight step, nested workflows bounded by `MAX_WORKFLOW_DEPTH`, a 15s scheduler for cron/interval triggers, and `reconcileWorkflowRuns` on boot so a run interrupted by a quit is written off rather than looking stuck. A shell step's command comes from the workflow the user wrote (never from the triggering event), so it runs unattended like a CI step instead of raising a guardrail prompt nobody is there to answer.
  - **Triggers**: manual, schedule (a 5-field cron parser in shared, so the editor validates and previews the next fire, plus a plain interval), CLI, Slack, and git-provider events **normalized across GitHub/GitLab/Bitbucket/other**. `dispatchWorkflowEvent` is the single door, reachable from the CLI in-process and, in the web edition, over the existing `POST /api/:channel` route, which is what a Slack app or a webhook posts to. New `kotrain workflow list|run <name>|trigger <command>|runs`.
  - **UI**: a filter rail (search that reaches into step bodies, state, trigger kind, category) and a list grouped **by category** or **by event listener** (a workflow appears under every event it listens for), groups collapsing and capping rows at 12 with a "show more" so hundreds stay navigable. A row expands into the **step canvas** and run history; `layoutWorkflow` in shared was made generic over node/edge shapes so the Skills tab's layered node graph now draws both. Failure edges are drawn in the danger color, loop-backs dip under the row dashed and labelled, terminal Done/Failed nodes close the graph, and with a run selected each node wears that run's outcome and attempt count. The editor redraws the canvas as you type, flags steps nothing routes to, and re-points routes left dangling by a deleted step. Templates: build/verify loop, PR reviewer, nightly sweep, empty.
  - **Goal runs kept**: the tab is gone, but goal runs now list in the **Training** tab so existing ones stay reachable, and `/goal` still works. Also fixed in passing: the desktop transport never forwarded `tasksUpdated`/`trainingUpdated`, which the host has always emitted, so those surfaces only refreshed when remounted.
  - **Two CodeQL findings on the PR, both fixed rather than dismissed.** `slugify` trimmed separators with `/^-+|-+$/`, which backtracks quadratically on a name that is mostly punctuation (a workflow name is user input); it now splits, filters, and joins, with a test that slugs 50k dashes in well under a second. And the flagged "insecure randomness reaching a security context" traced back through the new engine to `createSession`, whose ids were `Math.random()`-derived; a session id names a file and the remote transport lets one reach the network, so it now takes a `randomBytes` suffix in the same readable `s_<time>_<rand>` shape (ids are never parsed, so old sessions are unaffected).
  - **Verified**: 31 domain tests (routing edges, loop-back detection, unreachable steps, grouping, filtering, event matching per provider/repo/branch, cron incl. the Vixie day-of-month/day-of-week OR, templates) and 25 engine tests (ordering, retries, continue-on-error, the loop cap, a build/verify loop that *converges*, dangling transitions, timeouts, nesting depth, history trimming). Hands-on over the web edition against an **isolated data dir**: created the loop-back template from the UI and confirmed the canvas draws the dashed danger-colored "on failure, retry" edge; ran a 3-step shell workflow and got `s1 success, s2 attempt1+2 failure (continue-on-error), s3 success` with the row updating live over the push event; confirmed listener grouping lists a two-trigger workflow under both CLI and Schedule, that search matches a step body ("typecheck" → 1 of 2), dark mode, and no horizontal overflow at 375px. · [spec](SPEC.md#workflows-procedural-automation) · Done: 2026-08-13

### Latest, macOS metrics that are true, and runs that survive being cut off (2026-08-28)
- [x] **T134**, **CI had been red since T-the-bin-fix, because the smoke test asserted the spelling instead of the target.** #151 changed `apps/cli/package.json`'s bin values from `./dist/index.js` to `dist/index.js`, but `scripts/smoke-cli-pack.mjs` still compared them against the `./`-prefixed string, so `npm run test:cli-package` threw "Packed package is missing the kotrain and nekkos bin entries" on a package that was correct. It went unnoticed because that step runs after `npm test` and `build` was already the last check anyone looked at; it failed identically on CI (Node 22) and locally (npm 11), so it was never a version quirk. The check now strips a leading `./` before comparing, which is the thing #151 existed to be robust against in the first place, and reports what it actually found when it fails. Verified by running the full smoke test through to `CLI package smoke test passed: kotrain@0.6.0` (pack, dist manifest, `kotrain/run` import, native `node-pty` resolve, `status`/`workspace` JSON commands, and the MCP initialize/tools-list/tools-call round trip). · Done: 2026-08-28

- [x] **T133**, **Three pieces of feedback from running Kotrain on a Mac: tok/s was wrong, the GPU monitor said there wasn't one, and a timed-out run threw all its work away.** Philip: *"the tokens per second are not accurate (I'm running it on macos so not sure if that changes anything) / it says no nvidia gpu detected. could we show the macOS gpu usage instead when on a mac? / I ran a long running chat on kotrain and it timed out, but my issue is that it lost all of work progress and just said failed and I would have to start all over. Can we persist the progress so retries or pausing don't start all over?"*
  - **(1) tok/s was tokens over the whole wait, not over generation.** `ChatPane` divided the turn's output tokens by `Date.now() - turnStart`, and `turnStart` is set when the *turn* begins. That denominator includes prompt processing, every tool the agent ran, and any approval the user sat on, so a reply that generated for 10s and then ran a 40s test suite reported 10 tok/s instead of 50. The wrongness is not macOS-specific, it just shows up there because a local model's real rate is a number you can check against LM Studio. Providers now report **`outputMs`**, the decode phase only, on the `usage` chunk and the `usage` agent event: Ollama's server-side `eval_duration` where it's offered (the figure `ollama run --verbose` prints, and it excludes transport too), otherwise a `DecodeClock` timing first generated chunk → usage report, which drops time-to-first-token by construction. Tool-argument deltas start the clock as well, so a step that only calls a tool still has a rate. The renderer sums `outputMs` across the reply's steps and divides by that; `decodeRate`/`formatRate` moved into shared so the rule is one tested function, and single-digit rates keep a decimal (8.6 and 9.4 tok/s are different answers). `elapsed` deliberately stays wall clock: the two measure different things and are no longer expected to divide into each other.
  - **(2) macOS has a GPU, we just never asked it.** `gpu.ts` only ever shelled `nvidia-smi`, so every Mac fell through to "No NVIDIA GPU detected." The tool that reports real GPU residency there, `powermetrics`, needs root, which no monitoring chip should ask for. New `gpu-macos.ts` parses `ioreg -r -d 1 -w 0 -c IOAccelerator` instead, which any user can read: each accelerator entry's `PerformanceStatistics` carries `Device Utilization %` (the same 0-100 figure `nvidia-smi` reports) and the driver's current allocation. `gpu.ts` now dispatches on platform (`darwin` → ioreg, else nvidia-smi) rather than paying for a spawn that always fails. Apple Silicon has **no discrete VRAM**, so the reading is flagged `unified`, measured against `os.totalmem()`, and aggregated without summing one pool per GPU; the UI renames the meter **GPU memory** via shared `gpuMemoryLabel`, sub-labels it "unified with system RAM", and the dock names its probes (`os + ioreg`). The dead-end message became "No GPU stats available on this machine", which is true everywhere instead of naming a vendor we never checked for. Parsing is split from the spawn and tested against a real captured dump, including the `"In use system memory (driver)"` key that sits directly before the one we want and would read as zero under a loose match.
  - **(3) A cut-off run lost everything, and Retry finished the job.** Three separate defects compounded. `sendChat` persisted only on `done`/`error`, so a run killed mid-flight (timeout, crash, quit) left every completed step in memory only; it now **checkpoints on each `tool_result`**, written *before* the event is sent so anything re-reading the session finds the step it was just told about. `runAgent`'s stream `catch` yielded an error and returned **without pushing the partial turn**, so the reply's text died with the connection; `endInterrupted` now keeps it, marked `interrupted`, dropping only a tool call the model was mid-way through emitting (it never ran, and truncated arguments aren't trustworthy). And `retryLast` called `truncateSession` back to the last user message, which is what actually threw the work away, so the one visible recovery action was the destructive one. New `resume.ts`: `repairInterruptedHistory` answers any tool call that never ran with a stand-in result, which is what makes an interrupted transcript valid to send again (Anthropic and OpenAI both reject an unanswered tool request), and `RESUME_PROMPT` is injected as one **transient** turn, never persisted, only when the transcript ends on the assistant's own cut-off reply. `SendOptions.resume` skips both the append and the truncate. The notice now leads with **Resume** (shown when `hasResumableProgress`) and keeps the destructive path under its honest name, **Start over**. A user-initiated stop reports as `Stopped` and wears the warning colour, since it isn't a failure, and it keeps its partial answer too.
  - **Also**: `packages/shared/src` had 30 stale build artifacts (`chat.js`, `chat.d.ts`, …) committed into it back in June, which shadow the real modules for anything importing `./chat.js`, so the package could not be unit-tested at all; removed, and `@kotrain/shared` gained a `test` script and a place in the root `test` chain, which was missing.
  - **Verified** on the reporting machine (M1 Max, 64 GB): against the real LM Studio serving `qwen3.8-27b-uncensored-mlx`, one generation reported **19 tok/s** on the new decode-time denominator versus **9 tok/s** on the old wall-clock one, with 1.80s of time-to-first-token correctly excluded. The GPU probe returns `{source: 'ioreg', name: 'Apple M1 Max (32-core GPU)', utilizationPct: 29, 22486/65536 MB, unified: true}` and the numbers move between samples. Over an isolated web edition (own port + throwaway data dir) the chip reads `CPU 33% RAM 91% GPU 29% 22/64 GB` and its panel lists `GPU memory` + `Apple M1 Max (32-core GPU) · 29% util`, while the chat's Resources dock renders `GPU 31% utilization` / `GPU memory 35% · 22 / 64 GB used · unified with system RAM` / `os + ioreg`. 34 new tests (parser against the real dump, decode-rate arithmetic and formatting, provider decode timing incl. the excluded prefill, history repair, and host-level checkpointing that asserts the tool result is on disk at the moment the event fires and that resuming does not re-run it), 427 green across eight workspaces, typecheck and `build:web` clean. **Not fixed, and worth a follow-up:** the likely trigger of the original timeout is undici's 300s `headersTimeout`/`bodyTimeout` defaults on Node's `fetch`, which a long prefill on a large local context can exceed. Raising them needs `undici` as a direct runtime dependency of the packaged app, so it was left alone rather than bundled in with this; the run is now resumable either way. · [spec](SPEC.md#chat--cowork-unified-surface) · Done: 2026-08-28

### Latest, releases cut themselves on a version bump (2026-08-13)
- [x] **T129**, **A version bump on main now ships the release.** Philip: *"let's also update that every PR for kotrain or hypergate cuts a new github release and installers when the version number is updated."* Releases were tag-only: `release.yml` and `docker.yml` trigger on `v*`, so someone had to remember to tag after merging. New `version-release.yml` watches pushes to main that touch `package.json`, and when the version there has no matching tag yet it tags it and ships.
  - **Why it dispatches instead of only tagging.** A tag pushed with `GITHUB_TOKEN` does **not** trigger other workflows (GitHub blocks that to stop recursion), so tagging alone would leave `release.yml` unfired and look like a silent failure. It therefore tags, then `gh workflow run release.yml --ref <tag>` and the same for `docker.yml`; dispatching at the tag makes `github.ref_name` the tag, which is what every step in those workflows already reads. `release.yml` gained a `workflow_dispatch:` trigger for this (also useful for re-running a release by hand). No personal access token needed.
  - **Guards.** A push that touches `package.json` without changing the version (a dependency bump, a script edit) finds the tag already present and does nothing, so it's a no-op rather than a duplicate release. The version must look like a release version, and **apps/desktop and apps/server must agree with the root**, checked *before* tagging, because the installers carry the desktop version and the web edition the server one, so a partial bump would otherwise publish assets labelled with the old number.
  - Same workflow added to **hypergate**. · Done: 2026-08-13
- [x] **T131**, **The version-bump trigger needed a schedule, because our own merges can't fire it.** T129 shipped with `push: branches: [main]` alone, and v0.6.0's release did not happen: `auto-merge.yml` enables auto-merge with `GITHUB_TOKEN`, so the merge is attributed to `github-actions[bot]`, and **a push from that token never starts a workflow run**. That is the same recursion guard T129 already worked around for the bot-pushed *tag*, one step earlier in the chain than expected, and the failure mode is silence: the merge looked clean, CodeQL ran (GitHub's managed workflows are exempt), and nothing said the release had been skipped. v0.6.0 was cut by dispatching the workflow by hand. The fix is a `schedule: '*/15 * * * *'` tick that notices an untagged version bump whoever merged it, with the `push` trigger kept for a human merge (instant) and `workflow_dispatch` to cut one now. Every path is idempotent, it stops as soon as the tag exists, and a `concurrency` group stops a tick and a push racing to create the same tag. **A PAT in `auto-merge.yml` would make bot merges fire it directly and let the schedule go**, which is a call for Philip since it means provisioning a secret. · [spec](SPEC.md#distribution--platforms) · Done: 2026-08-13
- [x] **T132**, **`publish-release` could never publish, because a draft has no tag lookup.** T129's publish job resolved its release with `GET /repos/{owner}/{repo}/releases/tags/{tag}`, and that endpoint **only resolves published releases**: it 404s on a draft, which is exactly what `electron-builder --publish always` had just created one job earlier. So the step failed on its first API call every time, `set -e` aborted, and the release it existed to publish stayed a draft. It went unnoticed because the macOS job had been failing first, and `needs: release` meant the publish job was skipped rather than run and failed, so v0.6.0 was the first run where all three builds passed and the bug could surface at all. Fixed by listing `releases?per_page=100` (the list *does* include drafts for a token with push access, newest first) and selecting on `tag_name`, plus an explicit not-found guard so a genuine miss says so instead of failing on a `null` id. Verified the new lookup against the real v0.5.1 draft (resolves, `draft=true`) and a nonexistent tag (empty, caught by the guard). v0.6.0 itself was published by hand with the same body the job would have written. · [spec](SPEC.md#distribution--platforms) · Done: 2026-08-14
- [x] **T130**, **Released v0.6.0.** Root, desktop, server, and CLI bumped 0.5.1 → 0.6.0 (a minor bump: Workflows is a new feature and it replaced a nav destination), lockfile versions updated to match. Carries T126 (Hypergate one-click), T127 (runaway-output backstops), T128 (the Workflows tab), and T129. Cut by T129's automation on merge rather than by a hand-pushed tag, which is also its first real test. **The tag was cut but the release did not ship on 2026-08-13, and this entry claimed otherwise for a day**: the macOS job failed on T125's partial-secret check, so the draft carried Windows and Linux only, and `needs: release` correctly skipped the publish job. Re-run on 2026-08-14 once the signing secrets were complete, which produced all 11 assets including the first notarized `.dmg`. Publishing still had to be done by hand because of the `publish-release` bug in T132. · Done: 2026-08-13, actually shipped: 2026-08-14

### Latest, Hypergate in one click, and as a tab (2026-08-13)
- [x] **T126**, **Kotrain and Hypergate now connect in one click from either side, and Hypergate runs as a tab in the Kotrain window.** (T-number to re-check at merge per the shared-checkout note; T125 was taken by the macOS signing task.) Philip: *"kotrain and hypergate should be a 1 click easy connection in either direction. Neither of them are connecting easily. Also, let's add the ability once kotrain is connected to hypergate, hypergate runs as a tab in the same kotrain window."*
  - **The bug.** `detectKotrainMcp` required `/health` to answer `service: "kotrain-mcpd"`. The daemon was renamed with the product and answers `hypergated`, so detection never matched, the one-click card the Settings copy promised could not appear, and what showed instead was a stale **Get KotrainMCP** nudge pointing at a repo that no longer exists. Fixed and rebuilt as `detectHypergate` (accepting both service names), running at app start rather than only when Settings mounts.
  - **A scoped token, not the master one.** `connectHypergate` claims this install's own agent from `POST /api/clients/resolve {key:'kotrain', create:true}`, falling back to the gateway token on daemons that predate that endpoint. This is what makes the pairing visible from Hypergate's side too, where Kotrain had been sitting at "Offline, never used": it now shows Active, with its own per-server permissions and usage history.
  - **One entry, written in one place.** The host owns the settings write (`withHypergate`), so a deep link can connect with no view mounted. Re-connecting replaces the `hypergate` row instead of adding a second, keeps a name the user chose, and drops the pre-rename `kotrain-mcp` row that would otherwise offer every tool twice.
  - **Hypergate as a tab.** New `hypergate` pane kind and `HypergatePane`: the manager's own UI over loopback, with the live version, server count and connected agent in its header, no address bar, and an offline state that offers to reconnect when the daemon restarts. Opened by connecting, by **Open tab**, or from the command palette.
  - **`kotrain://hypergate/connect?port=N`.** Registered in main, with the single-instance lock that makes it safe: a second launch now hands its argument to the running window instead of starting a rival host on the same data directory, and a cold launch replays the link once the page can hear it. The link carries a port and nothing else. `deepLinks.ts` checks it, the daemon is asked what it is, and a dialog naming the version, port and server count waits for a click, because any local program can open a URL and a gateway's tools reach every chat.
  - **Verified** against an isolated daemon on port 7801 (never Philip's real one on 7777) and the packaged app (`electron-builder --dir`, isolated `--user-data-dir`, driven over CDP): detection and the token exchange; a gateway that answers the scoped token with 9 tools and a bogus one with 401; the Settings card; one click producing a connected entry, 9 tools in the agent loop, and the Hypergate tab reading "connected as Kotrain"; the deep link both handed to a running instance (which stayed one process) and cold-launching the app. A wrapped, card-overflowing button and a singular/plural slip were caught in the packaged build and fixed. 16 host + 40 desktop tests green, typecheck across the workspaces. · [spec](SPEC.md#tooling--integrations) · Done: 2026-08-13

### Latest, Aphelion activity animations (2026-08-06)
- [x] **T124**, **Aphelion now behaves like one right-facing cat instead of a standing body under a separate head.** The helmet's glass, cream fill, ginger accents, irregular ink, and facial detail now carry through filled, outlined bodies, tails, legs, and paws across five activity-linked poses. Launch and return from AFK wake Aphelion into a stand-and-spin; ordinary idle lies down with quiet breathing; active pauses stretch; agent work watches a tiny bug at the workbench edge with both front paws braced against it; 60 seconds without keyboard, pointer, or touch activity falls asleep, as does a hidden document. Visibility and reduced-motion preferences pause or collapse nonessential loops, while dynamic labels and Enter/Space keep the mascot operable. The collapsed nav rail has no right divider; its 1px edge appears only with the expanded hover or keyboard-focus state. Verified with 213 tests (144 core, 36 desktop, 33 cloud), typecheck across eight workspaces, production build, deterministic pose checks at 4/13/61 seconds, measured rail rest/hover/focus states (`64px/0`, `200px/1`, `200px/1`), dark and light before/after snapshots, and an 8-second motion capture. The Impeccable detector reported only five pre-existing warnings outside this change. · [spec](SPEC.md#vision) · Done: 2026-08-06

### Latest, inline app updates (2026-08-06)
- [x] **T123**, **The title-bar version pill now owns the complete desktop update flow, matching Hypergate.** At rest it shows `vX.Y.Z` and swaps to **Check for updates** on hover or keyboard focus without changing width. Checks hold their spinner for at least 500 ms, then confirm `latest`, offer **Download & install**, **Download only**, and a version-specific **Skip**, show real Electron download percentages, stage **Install & restart**, show an indeterminate restarting state, report **Updated to vX.Y.Z** once after relaunch, and expose retry plus error details. One `UpdateProvider` feeds both the title bar and Settings, so two surfaces cannot disagree. The first-run network consent and web refresh fallback remain intact. `autoInstallOnAppQuit` is off, making Download only and Skip honest; only an explicit install action applies the staged package. Published GitHub releases remain the feed boundary, so drafts are never offered. Verified with full typecheck across eight workspaces, 144 core + 36 desktop + 33 cloud tests, production build, unpacked Windows packaging, packaged-DOM checks (`38px` draggable title bar, `28px` no-drag control), a real check, and dark/light captures including the completed-update pill. Shipped in [v0.5.0](https://github.com/nekko-labs/kotrain/releases/tag/v0.5.0) through PR [#138](https://github.com/nekko-labs/kotrain/pull/138); all Windows, macOS, and Linux release jobs passed and published 13 installers plus update manifests. · [spec](SPEC.md#distribution--platforms) · Done: 2026-08-06

### Latest, one title bar instead of three (2026-08-06)
- [x] **T122**, **The window's chrome is the app's, and the menu bar is gone.** (T-number to re-check at merge per the shared-checkout note.) Philip: *"let's remove the file/edit/etc toolbar. Let's also make the minimize/close/maximize buttons feel integrated on the same line and color as the background."* Three strips stacked above the UI — an OS title bar, a File/Edit/View/Window menu, then the app — and the window buttons sat in a grey frame that belonged to no theme the app has.
  - **No menu bar.** `Menu.setApplicationMenu(null)` on Windows and Linux; macOS keeps its menu, which lives in the system bar rather than in the window and is where ⌘Q/⌘H/⌘W come from. Reload (Ctrl/⌘+R) and devtools (F12 / Ctrl+Shift+I) were the only items worth keeping and are bound with `before-input-event`; everything else duplicated a shortcut `shortcuts.ts` already owns or a gesture Chromium handles itself. Cut/copy/paste keep working without menu roles on Windows and Linux, which is why macOS is the exception.
  - **The app draws the title bar.** New `components/TitleBar.tsx`: a 38px strip in `--paper` carrying the Kotrain wordmark, `-webkit-app-region: drag` so it moves the window and double-click maximises. `titleBarStyle: 'hidden'` everywhere, plus `titleBarOverlay` on Windows/Linux and `trafficLightPosition` on macOS — both fed `TITLEBAR_HEIGHT` from the new `src/windowChrome.ts`, shared by main and preload, so the buttons land on the wordmark's line rather than above it.
  - **The buttons follow the theme.** The overlay is a native surface, so its colour has to be pushed: `applyTheme` now calls `syncTitleBarOverlay` (`renderer/chrome.ts`), which reads the resolved `--paper` / `--ink-soft` and sends them over a dedicated `chrome:titlebar-overlay` channel. Deliberately **not** in `IpcChannels`, every entry of which is routed through the shared Host dispatcher and reimplemented by the web transport — and a browser tab has no window chrome. The bridge is exposed as `window.kotrainChrome`, separate from `window.kotrain`, and its absence is how the renderer knows it isn't in the shell.
  - **The rail lost the wordmark it only showed on hover.** `.rail-label` is `opacity: 0` at rest, so the 48px header row above the nav was blank in the collapsed rail; the wordmark moved to the strip and the rail now starts on its first destination. Net, the app is a row shorter than before rather than a row taller.
  - **Verified** against the packaged build (`electron-builder --dir`, isolated `--user-data-dir`, screenshots off the real window because CDP only captures the page): no menu bar, one strip, wordmark top-left, native buttons top-right in `#0c0c11`; switching to light repainted them to `#fbfbfd` with `#52525e` glyphs. Structural check over CDP confirmed `platform: 'win32'`, exactly one `.titlebar` at 38px with `webkitAppRegion: 'drag'`. Full typecheck ×8 green. · [spec](SPEC.md#mascot-polish--onboarding) · Done: 2026-08-06

### Latest, chat + PR + nav + MCP polish (2026-08-05)
- [x] **T121**, **Nine pieces of chat-surface feedback from a PR-conflict run: honest agent wrap-ups, a non-vanishing final answer, a calmer working animation, a slimmer PR card, an Aphelion send button, project auto-filing, hover-reveal skills, the Agent tab, and an MCP menu.** (T-number to re-check at merge per the shared-checkout note.) **(1) Agent diagnoses access failures + honest wrap-up (prompt):** two new operating principles in `core/agent/prompt.ts`, an empty/unauthorized/not-found result (empty `gh`/API output, 401/403/404, "permission denied", an auth prompt) now makes the agent stop after a try or two and name the likely cause (a private or inaccessible repo, missing/expired credentials or `gh` auth, a wrong remote, a rate limit) with how to fix it, and it must end every turn with an honest wrap-up (what it did, what actually happened including anything blocked/unverified, the concrete next step) rather than claiming a false "done". Prompted by a run that flailed on a private repo and then reported success. **(2) Final answer never vanishes:** `toStreamBlocks` in `ChatPane.tsx` treated any assistant message carrying tool calls as a working step, so when a run ended on a tool-calling message (step budget spent, abort, or a model concluding in the same message as its last tool call) its wrap-up text folded into the collapsed "Worked on N steps" group and disappeared. It now surfaces the **last** assistant message's own text as the answer bubble even when that message also made tool calls (mid-run narration still folds in). **(3) Border beam, border only:** dropped the `.composer-beam::after` interior color wash (and the `beam-wash` keyframe) in `styles.css`, keeping just the rotating border ring, so the composer surface stays still as you type. **(4) PR card modernized + slimmed:** `PrCard.tsx` open-state relaid out as info-left / actions-right, a solid green gradient **Approve** primary, quiet ghost **Decline**, bordered **Review**, subtle violet **Merge**, and a compact ↗ open-on-GitHub icon, all right-aligned so the card is one slim row. **(5) Send is Aphelion:** the gradient send button is now the `AphelionAvatar` cat with no fill (new `.send-avatar` utility, transparent bg, lift + drop-shadow on hover); `SendIcon` import dropped. **(6) Auto-file chats under projects:** new pure `shared/project-detect.ts` (`detectSessionWorkspace`, 8 tests in `core/project-detect.test.ts`), signals = an attached path inside a workspace (decisive) or the prompt naming the workspace by display name / folder basename, with stopword-filtered generic names, word-boundary matches, and a null return on a two-project tie. `ChatPane.send()` calls it for a project-less chat on first run and `setSessionWorkspace`s the match; the sidebar's no-project bucket was renamed **General** and moved to the top (`WorkbenchView.tsx`). **(7) Skills on hover:** the + menu's Skill row is now a hover-reveal side flyout (`skillsHover` state + a 140ms close-delay so the pointer can cross the seam) instead of a click-to-open second page; the `attachMenuPage` state and back-arrow are gone. **(8) Agent tab:** the "Chat" nav destination is **Agent** (i18n `nav.chat` retranslated in all 7 locales) wearing the `AphelionAvatar` cat instead of the speech-bubble icon; the rail header lost its gradient logo mark and is now just the "Kotrain" wordmark (`App.tsx`, `navIcons` `ChatColorIcon` no longer imported). **(9) MCP menu in the strip:** new `McpMenu` in `ChatControls.tsx`, right of Tools, lists every configured server with a live status dot (connected/offline/checking via `getMcpStatus` probed on open), name + transport, and an enable/disable switch (global `settings.mcpServers[].enabled` via `updateSettings` + `refreshSettings`), plus **Add MCP server** (jumps to Settings) and a **Hypergate** recommendation (hypergate.app) for secure server management; SPEC "Instrument strip" + "MCP client" updated. Verified over an isolated seeded web edition: nav reads **Agent** with the cat avatar and a wordmark-only rail; a new chat shows the **General** group at the top; the strip carries **MCP** right after Tools, whose menu shows the empty state + Add MCP server + Hypergate; the **+ → Skill** flyout opened on hover listing `/goal`, `/research`, `/plan`, …; the send control is the Aphelion SVG (transparent bg confirmed via computed style); and `composer-beam::before` remains while `::after` / `beam-wash` are gone. Typecheck ×8 clean, 144 core tests + 36 desktop tests green. · [spec](SPEC.md#chat--cowork-unified-surface) · Done: 2026-08-05

### Core build (1–25)
- [x] **T1**, Monorepo scaffold, tsconfig, lint-light, README, MIT license.
- [x] **T2**, `packages/shared`: types + IPC contracts.
- [x] **T3**, `packages/core` providers: OpenAI-compatible (streaming SSE), Anthropic, Ollama native (list/pull/ps/load), LM Studio, vLLM, OpenRouter presets; provider auto-discovery (scan localhost:11434/1234/8000).
- [x] **T4**, `packages/core` agent loop: system prompt, tool-use loop (read/write/edit/glob/grep/bash), streaming events.
- [x] **T5**, Guardrails: risky-command classifier (destructive fs, force push, curl|sh, registry, privilege esc), policy engine (allow/ask/deny), default ruleset.
- [x] **T6**, Context engine: context assembly with provenance records (file/guideline/memory added, token estimates) → drives Context Inspector.
- [x] **T7**, Memory: markdown memory store (global + per-workspace), CRUD.
- [x] **T8**, Indexer: workspace file tree, code outline (regex symbols), search.
- [x] **T9**, Connector framework + Linear/Slack/Discord (real REST), Gmail/Drive (OAuth scaffold).
- [x] **T10**, Electron main: IPC router, settings store, multi-window, multi-root workspaces, tool execution with sandbox modes (workspace-jail / ask / docker-if-present).
- [x] **T11**, Renderer shell: theming, left rail, layout, command palette.
- [x] **T12**, Chat surface: streaming messages, markdown+code rendering, tool-call cards, approval prompts, model picker.
- [x] **T13**, Models page: server cards (add/connect ollama/lmstudio/vllm/cloud), model list, load/unload (ollama), usage analytics.
- [x] **T14**, Context Inspector panel (unique feature #1).
- [x] **T15**, Guardrails settings UI (unique feature #2).
- [x] **T16**, Memory manager UI.
- [x] **T17**, Projects page: folder add/remove, index status, file browser.
- [x] **T18**, Connectors UI.
- [x] **T19**, Mascot: 8-bit pixel cat (CSS/canvas sprite), wave / biscuit-kneading / peek-from-edge / idle, hooked into app state (thinking → biscuits).
- [x] **T20**, Marketing website with downloads, feature tour, mascot.
- [x] **T21**, electron-builder config (win/mac/linux) + GitHub Actions release workflow.
- [x] **T22**, Core tests (vitest): guardrails, context assembly, outline, providers (mocked SSE).
- [x] **T23**, Polish pass: welcome/empty states, approval flow, command palette (Ctrl+K), keyboard shortcuts, toasts, onboarding banner, connector→context wiring, persistent Context Inspector prefs, app icon, boot smoke test.
- [x] **T24**, Docs: UI screenshots + walkthrough guide (docs/WALKTHROUGH.md).
- [x] **T25**, Verified local model end-to-end vs LM Studio (gemma reasoning model): reasoning streaming, base-URL `/v1` normalization, single + multi-step tool loops; friendly connection errors; workspace-bound chats; index-grounded context; persisted defaults; window state.

### Phase 2, Web & Docker editions (same code) (26–29), see [spec-web-and-hosted.md](spec-web-and-hosted.md)
- [x] **T26**, Extract `packages/host` (`createHost()` = KotrainApi surface + event emitter); desktop main rewritten as thin IPC wiring; verified via `scripts/itest-host.mjs` against LM Studio.
- [x] **T27**, `apps/server` (Fastify): shared `createDispatcher` routes `POST /api/:channel` + `/api/events` WS over the same host; `renderer/web-client.ts` provides `window.kotrain` over fetch/WS; `npm run web`; localhost bind + token when exposed.
- [x] **T28**, Docker: multi-stage `Dockerfile` + `docker-compose.yml` (volume workspace+data, `host.docker.internal`, localhost publish) + `.dockerignore` + GHCR publish workflow. Verified: 361MB image serves UI + API, reached LM Studio.
- [x] **T29**, `npx kotrain` package: `npm run bundle:web` → esbuild self-contained `kotrain` (server+engine inlined, fastify external, built `web/`). Verified by running the packed tarball outside the repo. Actual `npm publish` needs the user's npm login.

### Phase 3, Kotrain Cloud (paid, hosted), shipped portion (30, 31)
- [x] **T30**, Cloud foundation (`apps/cloud`): multi-account hosted edition wrapping the same host + renderer per account. Email+password auth (scrypt), bearer tokens, file-backed `CloudStore` (Postgres-swappable). Per-account isolation via `withDataDir` (`AsyncLocalStorage`) + settings cache keyed by data dir. Entitlements (free/pro/team) gated server-side; OSS app never license-checks. Thin `CloudLogin` renderer gate. 13 cloud tests + live smoke. `npm run cloud` (:4318). PR #54.
- [x] **T31**, Payments (Stripe): `apps/cloud/src/billing.ts` hand-rolled against the Stripe REST API (no SDK dep, same dependency-free DNA as the relay's APNs/FCM senders), gated on `STRIPE_SECRET_KEY`. `POST /api/billing/checkout` ({plan} → Checkout Session URL), `POST /api/billing/portal` (Customer Portal URL), and a raw-body `POST /api/billing/webhook` whose `Stripe-Signature` is verified with HMAC-SHA256 (`node:crypto`, replay-window check). `planChangeFromEvent` maps `checkout.session.completed` / `customer.subscription.updated|deleted` → a plan, applied via `store.setPlan` so entitlements update immediately; `CloudStore` now carries `stripeCustomerId` (+ `findByStripeCustomer`/`setStripeCustomer`). `/api/auth/config` advertises `{billing}`. 19 new cloud tests (signature verify incl. tamper/replay, event mapping, and a full signed-webhook → account-upgraded HTTP path via Fastify `inject`); 32 cloud tests total. Live Stripe charges need the user's keys (`STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET`/`STRIPE_PRICE_PRO`/`STRIPE_PRICE_TEAM` + `CLOUD_PUBLIC_URL`). Renderer Upgrade UI deferred (server-side complete).

### Chat UI/UX polish (post-1.0 desktop) (35–59)
- [x] **T35**, Chat redesign: fixed spacing + light/dark `color-scheme`, speech-bubble messages, collapsible Thinking box; in-chat metrics bar (context used/total + by-source tooltip, tokens/sec, thinking on/off, effort cycle → `EFFORT_TEMPERATURE`).
- [x] **T36**, Context Inspector Sources: manage workspace folders + attached files per chat; highlight SPEC.md + guideline/memory links; **living spec** (`buildSpec(sessionId)` → `<workspace>/SPEC.md`) with Build-from-chat + Live toggle (`session.specLinked`).
- [x] **T37**, Settings polish: accent-color swatch edge-to-edge; aligned toggle/control rows.
- [x] **T38**, Auto-updates: desktop `electron-updater` (GitHub feed, NSIS) behind first-run opt-in + Settings toggle + auto-check; Update banner; web compares served build version; app version via `app:info`.
- [x] **T39**, Models UX: auto-discovery probes `127.0.0.1` too; Test-connection in add form; cards auto-check Connected/Offline; Local vs Cloud sections.
- [x] **T40**, Chat control modes: per-chat Ask/Guardrails/YOLO + Settings default; in-composer Tools popover (`session.disabledTools`); Offline + Incognito; guardrails JSON editor.
- [x] **T41**, Command Center dashboard: next-action suggestions, usage stats + token chart, kanban of agent chats by activity with inline transcript, background-tasks/agents board.
- [x] **T42**, Multi-language UI (i18n): system-language detection + English fallback (en/es/fr/de/pt/ja/zh); Settings language picker.
- [x] **T43**, Branded NSIS installer (dark sidebar + Aphelion mascot + tagline) + hover Copy on chat messages.
- [x] **T44**, macOS Gatekeeper fix: afterPack ad-hoc signs the `.app`; `hardenedRuntime:false`/`gatekeeperAssess:false`; `xattr -cr` workaround documented (clean opening still needs notarization).
- [x] **T45**, Competitor-parity chat features: export (Markdown), regenerate, edit & resend (`session:truncate`), per-message + code-block Copy, slash commands, @-mention files, chat-list search + rename, model favorites.
- [x] **T46**, Released v0.1.1.
- [x] **T47**, Windows uninstaller: NSIS `customUnInstall` offers to delete user data; silent/auto-update uninstalls keep data. Released in v0.1.2.
- [x] **T48**, Data & privacy cleanup (Settings): delete chats by window, Reset configs, Delete everything; export/import helpers.
- [x] **T49**, Per-chat ⋯ menu: Pin / Rename / Delete.
- [x] **T50**, MCP client support: hand-rolled JSON-RPC-over-stdio client; `settings.mcpServers`; tools merged namespaced `mcp__<id>__<tool>`; Settings card + live status. Verified vs `@modelcontextprotocol/server-filesystem` (14 tools).
- [x] **T51**, Released v0.1.3.
- [x] **T52**, MCP tools per-chat toggle; MCP servers shown in Command Center workers board.
- [x] **T53**, Estimated cost: `MODEL_PRICING` + `estimateCostUSD`/`formatUSD`; `UsageSummary.bySession`; per-chat cost in metrics bar + Command Center.
- [x] **T54**, Conversation tags: `session.tags`, per-row chips, tag filter bar.
- [x] **T55**, Settings export/import: backup & restore (export/import config JSON).
- [x] **T56**, Native phone apps (Capacitor): `apps/mobile` wraps the shared renderer; RelayPairing first-run screen + QR (jsQR), local notifications; `mobile.yml` builds Android APK + iOS simulator (both green on CI).
- [x] **T57**, Remote push (relay-mediated): phone registers token with the relay; desktop sends content-free `notify` on `done`; relay pushes via APNs (HTTP/2 + ES256) and FCM (HTTP v1, RS256 → OAuth), dependency-free. Real delivery pending creds + device.
- [x] **T58**, CLI + MCP server (`kotrain`): runs the host in-process or against a running server over HTTP+WS; `kotrain status|sessions|chat|mcp|watch`; `kotrain mcp` exposes Kotrain as an MCP server (chat/list/new/get/status). Embedded into the npx package. Released v0.1.5.
- [x] **T59**, Local test loop + provisioning tooling: `npm run local` / `npm run web`; reusable vault skills `provision-keys` + `rotate-keys`; needs in [provisioning.md](../../obsurdian/projects/agent-nekko/provisioning.md); standing confirm-before-public rule.

### Phase 4, Workbench, terminals & sub-agents (60)
- [x] **T60**, Multi-pane Workbench + terminals + sub-agents + Command Center board. **Backend**: `packages/host/terminal.ts`, persistent-shell terminal service (one process per terminal, marker-delimited Warp blocks, `$?`-aware PowerShell exit codes, scrollback retained for reattach), exposed over a new `terminalEvent` bus channel wired through dispatch → preload/web-client/main IPC/server/cloud; `spawn_agent` builtin tool (core) handled in `host/chat.ts` (nested `sendChat` on a `parentSessionId` child session, depth-capped at 2, returns the child's last answer); `Session.parentSessionId` + terminal types in `packages/shared`. **Frontend**: `WorkbenchView` (project-grouped sidebar with nested sub-agents, tab strips, up to 3 side-by-side split columns), `ChatPane` (self-contained conversation extracted from the old `ChatView`, independent per-pane provider/model so agents run in parallel), `TerminalPane` (block terminal w/ history + interrupt), Zustand pane/split model + terminal state, Ctrl+J new-terminal. **Command Center**: prominent live *Active agent work* board (status/elapsed, model, msg+token counts, mode, last-answer snippet, nested sub-agents, Open/Stop), running/terminal stat tiles, Terminals strip. Verified: full build + all 27 core tests green; terminal create/run/cwd-persistence/exit-codes confirmed end-to-end over the web edition.

### Phase 5, Command Center / orchestration / spec-driven dev (61–65)
- [x] **T61**, First-class spec-driven development (Kiro-inspired). `shared/spec.ts`: `SpecMethodology`/`SpecDocDef` types + `SPEC_METHODOLOGIES` (kotrain spec→plan→tasks, kiro requirements→design→tasks, lean single-spec) + pure `parseTasks`/`toggleTaskLine`/`getMethodology` helpers. `host/spec.ts` generalized to `buildSpecDoc(sessionId, docId)` (role-specific system prompt; earlier artifacts chained in as context), `readSpecDocs`, `setSpecMethodology`, `toggleSpecTask`; `buildSpec` kept as a thin alias for the primary doc. New channels `spec:buildDoc|readDocs|setMethodology|toggleTask` wired through dispatch/host/ipc/preload/web-client. `settings.specMethodology` + `session.specMethodology`. Renderer `SpecPanel` (methodology picker, per-artifact Build/Update, Build-all, interactive tasks checklist + progress) replacing the thin Spec section in the Context Inspector; Settings default-methodology section. 8 new unit tests (parse/toggle/methodology), 35 core tests + typecheck across 8 workspaces green; verified rendering + methodology-switch persistence over the web edition.
- [x] **T62**, Agent orchestration strategies + swarm tree. `shared/orchestration.ts`: `OrchestrationStrategy` (solo/balanced/swarm) + `OrchestrationSettings` (strategy/maxDepth/maxParallel) + `ORCHESTRATION_STRATEGIES` (allowsSpawn + promptHint) + `DEFAULT_ORCHESTRATION` + pure `getStrategy`/`orchestrationPromptHint`. Real behavior: `host/chat.ts` withholds `spawn_agent` under `solo`, injects the strategy's guidance into the system prompt (new `PromptContext.orchestrationHint`), and reads `maxDepth` from settings (replacing the hardcoded const). `settings.orchestration` + store default. SettingsView "Agent orchestration" section (3 strategy cards + depth/parallel inputs, hidden under solo). CommandCenter `AgentCard` now renders a **recursive `SubAgentTree`** of the whole descendant swarm (live dots, descendant counts) instead of a flat 4-item list. 7 new unit tests (42 core total) + typecheck across 8 workspaces + builds green; verified Settings render + strategy persistence (swarm/solo/balanced) + conditional depth inputs over the web edition.
- [x] **T63**, Optimization insights ("Optimize" panel). `shared/insights.ts`: pure `optimizationTips({usage,sessions,providers}, limit)` → prioritized `OptimizationTip[]` (warn→suggest→info, then by saving). Heuristics: cloud-spend-while-local-available (with savings est.), expensive-model-on-short-chats (suggests a cheaper alternative via `cheaperAlternative`), context-heavy prompts (input:output ≥ 12:1), top cost driver (≥50% of spend), model-sprawl favorites nudge. CommandCenterView `OptimizePanel` renders the tips as severity-coloured cards + a total-potential-savings chip + "Manage models" link. 6 new unit tests (48 core total) + typecheck across 8 workspaces + builds green; verified rendering against seeded usage (use-local + top-driver tips, $5.29 savings) over the web edition, then cleaned the seed.
- [x] **T64**, Model auto-mode. `shared/model-select.ts`: `AUTO_MODEL_ID` sentinel + pure `isComplexPrompt`/`modelTier`/`recommendModel(models, prompt, preferred)` (complex→strongest tier, simple→smallest-capable tier≥2, favorites break ties). `session.autoModel` (added to all THREE setSessionOptions Picks, sessions.ts/host.ts/ipc.ts). ChatPane: ✨ Auto option in the model select (shown when >1 model), resolves the concrete model at send/regenerate/editResend time, persists `autoModel` on change, and previews "→ <model>" for the current draft. 8 new unit tests (56 core total) + typecheck across 8 workspaces + builds green; verified the `autoModel` field round-trips through the host (set true→reread→false) over the web edition.
- [x] **T65**, Refined design system (Warp/Linear-grade polish). `renderer/styles.css`: cleaner cooler neutrals (light + dark), more whitespace, soft elevation `--shadow-sm/md` on `.card`, a friendly `--accent-2` + `--brand-grad` (used on the Command Center wordmark via `.text-gradient`, `.btn-primary`, and the `.nav-item.active::before` indicator), focus `--ring` on inputs, button press transitions, tighter heading tracking; `--accent-soft`/`--ring`/gradient all derive from `--accent` (color-mix) so they track the user's accent. CommandCenter stat tiles got per-tile colour accents + more padding. typecheck + desktop/web builds green; verified the tokens/gradient-title/card-shadow/nav-indicator via computed styles over the web edition.

### Phase 6, IDE surfaces + prompt analyzer (66)
- [x] **T66**, Built-in file viewer/editor, integrated browser, file explorer, diff/approval, hoverable Context Inspector, and the prompt analyzer, the "stay in the app" wave. **Foundation**: `readFile`/`writeFile`/`listDir` IPC wired through shared/host(`files.ts`)/dispatch/preload/web-client (`readFile` → `{content, truncated, binary}`, 1 MB cap + NUL-byte binary detect; `listDir` dirs-first). **Dead-click fix**: `SpecPanel` ↗/row → `openFilePane(path)` instead of `openPath` (which silently failed with no `.md` handler); FilePane keeps a "reveal in OS" button. **Panes**: `WbPane.kind` extended to `chat|terminal|file|browser|diff`; store openers `openFilePane`/`openBrowserPane`/`openDiffPane`. `FilePane.tsx` (md Source/Preview toggle; mono editor + Save/Ctrl-S + dirty dot; binary/large notice). `BrowserPane.tsx` (`<webview>` + back/forward/reload/open-external; `webviewTag` enabled in main window). **Explorer**: `FileTree.tsx` (`ProjectFiles`) collapsible per-project tree, lazy children via `listDir`, `fileIcons.tsx` color-tinted chips (Linguist/Material palette). **Diff/approval (Devin-style)**: host `changes.ts` snapshots the original on first `write_file`/`edit_file` per session; `Δ N` button in the chat header; `DiffPane.tsx` client-side LCS line diff with per-line tick→Revert, Keep/Revert file, Keep-all/Revert-all; live `changesUpdated` event. **Hoverable inspector**: `ContextInspector.tsx` `InfoHint` group-hover popover on every section + source category. **Prompt analyzer**: `promptAnalysis.ts` (pure, no LLM) + `PromptAnalyzer.tsx`, A–F grade, part checklist, inline wavy underlines, suggestions, model recommendation (complexity/context heuristic → fast/balanced/frontier). All eight workspaces typecheck; desktop builds. **NOT yet exercised in the running GUI**, needs a hands-on pass (webview browsing, file edit+save, file tree, diff line-revert math, analyzer overlay); tracked under Road to v1.0. *Deferred follow-ups*: opt-in "Improve prompt" LLM rewrite (before/after diff); browser pane on `WebContentsView`; file-tree right-click new/rename/delete; markdown scroll-sync; live underline overlay in the textarea.

### Phase 7, Editor comments + Design board (72–73)
- [x] **T72**, **Inline editor comments.** `FilePane` gained a scroll-synced line gutter: a **+** on each line opens an inline comment box; commented lines keep a marker. Host `comments.ts` persists comments per file to `comments.json` (`comments:list/add/resolve` IPC, wired through dispatch/host/ipc/preload/web-client). A new `store.sendToChat(text, run)` sets a `composerInbox` that `ChatPane` consumes, **Add to prompt** appends the formatted `Re file:line, comment` block to the draft, **Run now** sends it once the provider is ready (guarded against the freshly-opened-pane race). A bottom comment dock lists a line's comments with re-send / resolve. Editor switched to a non-wrapping textarea so the gutter aligns row-for-row. No new deps. Verified end-to-end over the web edition (gutter + line numbers, dock on a line, comment persisted to `comments.json`, Add-to-prompt landed in the composer). Feature folder: `features/inline-editor-comments/`.
- [x] **T73**, **Design board (Figma-style).** New left-rail **Design** view (`DesignBoardView`, `View='design'` + nav entry + `LayoutIcon`). Each page is a card with a live, scaled, read-only `<iframe>` "snapshot" (no capture infra, mirrors the app as it renders); zoom slider; add/remove pages. Host `design.ts` persists pages + notes per workspace to `design.json` (`design:get/addPage/updatePage/removePage/addNote/resolveNote`). A side sheet pins **persistent notes** and routes **comments** (Add to prompt / Run now) via `sendToChat` carrying page identity. Subscribes to `onAgentEvent`/`onChangesUpdated` filtered to the workspace's sessions to flag pages **updating** and reload previews; the badge opens the running agent's chat (`openChatPane`). Verified end-to-end over the web edition (added a page with a live snapshot, pinned a note that round-trips through `design.json`, routed a comment into the composer; screenshot captured). Feature folder: `features/design-board/`.

### Phase 8, Command Center, automation & skills (74–77)
- [x] **T74**, **Cost breakdowns + empty-state placeholders.** Host `usage.ts` now computes `totalCost`, `bySessionCost`, and per-day `cost` (via `estimateCostUSD`). Command Center **Cost** panel: this-month actual + month-end projection + all-time, a daily-spend bar chart, a per-agent breakdown (accurate to each session's model), and a collapsible token-pricing reference (USD/1M, published list prices). A reusable `ChartEmpty` skeleton placeholder shows for any metric with no data yet (Cost + Token usage). UI hardened against older/missing usage shapes (`?? {}` / `?? 0`).
- [x] **T75**, **Tasks & scheduled work.** Host scheduler `tasks.ts` (+ `tasks.json`): `scheduled` (one-shot at a time), `recurring` (every interval), and `background` (long-running; keep alive **forever** or **until** a condition the agent judges met via the `⟦DONE⟧` token). A periodic tick fires due tasks through `sendChat`, reusing one session per task; `tasks:list/create/update/delete/runNow` IPC + a `tasksUpdated` event, wired through dispatch/host/ipc/preload/web-client. Command Center board split: model servers/MCP/relay → **Services & model servers**; new **Tasks & scheduled work** board with cadence/status/run-count/last-result + Run-now / Pause-Resume / Open-chat / Delete, updating live. **Every task links to a chat**: `createTask` provisions a tagged session (`session.taskId`) up front so scheduled tasks/goals always open a chat (even pre-fire); task chats are kept out of the agent board / All-chats / sidebar and dropped on delete if still empty.
- [x] **T76**, **⚡ Automate menu in the agent window.** `ScheduleTaskModal` (opened from a chat header button) creates a scheduled / recurring / background task, pre-filled with the chat's project/model/draft; background tasks pick keep-alive forever or until a condition.
- [x] **T77**, **Skills in the `/` menu (+ goal).** `shared/skills.ts`: a standard skills registry (research, plan, review, security-review, simplify, test, explain, fix, commit, pr) shown in the composer's `/` menu above saved prompts. **`goal` is highlighted (★)**: `/goal <condition>` routes to a background "until" task (work-until-done) instead of a one-off turn, surfaced in the Tasks board. Verified end-to-end over the web edition (all sections render with empty-state placeholders; created scheduled + background `/goal` tasks that persisted and showed in the dashboard; skills menu lists the set with goal starred; screenshot captured).
- [x] **T79**, **Skills tab + workflow visualizer (n8n / Make-style).** Extended `shared/skills.ts`: each `SkillDef` now carries a `category` (Research & planning / Code quality / Delivery / Automation), a `tools` list, and a real `workflow` graph (`SkillNode` kinds trigger/context/agent/tool/decision/loop/output + `SkillEdge` with an optional `back` flag for loop returns). Added a **pure, unit-tested** `layoutWorkflow` (longest-path layered layout; fan-outs splay and centre; back edges ignored for layering) → `LaidOutNode` x/y. New left-rail **Skills** view (`SkillsView`, `View='skills'` + `WandIcon` nav entry + command-palette "Go to Skills"): searchable category-grouped list (goal ★) on the left; on the right the selected skill's header (category/background-agent chips, tools), a **Use in chat** button (`store.sendToChat(template)`), and a **WorkflowCanvas** rendering the graph on a dotted node-editor canvas, node cards with colour-coded type borders + glyphs, bezier connectors with arrowheads, branch labels, and dashed return arrows for loops. 10 new core tests (66 total). Verified over the web edition: all 11 skills grouped correctly, `/research` fan-out splays to 3 parallel Search nodes, `/goal` shows the decision + dashed "no" loop back to "Do the work"; CDP screenshots captured (preview screenshot tool still hangs on the SPA, used the headless-Chrome fallback). `nav.skills` i18n added.

- [x] **T80**, **Skills marketplace.** New `shared/skills-market.ts`: `MarketplaceSkill` catalog split into `KOTRAIN_SKILLS` (6 first-party: review-council, spec-sync, changelog, standup, dep-audit, a11y-audit) and `POPULAR_SKILLS` (Anthropic official skills, obra/superpowers, community packs) with curated star/install metric snapshots (offline-first, `popularSkills()` ranks by them); pure `marketWorkflow` (bespoke graph or derived trigger→context→agent→tools→output chain), `marketToSkillDef` (runnable in-app skill), `skillToMarkdown` (SKILL.md with frontmatter). Host `skills.ts` + `skills.json`: `listInstalledSkills` (self-heals deleted file installs), `skillTargets` (Kotrain always; Claude Code / Codex detected via `~/.claude` / `~/.codex`), `installSkill` (kotrain = record only → joins the `/` menu + Skills library; claude/codex = writes `<skills dir>/<name>/SKILL.md`, never clobbering folders it didn't create), `uninstallSkill`. Channels `skills:installed|targets` + `skill:install|uninstall` wired through the five-touch chain. Renderer: store `installedSkills`/`installedSkillDefs` + `refreshSkills()` (booted from App); `matchSkills(query, extra)` merges installed skills into the composer `/` menu; `SkillsView` gained **Library | Marketplace** tabs, marketplace shelves (Installed / Nekko Labs / Popular online) with metric chips, per-target Install/Remove rows, Use-in-chat for installed skills, and the shared `WorkflowCanvas` (now workflow-driven, not SkillDef-driven) + an instructions card. 9 new core tests (75 total).

- [x] **T81**, **Workflow explainer.** The Skills visualizer now teaches as well as shows: `WorkflowExplainer` wraps the canvas with a side panel that lists the steps in run order (layer/row order from `layoutWorkflow`), numbered with the node-kind colour. Clicking a node (canvas or list) selects it, ring highlight on the canvas, and the panel explains the step: its label/detail, a plain-language description of its node kind (`KIND_EXPLAIN`), and its branches/loops via `describeEdges` ("yes → Done", "no: loops back to …", "repeats after …"). Shared by the Library and Marketplace tabs (the marketplace instructions card renders beneath the canvas). Renderer-only change; build + typecheck green.

- [x] **T82**, **Agent types + per-agent queues (Command Center).** New `shared/agent-types.ts`: pure `classifyAgent(signals)` → `AgentType` (role/label/icon/color) from the skill, task kind/condition, prompt, and model, monitors (recurring/background watch work, "Production monitor" when prod/deploy language), code-review bots, research agents, build agents, automation, else assistant; `detectSkill`, `agentSignals(session, task?)`, `classifySession`. `Session.queue?: string[]` + host `queuePrompt`/`dequeuePrompt` (sessions.ts) + channels `chat:queue`/`chat:dequeue` through the five-touch chain; `sendChat` auto-runs the next queued prompt when a turn ends (unless aborted), and `persist()` re-reads the on-disk queue so mid-run additions aren't clobbered. Command Center: a **Fleet** panel groups running/recent agents + active tasks by type with live counts; `AgentTypeBadge` on agent cards and task cards; agent cards list the run-queue with per-item remove; tasks lifted to the parent (one `onTasksUpdated` subscription) and refreshed on `done` so queue/last-message update live. ChatPane: a **Queue** button (queues the draft even while streaming) + a queued-items strip above the composer; host-started turns (queued follow-ups / task runs) now reflect as streaming. 11 new core tests (86 total). Verified over the web edition: skills + queue + classify channels round-trip via HTTP, and the Fleet ("Code review bot" + "Production monitor"), type badges, queued prompt, and monitor task all render with no console errors.

- [x] **T83**, **Fresh color palette (off orange).** New default accent is indigo-violet `#6d5efc` with a cyan `#06b6d4`/`#22d3ee` secondary → violet→cyan brand gradient (Linear/Vercel-era developer-tool look), replacing the salmon `#ff7a59`. `shared/settings.ts` gained `DEFAULT_ACCENT` + `LEGACY_ACCENTS`; host `store.getSettings` migrates any stored legacy-default accent to the new one on load (custom accents untouched), and `defaults()` uses `DEFAULT_ACCENT`. Cooled the neutral tokens in `renderer/styles.css` (light + dark) and repointed `--accent`/`--accent-2`; the rail logo now uses the brand gradient. Updated the hardcoded fallbacks (TerminalPane xterm theme + selection), the branded NSIS installer art (`gen-installer-art.mjs`), and the marketing website tokens + primary button gradient. The 8-bit Aphelion mascot sprite stays ginger (persona, not chrome). Verified over the web edition via computed styles: `--accent` #6d5efc, brand gradient violet→cyan, nav-active/ring/primary-button all violet, no console errors. Build + typecheck green.

- [x] **T84**, **MCP over HTTP + KotrainMCP integration.** The MCP client now speaks **streamable HTTP** alongside stdio: `McpServerConfig` gained optional `url` + `token` (a config with `url` connects over HTTP; stdio otherwise), the hand-rolled client POSTs JSON-RPC with dual-accept headers, parses JSON or SSE-framed replies, echoes `mcp-session-id`, and surfaces 401s with a check-the-token hint. New shared `KotrainMcpInfo` + host `detectKotrainMcp()` (probes `http://localhost:7777`, `KOTRAIN_MCP_URL` override, 1.5s timeout) wired as `mcp:kotrain` through the five-touch chain. Settings → MCP servers: a **🐾 KotrainMCP detected** card (version + managed-server count) with one-click **Connect gateway** (adds/updates the `kotrain-mcp` entry with URL + token, enabled) and **Open manager** (KotrainMCP's web UI in a workbench browser pane); an **+ Add URL** button for any HTTP MCP endpoint; server cards show an http/stdio chip and URL+token inputs for HTTP entries. Verified: `scripts/itest-mcp-http.mjs` (host → KotrainMCP gateway → echo server round trip, bad token rejected) + hands-on over the web edition (detection card, one-click connect showing "1 tools", manager pane). Counterpart daemon work (streamable-HTTP gateway + token + served UI) landed in `nekko-labs/kotrain-mcp` v0.2. · Done: 2026-07-04

- [x] **T85**, **Kotrain Dojo skills hub integration + KotrainMCP discoverability.** The Marketplace gained a **Kotrain Dojo** shelf wired to [nekko-labs/nekko-dojo-skills](https://github.com/nekko-labs/nekko-dojo-skills). New `shared/dojo.ts`: `DojoCatalogSkill`/`DojoCatalog` types mirroring the repo's `catalog.json`, a bundled offline `DOJO_SNAPSHOT`, raw-URL helpers, pure `dojoCategory` (dojo free-form category → SkillCategory), `splitSkillMd` (frontmatter/body), and `dojoToMarketSkill` (catalog entry → `MarketplaceSkill`, carrying a fetched SKILL.md verbatim as `markdown`). `MarketplaceSkill` gained `tier` ('kotrain-official'/'community') + `markdown`; `skillToMarkdown` writes `markdown` verbatim when present; `InstalledSkillRecord` gained an optional `skill` snapshot so non-catalog (Dojo) installs stay resolvable offline. Host `dojo.ts`: `getDojoCatalog(refresh?)` (no network unless refreshed: cache in `dojo.json` else bundled snapshot; live fetch validates entries + slugs and falls back on failure) and `getDojoSkillMd(slug)` (verbatim SKILL.md at install time), host-side per the T84 lesson (renderer cross-origin fetch is blocked). `installSkill(skillId, target, payload?)` accepts a payload for non-catalog skills (name sanity-checked, snapshot persisted on the record); channels `dojo:catalog` + `dojo:skillMd` and the extended install signature wired through the five-touch chain. Renderer: SkillsView marketplace **Kotrain Dojo shelf** (offline/cached/live indicator, **↻ Refresh** live-fetch, **Browse ↗**, trust-tier chips with a community caution note in the detail pane, install flow that fetches the real SKILL.md); store `refreshSkills` resolves record snapshots so installed Dojo skills join the `/` menu + Library. Settings → MCP servers: a quiet **Get KotrainMCP** hint row when no daemon is detected and no gateway configured (tri-state probe so it never flashes while probing). 9 new core tests (95 total) + 32 cloud green; typecheck across all workspaces; verified over the web edition: `dojo:catalog` returned the bundled snapshot offline and `source:'live'` after refresh (both skills from GitHub), `dojo:skillMd` returned nyaa's real SKILL.md, a scripted install round-trip installed dojo-nyaa to Kotrain (snapshot persisted) and to Claude Code (verbatim SKILL.md written then cleanly uninstalled), and the UI showed the shelf/chips/detail/hint with zero console errors (screenshot MCP still hangs on the SPA; DOM checks used). · Done: 2026-07-11

### Phase 9, Kotrain rebrand + kendo mascot + immersive website (87–89)
- [x] **T89**, **Marketing website rebuilt in the liquidink "immersive space" style.** `apps/website` rewritten (still framework-free HTML/CSS/JS): deep-space palette sampled from liquidink.design/github_web (bg `#01050b`, panel `#010e22`, muted `#8ca5ae`, Inter Tight + JetBrains Mono), a fixed **starfield canvas** (stars drifting toward the viewer, resolution-scaled count, reduced-motion aware) under fixed **nebula gradients** tinted with the Kotrain violet→cyan brand, a giant `KOTRAIN` display hero with the new tagline ("Train your models. Run your agents. Own your machine."), mono uppercase eyebrows and numbered chapters (01 THE SURFACE … 05 TOUCHDOWN), glass cards/buttons (`backdrop-filter`), scroll reveals (IntersectionObserver + an unconditional viewport/scroll fallback so content shows even where IO/rAF are throttled), the **kendo Kotrain** doing bokken suburi in the hero, and the retained window mock / feature tour / local-models spotlight / positioning / editions / OS-aware download cards. Dark-only (theme toggle removed). Verified: DOM checks over the static server + real full-page screenshots via headless Chrome (`--headless=new --virtual-time-budget`; the preview screenshot tool still hangs on animated pages). · Done: 2026-07-18
- [x] **T88**, **Kendo mascot: Nekkos trains while the model thinks.** `components/Mascot.tsx` redrawn: red hachimaki bandana (with knot tails), indigo keikogi + hakama + belt over the ginger cat. New `useTrainingMove` hook cycles **punch (2.8s) → side-kick (2.8s) → bokken overhead swings (3.6s)** while `mood === 'thinking'`; waving stays for greeting. The bokken group (raised arms + gripped sword + tsuba) rotates around the shoulders with a suburi timing curve (slow lift, fast strike, brief hold); the kick leg snaps out from the hip; punches alternate arms with mirrored keyframes. ViewBox extended upward (`0 -12 36 52`) so the raised bokken isn't clipped. MiniNekkos (inline spinner) got the bandana + gi and quick alternating jabs. New keyframes in `styles.css` (`nekkos-punch(-r)/jab/kick/swing/guard`). Verified over the web edition via DOM: outfit colors render, all keyframes registered, zero console errors (screenshot MCP still hangs on the SPA). · Done: 2026-07-18
- [x] **T87**, **Rebrand: Open Paw → Kotrain (superseded by T113's Nekkos rename, then reinstated by T116 below).** Full scripted sweep of every branded identifier across all tracked text files: npm packages `@open-paw/*` → `@kotrain/*`, the npx package `open-paw` → `kotrain`, CLI bin `opaw` → `kotrain` (legacy `opaw` alias kept), product name "Open Paw" → **Kotrain**, appId `dev.nekkolabs.openpaw` → `dev.nekkolabs.kotrain`, env vars `OPENPAW_*` → `KOTRAIN_*`, GitHub repo `nekko-labs/open-paw` → `nekko-labs/kotrain`, GHCR image + workflows, installer art, website naming (kotrain.com). **Back-compat**: server + CLI read the new env names with `OPENPAW_*` fallback; the default data dir moved but a pre-rebrand `~/.open-paw` keeps being used if present; desktop main migrates the legacy "Open Paw" userData dir on first run (`migrateLegacyData`). Lockfile regenerated; build + typecheck across all workspaces + 95 core / 32 cloud tests green. · Done: 2026-07-18

### Phase 10, Training + Goals tabs (90–91)
- [x] **T90**, **Model Training tab (data-scientist agent + idea maze).** One shared run engine for both new tabs. `shared/training.ts`: `TrainingRun`/`ExperimentNode`/`RunHint`/`TrainingConfig` types, pure `runStats` (best/success-rate/niches/self-repairs/runtime), `layoutMaze` (deterministic DFS layout: chronological left→right, branches under parents, best-path ancestry), `bestExperiment`/`isBetterScore` (direction-aware), `formatRuntime`, `APPROACH_PRESETS`, `RUN_DONE_TOKEN`. Host `training.ts` (+ `training.json`): run CRUD, start/pause/stop, hint queue, and a turn loop that drives the run's dedicated session through `sendChat` with a data-scientist brief (purpose, dataset incl. HF/Kaggle/local/URL, base model, metric + direction, expert levers, harness artifacts) then a compact tree-brief each later turn; unconsumed hints fold into the next turn; `RUN_DONE_TOKEN` completes the run; running runs **auto-resume on host restart**. New core tool `REPORT_EXPERIMENT_TOOL`, offered only to sessions with `Session.trainingRunId` and handled host-side (`reportExperiment`: upsert node, leader tracking with minimize support, milestone/failure log, replies with the current standing). Channels `training:list|create|update|delete|start|pause|stop|hint` + `trainingUpdated` event through the five-touch chain. Renderer: **Training** nav view (run list, simple form: purpose/dataset/base-model/metric/harness + Expert levers accordion; dashboard: stat tiles, champion card, **IdeaMaze** SVG with vertical labels, status colors, path-to-leader, node detail on click, HintComposer, activity log); shared `components/RunBoard.tsx`; run sessions hidden from chat boards/sidebar. 8 new core tests (103 total). Verified over the web edition: run + hint via HTTP API, seeded 7-experiment tree rendered all tiles/maze/legend/champion/hint-chip with zero console errors, and a direct host integration test exercised leader tracking, non-improving records, auto-ids, and error paths. · Done: 2026-07-18
- [x] **T91**, **Goals tab (long-running goal solving).** Same engine, `kind: 'goal'`: **Goals** nav view with a new-goal form (goal + name + workspace + **approach preset cards**: AutoML sweep / feature-first / hyperparameter search / ensemble & stacking / transfer learning / data-centric / evolutionary / custom, each folding its hint into the agent brief) and a long-horizon dashboard: header card with a big **"working for"** clock + status + approach chip, shared stat tiles / idea maze / champion, a larger activity feed, and the guidance composer for course corrections, new ideas, or new-data pointers mid-run. Verified over the web edition (goal created via API, dashboard + all 8 preset cards render, no console errors). · Done: 2026-07-18

### Phase 11, UI cleanup pass (92)
- [x] **T92**, **Nav + chat surface cleanup (Projects removed, hover rail, sidebar + composer polish).** Removed the dedicated **Projects** view (nav item, `ProjectsView.tsx`, `View` union member, palette entry, `nav.projects` i18n keys, colored nav icon); folder add stays in the chat **+ → Folder** menu and the Context Inspector, and the index keeps powering `@` mentions/context. **Hover-expanding rail**: the icon rail is now a 64px-wide `relative` nav whose inner `.rail` overlays the content and springs to 200px on `:hover` (or `:has(:focus-visible)`, deliberately not `:focus-within`, which pinned it open after a click), with label fade/slide (`.rail-label`) and a "Kotrain" wordmark by the logo. **Workbench sidebar**: folder headers use a rotating SVG chevron + tracking-wider label + item-count badge when collapsed, sections animate via the `grid-template-rows 0fr→1fr` trick (`.collapse-wrap`), the boxy `bg-surface-2/30 border-l` child container is gone, active chat rows highlight with `bg-accent-soft` + accent bullet. **Chat pane**: header now shows the session title; unified `.composer` card (borderless textarea + attach menu + Queue + gradient send icon button inside one rounded surface with a whole-card focus ring); friendlier empty states for chat and Memory; metrics bar hides `tok/s` until a turn produced tokens. Verified in Chrome against the web edition (rail expand/collapse, folder collapse, slash menu anchoring, empty states). · Done: 2026-07-18

### Phase 12, Goals plan-first + Design studio (93–96)
- [x] **T93**, **Goals tab rework: plan-first, execute, iterate (not model testing).** The Goals experience was refocused per the product intent: model testing lives in Training; Goals is "give the agent something to finish". `shared/training.ts`: new `PlanStep`/`PlanStepStatus` types, `TrainingRun.plan`, and pure `planProgress` (done+skipped ratio, current-step pick; 4 new core tests, 107 total). New core tool `UPDATE_PLAN_TOOL` (`update_plan`), offered beside `report_experiment` to run sessions and handled host-side (`updateRunPlan`: replace or upsert-by-id with auto ids, step-done/skip logged as milestones, echoes the plan + ids back). Goal-run prompts rewritten around three phases, PLAN FIRST (4-12 concrete verifiable steps via update_plan before any execution), EXECUTE (active → done-with-note only when verified), ITERATE (revise the plan when reality disagrees), with continue-turns fed a `planBrief`; the ML approach presets were removed from the goal flow (goal `config.extra` now carries free-form context & constraints from the form). Renderer `GoalsView`: new-goal form with a Plan/Execute/Iterate explainer strip + context field (no preset cards), dashboard rebuilt around the **live plan checklist** (status glyphs, notes, progress bar), a phase chip (planning/executing/verifying/finished), goal-shaped stat tiles (plan progress / iterations / attempts / runtime), current-step card, activity feed, and per-goal plan progress bars in the sidebar list; the idea maze + champion stay Training-only surfaces. Verified over the web edition with a seeded 6-step in-flight run (checklist, phase chip, tiles, activity, current-step all render; form shows the new strip). · Done: 2026-07-19
- [x] **T94**, **Design studio: sketch → code prototype + prompt → design (Claude Design-style).** The Design tab became a creation surface with two first-class ways in. `shared/design.ts`: `DesignPage` gained `kind: 'live' | 'concept'`, `html`, `prompt`, `origin: 'prompt' | 'sketch'`, `file`; new `GenerateDesignInput`. Host `design.ts` `generateDesign(workspaceId, input)`: one-shot generation with the default provider/model (spec.ts pattern, no agent loop), a designer/engineer system prompt demanding one self-contained responsive HTML document (no external resources), sketch PNG data URLs passed as multimodal images, code-fence/preamble stripping, refine mode that feeds the existing html + change request, and the prototype mirrored into `<workspace>/kotrain-designs/<slug>-<id>.html` (refines overwrite the same file); wired as `design:generate` through the five-touch chain. Renderer `DesignBoardView`: header actions (✏️ Sketch / ✨ Generate / + Live page), a two-card **Draw it / Describe it** empty-state hero, `SketchStudio` (full-screen pointer-events canvas built for iPad/iPhone over the web edition: Apple Pencil/touch with `touch-action: none`, DPR-sharp strokes, 5 pen colors, 3 sizes, destination-out eraser, undo/clear, optional note, white-backed PNG export), `PromptStudio` (headline prompt box + starter idea chips + Cmd/Ctrl-Enter), a shimmer **Designing…** placeholder card, concept cards rendered via iframe `srcdoc` with origin chips, and a side-sheet **Refine** box per concept (busy state, prompt echo) plus open-prototype-file and file-path-aware agent comments. Verified over the web edition against a live Ollama qwen3:14b: prompt → concept card + workspace file, sketch canvas draws and gates Generate on stroke count. · Done: 2026-07-19
- [x] **T95**, **Run model picker (wraps up a concurrent session's half-done GoalsView edit).** A parallel session had started wiring a `modelOverride` field into the old goal form (state never bound to an input, aimed at a field that doesn't exist on `NewTrainingRun`); superseded it with the real thing on the plan-first form. New shared `RunModelPicker` in `RunBoard.tsx`: provider select (enabled providers, "App default" first) + a model select fed live by `listModels(providerId)` (free-text model-id fallback when a provider can't list, e.g. no key yet); the override only applies as a complete provider+model pair, since `createTrainingRun` pairs `input.providerId ?? default` with `input.modelId ?? default` and a half-set pair would mismatch. Wired into the Goals form ("Agent model (optional)", the model that drives the goal for hours/days) and the Training form ("Data-scientist model (optional)", explicitly distinct from the base model being trained), both passing `providerId`/`modelId` through the already-supported `NewTrainingRun` fields onto the run's session. Also: `.gitignore` now excludes `kotrain-training/`/`kotrain-goal/`/`kotrain-designs/` (runtime artifacts when this repo is dogfooded as a Kotrain workspace) and the Training purpose label lost its em dash. Verified over the web edition: picker renders in both forms, loads the live Ollama model list, and a goal created with an override lands the chosen provider/model on its driving session. · Done: 2026-07-19
- [x] **T96**, **Goal-loop debug (bounded context + termination guards) + Command Center unified board + Goals mission-control reframe.** Four pieces of feedback ("goals shouldn't be a chat"; the Command Center board should lead, obviously a board, with the stats after a divider; the goal steer box was tiny and oddly placed; goals ran thousands of iterations and got stuck). **Engine (the debug):** the Goals/Training loop drove one ever-growing chat session, replaying the whole transcript to the model every turn, and re-armed forever relying solely on the model emitting `RUN_DONE_TOKEN`, so a weak/stuck agent looped for thousands of turns repeating itself. Fixed in `core/agent/loop.ts` + `host/{chat,training}.ts`: a new opt-in `SendOptions.maxHistoryTurns` windows the model input to the last N user-turn groups (cut on user boundaries so tool_use/tool_result pairs stay intact; `windowHistory` pure + unit-tested; normal chats send full history unchanged), and run turns pass `RUN_HISTORY_TURNS=4`; continue-turn prompts were made self-contained (re-state goal + constraints) since old turns are no longer replayed. `tickRun` now has host-enforced guards so a run can't spin forever: **time-budget** enforcement (`config.timeBudgetMin`), a **turn safety cap** (`config.maxTurns`, default `RUN_MAX_TURNS_DEFAULT=400`; resuming past it grants a fresh leg), **stall detection** (a `progressSignature` over plan+experiments plus an `outputSignature` of the answer; no measurable progress AND a repeating answer for `STALL_LIMIT=8` turns → **pause** with a log asking for a hint; adding a hint or resuming resets it), and an **error-loop cap** (`ERROR_LIMIT=5` consecutive failed turns → fail). **Command Center:** replaced the separate "Active agent work" grid + "All chats" lanes with ONE unified kanban **Command board** (Running / Recent / Idle lanes, `BoardLane`, sub-agents still nested, each lane capped + "+N more") as the hero at the top, then a **divider**, then the stat tiles, then Fleet/Optimize/etc.; removed `KanbanCard`. **Goals:** reframed as mission control, the hint box moved out of the cramped 300px rail into a full-width **"Steer the mission"** console (`HintComposer` gained `title`/`helper`/`buttonLabel`), "Open chat" became a low-key **Transcript** link, and a new **Mission control** card surfaces the turn/time budgets the loop enforces. Verified over an isolated seeded web edition (DOM-checked): board is the hero with Running/Recent/Idle lanes populated + sub-agent/goal sessions excluded, divider sits between board and stats, and the Goals dashboard shows the full-width steer console, Transcript button, Mission control card, pending-hint chip, and the stall-pause entry in the activity log, zero console errors. 111 core (+4) + 32 cloud tests, typecheck ×8, build green. · Done: 2026-07-20

### Phase 13, Training page clarity pass (97)
- [x] **T97**, **Training run page: artifacts made concrete + "what's happening" clarity + control regroup.** Four bits of feedback on the model-training page ("the artifact of a training isn't clear"; "it should be clear what is actually being done"; "when you start, it should take you to the page to see it starting"; "put the start and stop buttons nearby each other"). **Artifacts (the big one):** runs had no structured record of what they produced, only free text in the log. New `shared/training.ts` `ArtifactKind` + `RunArtifact` (kind/title/workspace-relative path/note) and `TrainingRun.artifacts`, plus shared `runFolderSlug`/`runOutputDir` helpers (host + UI now agree on the `kotrain-training/<slug>` · `kotrain-goal/<slug>` output folder; host dropped its private `folderSlug`). New core tool `REPORT_ARTIFACT_TOOL` (`report_artifact`), offered beside `report_experiment`/`update_plan` to run sessions and handled host-side (`reportArtifact`: upsert-by-path, first-sighting logged as a milestone); the agent briefs now tell the model to call it the moment each artifact (the trained model + every harness file) exists on disk and to keep everything under the run's output folder. Renderer: new shared **`ArtifactsCard`** (lists each artifact with a kind icon, title, path, and how-to-use note; every path + the run's output folder open in the OS file manager via `openPath`; empty state names the output folder), added to both the Training and Goals dashboards. **What's happening:** a one-line explainer under the Training header, plus a shared **`RunNowStrip`** that (while running) shows an explicit "starting up…" state the instant you hit start, then the currently-running experiment + latest activity line. **See it starting:** `onCreated` now merges the new run into local state immediately so create-and-start drops you straight on the live dashboard with no flash back to the form. **Controls:** Start · Pause · Stop are now one grouped cluster, set apart by a divider from Open chat/Transcript + Delete, on both dashboards. No new IPC (artifacts ride the existing `training:*` + `trainingUpdated` chain; the tool routes through `sendChat`). Build + typecheck ×8 + 111 core tests green. · Done: 2026-07-21

### Phase 14, Local-server management + VRAM + thinking toggle + context tooltip (98–99)
- [x] **T98**, **Model-server management, GPU/VRAM monitor, per-chat thinking toggle, and an expanded context tooltip.** Four pieces of feedback on the Models/Chat surfaces. **Models tab, loaded state + management:** local server cards now show which models are **loaded** in memory and auto-refresh it every 6s while connected. Ollama reports loaded + per-model VRAM from `/api/ps` (`size_vram`); LM Studio uses its native `/api/v0/models` `state` field (new `lmStudioModels()` in `openai-compat.ts`, falling back to `/v1/models`); vLLM's served model is marked always-resident. Ollama keeps per-model **load/unload**; every local provider gains a **Stop server** action, a new host module `servers.ts` resolves the PID listening on the provider's port (Windows `netstat`, POSIX `lsof`) and kills its tree (`taskkill /T /F` or SIGTERM→SIGKILL), which is the unload path for servers with no HTTP unload (LM Studio/vLLM). New channel `server:stop` → `host.stopServer`. **VRAM/GPU:** new host `gpu.ts` shells `nvidia-smi` (CSV query, 2.5s TTL cache, null when absent) exposed via `gpu:stats` → `host.getGpuStats`; shared `GpuStats`/`GpuDevice` types. New renderer `GpuStats.tsx` (`useGpuStats` poll hook + `VramInline` for the metrics bar + `VramPanel` for the Command Center). The Command Center gains a **GPU & VRAM** panel (per-GPU total/used/free + utilization bar) and the Chat metrics bar a compact **VRAM x / y GB** readout with a per-GPU hover breakdown. **Thinking toggle:** `Session.thinking?: boolean` + `ChatRequest.think` threaded through `runAgent` into both providers (Ollama native `think`; OpenAI-compatible `chat_template_kwargs.enable_thinking`, local kinds only so cloud endpoints aren't sent unknown fields); Ollama's `message.thinking` deltas now stream as reasoning. Shared `modelSupportsThinking()` (reasoning-family id/name regex) drives a per-chat **on/off toggle** that replaces the passive "thinking" indicator for reasoning-capable models. **Context tooltip:** the metrics bar's by-source hover became a Claude-Code-style breakdown, a segmented usage bar, each source's tokens + share of the window, a **Free space** row, and the total. Shared helpers `isLocalProvider`/`LOCAL_PROVIDER_KINDS`. Verified live over the web edition against a real LM Studio (7 models, `/api/v0/models` state confirmed) + an RTX 5090: Stop-server button local-only, VRAM panel + inline readout show live GPU numbers, the thinking toggle appears only for qwen3 and flips on/off (persisted), and the expanded tooltip renders per-source %+Free space. Build + core tests (111) + typecheck across server/cloud/cli/relay green. · Done: 2026-07-22
- [x] **T99**, **Per-model load/unload for LM Studio via the `lms` CLI.** Follow-on to T98's design note: LM Studio's OpenAI-compatible HTTP API has no per-model unload, so T98 left "Stop server" (kill the process on the port) as its only unload path. This wires true per-model control by shelling out to LM Studio's `lms` CLI, giving LM Studio the same load/unload chips Ollama has instead of the all-or-nothing Stop server. New host module `lms.ts`: `lmsProbe(baseUrl)` (available only for a **localhost** instance with `lms` installed, resolving the binary from `~/.lmstudio/bin/lms(.exe)` then PATH; `version` result cached 15s so the 6s Models poll doesn't spawn a process each tick), `lmsLoad`/`lmsUnload` (`lms load <key> -y` / `lms unload <key>`; success is gated on exit-0 **and** the output not matching a failure phrase, since `lms` exits 0 even on "Model Not Found"; ANSI-stripped CLI headline surfaces as the toast on failure; 3-min load timeout so a large model isn't killed mid-load). Model ids line up across surfaces: the `/api/v0/models` id = the `lms` identifier, so a listed model's id passes straight through. `host.loadModel`/`unloadModel` route `lmstudio` through `lms.ts` (Ollama unchanged); new `lms:probe` → `host.lmsAvailable`; `loadModel`/`unloadModel` returns widened to carry an optional `message`. Shared `LmsProbe` type. Models page: LM Studio cards probe on mount and, when available, render Ollama-style per-model **load/unload** chips (toasting the CLI error on failure); a remote instance or a missing `lms` falls back to T98's static "loaded" badge (its tooltip now explains why) + Stop server. Verified against the real `lms` on this machine: probe true on localhost / false+reason on a remote URL; a real embedding model **loaded then unloaded** through the module end-to-end (confirmed via `lms ps`), and bogus keys return `ok:false` with the real "Model Not Found" headline. Build + typecheck ×8 + 111 core + 32 cloud tests green. · Done: 2026-07-22

### Phase 15, Phone remote control (T33 completed, 100)
- [x] **T100**, **Phone remote control, complete (closes T33).** Run and manage chats, training, and goals from a paired phone, with real device security and a hosted relay.
  - **Relay v2** (`apps/relay/src/server.ts`, rewritten): per-client `cid`s, client frames tagged to the agent (`{type:'c',cid,data}`), agent **unicast** replies/events (`{type:'d',cid,data}`) so no device sees another's traffic, `kick` for revocation, per-device push tokens (`register-push` carries deviceId; `push-remove` drops it), timing-safe key compare, 10 MB frame cap, token-bucket rate limit (40/s, burst 400), 16 clients/room + 10k room caps, and an optional managed-hosting gate (`KOTRAIN_RELAY_AUTHZ_URL`: agents present `&access=<bearer>`, relay authorizes via the cloud). 6 new protocol tests over live sockets (9 relay total).
  - **Device registry on the agent** (no cloud dependency): `shared/remote.ts` types + pure `verifyHello` (8 new core tests, 119 total) + `packages/host/src/remote.ts` service persisting `remote.json` (enabled/relayUrl/room/secret/devices); 10-minute single-use pairing codes; revoke (live kick + push-token drop), rename, rotate (new room+secret, registry wiped); **auto-reconnect on host boot**. `packages/host/src/relay.ts` rewritten for v2: sealed HELLO handshake gates every connection, un-welcomed requests refused, denied devices kicked, and terminal/changes/tasks/training events now fan out over the relay (the phone's dashboards live-update). Five-touch chain: channels `remote:pair|devices|revoke|rename|rotate`.
  - **Renderer**: Remote access card rebuilt (managed-relay default, device table with connected dots/rename/revoke, expiring pair-QR with countdown, rotate, self-host docs link); web-client relay transport does the HELLO handshake, scrubs credentials from the URL/history, persists a stable deviceId, handles denied (revoked → clear creds → pairing screen with reason); phone **bottom tab bar** (Command Center / Chat / Training / Goals / Settings) replacing the hover rail on small screens.
  - **Cloud**: `POST /api/relay/authorize` (bearer → `{ok, plan, maxDevices}` from entitlements; 401 otherwise; 33 cloud tests). **Passthrough decision**: self-hosting free forever (`ghcr.io/nekko-labs/kotrain-relay`, Docker/Compose/Coolify/Fly documented in docs/REMOTE.md); the managed relay is free during beta and flips to a paid Cloud perk by setting the authz env, no code changes.
  - **Infra**: `apps/relay/Dockerfile` (3-stage, node:22-slim, non-root, healthcheck) + `apps/relay/fly.toml`; GHCR `kotrain-relay` image added to the Docker workflow; **deployed live to Fly.io** (`kotrain-relay.fly.dev`, Nekko Labs org, region nrt, scaled to exactly 1 machine since rooms are per-machine memory).
  - **Verified**: `scripts/itest-relay.mjs` rewritten for v2, 13 checks (transport rejection, refusal before HELLO, unknown-device denial + kick, enrollment, single-use codes, remote code-minting from a paired device, remote revocation with live 4001 kick + re-denial): PASS against a local relay AND **through the live Fly relay**, with a real LM Studio reachable behind it; hands-on web-edition pass on an isolated instance (enable → online chip, pair QR + countdown, phone tab enrolled via link with URL scrubbed + drove the host over the live relay, revoke emptied the list and cut the phone off, mobile viewport shows the bottom nav, rail hidden, no overflow). Build ×8 + typecheck + 119 core / 33 cloud / 9 relay tests green. · Done: 2026-07-22

### Phase 16, Chat UX pass + in-chat pull requests (101)
- [x] **T101**, **Grouped tool activity, empty-response recovery, per-prompt actions, live turn stats, project tags, and in-chat PRs.** Seven pieces of chat-surface feedback. **(1) Grouped activity:** a turn's working steps (reasoning + tool calls + inter-tool narration) now fold into a single collapsible **activity group** ("Worked on 6 steps · read_file, grep") via a pure `toStreamBlocks(messages)` in `ChatPane.tsx`; only the final answer stays a bubble, live and persisted alike. **(2) Empty-response stall (the reported bug):** with some local models a turn ended with `finish_reason:"stop"` and ~1 token, so the loop yielded `done` on a blank message and the chat looked dead. `core/agent/loop.ts` refactored to stream through a helper that accumulates into a `Turn`; an empty turn (no text/reasoning/tools) **retries once with a transient nudge** (appended to the sent messages only, never persisted), and if still empty finalizes with a visible marker instead of silently stopping (2 new loop tests, 130 core total). **(3) Prompt actions:** the single centered **Regenerate** button was removed; each user prompt now reveals a subtle on-hover row, its timestamp, Copy, Edit & resend, and **Reset here** (rewind to that message + re-run, reusing `truncateSession` + `sendChat`). **(4) Live turn stats:** a `TurnStatus` subtext under the conversation shows elapsed · tokens/sec · tokens generated while streaming (accumulating output tokens across the turn's iterations) and a muted last-turn summary when idle. **(5) Project tags:** a chat that references a project shows subtle chips on its sidebar row (supporting workspaces + free-form `tags`) and a project chip on its center tab (`WorkbenchView.tsx`). **(6+7) In-chat PRs:** a chat "has" a PR when a GitHub PR URL appears in its transcript (`gh pr create` output, or pasted). New pure `shared/pr.ts` (`extractPrUrls`/`parsePrUrl`/`collectSessionPrUrls` + `PrInfo`/`PrDiff`/`PrAction` types; 7 new tests) and host `pr.ts`: hydrate live state, diff, and actions via the **`gh` CLI**, falling back to the **GitHub REST API** with the connector PAT when `gh` is absent (15s cache; sticky `ghMissing` probe). Three new IPC channels (`pr:sessionList|diff|action`) through the full chain (ipc/host/dispatch/preload/web-client). Renderer `PrCard.tsx`: an inline **PR card** (open → Approve green / Decline faded-red / Review blue + a subtle Merge, each behind a one-click confirm; merged → purple star-dusted "successfully merged 🎉" banner), a **`PrPane`** diff side-pane (new `WbPane` kind `'pr'`, Devin-style per-file syntax-tinted diff), and **badges** on the sidebar row + chat header; store gains `prsBySession` + `refreshSessionPrs` + `openPrPane`, ChatPane refreshes on open/turn-end, and WorkbenchView back-fills badges for chats that reference a PR (bounded to 8). All actions are user-initiated. Verified over an isolated seeded web edition: the 5-tool run collapsed to one "Worked on 5 steps" group, the prompt hover row showed timestamp + Reset here (no Regenerate), sidebar/header PR badges + project tags rendered, the **open #84 card fetched live via `gh`** (draft, +6963/-5130, checks passing, all four buttons) and the **merged #95 card** showed the merged banner, and **Review opened the real 40-file diff** in a side pane, zero console errors. Typecheck ×8 + 130 core tests + desktop/web build green. · Done: 2026-07-25
- [x] **T102**, **Command Center rework: monitor strip, Now/Automations/Sessions row board, segmented Insights, global GPU HUD.** Feedback: the page "looked very generated", stat tiles with colored left borders, ten equal-weight card sections, mostly-empty kanban lanes, and the automations board buried at the bottom. Rebuilt `CommandCenterView.tsx` as a monitor-style operations board (see SPEC "Command Center dashboard"): a one-line **monitor strip** of vitals replaces the stat-tile grid; the kanban lanes become three row groups, **Now** (in-flight runs with a real **elapsed working timer**, first-sighting timestamps kept in a `runStarts` ref keyed by session id, plus queue, swarm tree, Stop/Open), **Automations** (schedule rows with a live **"next in Xm" countdown** off `nextRunAt`, a 30s re-render tick, ranked running → next-up → paused → error → done, hover actions), and **Sessions** (compact rows, Show-all past 10); Terminals as rows; Fleet folded into the Now header as icon×count chips; Optimize/Cost/Usage/Services fold into one segmented **Insights** section (Cost's three stat tiles flattened into the monitor-strip idiom). New `GpuHud.tsx` mounted in `App.tsx`: a bottom-right always-on GPU pill (util % + VRAM micro-bar, hover for per-GPU breakdown) visible on **every tab**, replacing the page's GPU & VRAM section (`VramPanel` deleted from `GpuStats.tsx`; mascot keeps bottom-left). Verified over the web edition: monitor strip, all four sections, Insights tabs, and the HUD render on Command Center + Training tabs with zero console errors; desktop typecheck + build green; impeccable design detector clean. · Done: 2026-07-25

- [x] **T103**, **Composer strip + model picker + prompt-analyzer feedback (rides PR #99).** Three pieces of feedback on the consolidated composer strip. **(1) No more improper wrapping:** the instrument strip is now a single non-wrapping row; the model chip is the designated flexible member (`min-w-0` + truncate, capped at 240px) so a long local-model name (e.g. `qwen/qwen3.6-27b · LM Studio`) ellipsizes instead of shoving the model/thinking/effort cluster onto a second line (`ChatPane.tsx` strip row, `ChatControls.tsx` goes `shrink-0`). **(2) Flat grouped model picker:** the nested provider `<select>` is gone; the popover now fetches every configured provider's models on open and renders one flat list grouped by provider, with a **★ Starred** section pinned on top (rows there carry a provider sub-label), a `StarIcon` toggle on every row persisting to the shared `favoriteModels` setting (same key format + `refreshSettings` flow as the Models page), a filter box past 8 models, ✨ Auto kept on top, and **implicit provider switching** (picking a model from another provider calls `onProvider` + `onModel` in one click; the existing model-list effect keeps the selection since it's in the new provider's list). **(3) Click-to-fill part chips:** new pure `suggestPartFill(partId, text)` in `promptAnalysis.ts` returns a starter snippet + placement matched to the prompt's rough domain (code / writing / data / general, question vs. task) via instant offline text matching, each snippet worded to satisfy that part's detector; `PromptAnalyzer` renders missing parts as dashed clickable chips (hover accent, tooltip previews the exact snippet) feeding a new `onFill` prop, and `ChatPane` prepends role / appends the rest to the draft and refocuses the composer. Model-generated fills deferred (no one-shot completion IPC exists yet). Verified over the web edition against a live LM Studio + Anthropic setup: single-row strip at narrow widths with the qwen name truncated, starring qwen pinned it live with the menu open, picking Claude Sonnet 4.6 from the LM Studio provider's chat switched provider (Offline chip correctly disabled for cloud), and clicking + Role / + Task on a 401-bug prompt inserted the engineer role on top + "Fix the issue…" below with both chips flipping to ✓. Typecheck + full build green. · Done: 2026-07-26

### Phase 17, Composer drafts + discoverable shortcuts (106)
- [x] **T106**, **Composer drafts survive anything, chats open ready to type, and the new-agent / new-terminal shortcuts are discoverable.** Three pieces of workbench feedback. **(1) Saved drafts:** the workbench only mounts the pane you're looking at, so switching tabs, leaving the Chat view, or quitting threw away whatever was half-typed. New `renderer/composerDrafts.ts` parks `{ text, images, savedAt }` per session in `localStorage` (the pattern the store already uses for the Files pane), and `ChatPane` seeds both `draft` and `pendingImages` from it via lazy `useState` initializers. Writes are debounced 400ms, with a flush on unmount **and** on `beforeunload` so the last keystrokes can't die inside the debounce window; sending or queueing clears the entry. Images are data URLs, so the store carries a ~4MB budget: on overflow it evicts other chats' least-recently-touched drafts first and, as a last resort for a lone oversized draft, keeps the text and drops the images (losing what you typed is the worse failure). Corrupt JSON degrades to "no draft". A `draftLoadedFor` ref guards against the component ever being reused for another chat, which would otherwise write one chat's words into the next. **(2) Focus on open:** `ChatPane` focuses the composer once per chat and puts the caret after any restored text, skipping the grab when you're already typing in another input. Keyed on `providers.length` too, because the textarea is disabled until providers load. Deliberately synchronous rather than in a `requestAnimationFrame`, which never fires while the window isn't compositing (found in verification: the callback was silently dropped, and an effect re-run cancelled it for good). **(3) Shortcuts:** chords and their printed labels now live together in `renderer/shortcuts.ts` (⌘ on macOS, Ctrl elsewhere) so they can't drift. New agent stays **Mod+N** (Claude Desktop / ChatGPT desktop / Cursor all use it for New Chat); new terminal becomes **Mod+`**, the terminal key from VS Code, Cursor, and Zed, with Mod+Shift+` (VS Code's literal "new terminal", arrives as `~`) and the previous Mod+J both kept as aliases. Hints surface as a new `.kbd` chip in the workbench **+** menu (beside "New agent" and on the Terminal section header), on the empty-state buttons, in tab-strip tooltips, and in the command palette, which also gained a "New terminal" command. Verified over the web edition: Ctrl+N opens a chat with the composer focused, Ctrl+` spawns terminals, a typed draft plus a pasted image round-trip through a tab switch **and** a full reload, Queue clears the text, and all four kbd chips measure inside their parents. Desktop typecheck + build green; `composerDrafts.test.ts` (7 cases: round-trip, per-chat isolation, empty-clears, images-only, oldest-first eviction, text-kept-over-images, corrupt JSON) wired into `npm test` via a new `test` script on `@kotrain/desktop`. · Done: 2026-07-26

### Phase 18, Agent step budget + resource monitors (107)
- [x] **T107**, **Step budget that wraps up instead of failing, "turn" > "reply"/"iteration" wording, and CPU/memory/GPU/VRAM monitors with per-monitor polling.** Four pieces of feedback from a long exploration that died on `Reached max tool iterations`.
  - **(1) The reported bug, lost replies.** `runAgent` capped tool round trips at **12** and, on reaching it, yielded an `error`, so a normal multi-file exploration threw away everything it had done. The default is now `DEFAULT_MAX_STEPS = 80` (shared, user-overridable 5-400 via `AppSettings.maxSteps` + `clampMaxSteps`, wired from `host/chat.ts`), and reaching the budget takes **one final pass with the tools withheld** (`stream(turn, extraMessages, sendTools)` gained a tools override; a transient wrap-up nudge is sent but never persisted) so the reply lands as a real answer plus an honest note naming the limit. New **Settings > Agent loop** section (`AgentLoopSection`, committed on blur/Enter so a half-typed number never becomes the setting). 2 new loop tests (130 core total) cover the wrap-up answer, the note, `tools: []` on the wrap-up round, and the nudge staying out of the transcript.
  - **(2) Wording.** A chat exchange is a **reply** and one cycle of a long-running run is an **iteration**; "turn" is gone from the UI. The failure banner reads **"Reply failed"**, the footer **"Last reply"** (`TurnStatus` > `ReplyStatus`), composer/queue/reasoning/context-panel/spec copy reworded, RunBoard + Goals read "Iterations" / "Iteration budget", and the run log says "Iteration failed". Persisted field names (`run.turns`, `maxTurns`, `maxHistoryTurns`) are untouched, wording only.
  - **(3+4) Resource monitors.** New shared `monitor.ts` (`MonitorKind`, labels/hints, `DEFAULT_MONITORS`, `resolveMonitors`, `monitorSources`, `SystemStats`) + host `system.ts` (CPU load as a delta between `os.cpus()` snapshots, re-primed after a >30s gap so a re-enabled monitor doesn't report a since-boot average; RAM from `os.totalmem/freemem`; no native module) behind a new `system:stats` channel through the full chain (ipc/host/dispatch/preload/web-client). `GpuStats.tsx` + `GpuHud.tsx` are replaced by one `ResourceMonitor.tsx`: a module-level **shared sampler** (one 4s poll feeding every surface, `useSyncExternalStore`), `ResourceHud` (the bottom-right chip, now CPU/RAM/GPU/VRAM, with **on/off switches per monitor** on hover) and `ResourceDock` (the chat context panel's full monitoring section: CPU + cores + processor, memory %+GB, GPU util, VRAM %+GB, per-device used/free). **Off means not sampled**: the sampler polls only the probes the enabled monitors need, so GPU/VRAM off spawns no `nvidia-smi`, CPU/memory off samples no CPU times, and all-off polls nothing (chip reads "Monitors off"). The chip and the section no longer **overlap**: the section publishes its rect (`store.monitorDockRect`, no-op writes skipped) and the chip **warps into it** (`.monitor-chip` transition + `.monitor-absorb` glow), measured from `offsetLeft/offsetTop` rather than `getBoundingClientRect` so a mid-flight transform can't compound the offset; `visibility` rides the transition so the chip leaves the tab order once it lands. Dead `VramInline` removed.
  - **Verified** end to end over an isolated web edition (own port + throwaway data dir) against this machine's real hardware: chip showed live CPU/RAM/GPU/VRAM, the section rendered `32 cores, AMD Ryzen 9 9950X3D` / `33 / 47 GB` / `RTX 5090, 7.4 GB used, 24 GB free`, the warp landed dead-centre on the section (chip centre [1119,539] vs section centre [1120,539]) and returned to rest on close, and network counts confirmed the polling contract: all on > `system:stats` + `gpu:stats` once per tick, GPU+VRAM off > `system:stats` only, all off > **zero** probes, re-enabled > both resume. Typecheck x8 + 130 core tests + desktop/web build green. · Done: 2026-07-26

### Phase 19, Skills in the + menu + the orbit app icon (108)
- [x] **T108**, **Skills reachable from the + menu, and a new app icon (the orbit mark).** The two items from the chat that died on the old step cap.
  - **+ menu skills.** The composer's **+** menu was Photo / File / Folder only; skills existed but you had to know to type `/`. It now has a second page: **Skill** (with a count) opens a list of every skill the chat can run, built-ins **plus** installed marketplace skills via `matchSkills('', installedSkillDefs)` (the same source and ordering `/` uses, so the two menus can't disagree), each with its ★ flag, `/name` and description, and an **Add skill** row that jumps to the Skills tab. Picking one calls the existing `armSkill`, so the composer gets the usual skill chip. Menu state is a small `attachMenuPage` union; closing always resets to the first page, Escape steps back a page before closing and returns focus to the + button, and the trigger gained `aria-haspopup`/`aria-expanded` plus a label that names what it now does.
  - **App icon.** The shipped icon was still the pre-rebrand orange pixel-cat, and the NSIS installer art still said **"Open Paw"**. Replaced with an abstract **orbit mark** (deliberately not the mascot: at 16px a cat is a smudge). New `apps/desktop/scripts/make-icons.cjs` holds the artwork as vector and rasterizes it with Electron (already a devDependency, so no image toolchain): `icon.svg`, `icon.png` (512), `icon.ico` with real 16/24/32/48/64/128/256 entries, the PWA/apple-touch `icon-512.png`, and both installer BMPs (24-bit, written by hand since NSIS reads nothing else). Two traps handled: `capturePage` honours the display scale factor (a 125% monitor was producing a 640px "512" icon and an .ico whose directory lied about its entry sizes), and Windows won't open a 16px window, so every asset is drawn at >=256px and downsampled to its exact target, which supersamples the curve as a bonus. The comet's tail is one tapered ribbon of opaque bands rather than stroked segments (translucent round caps double-composite into visible beading) and its fade follows arc length rather than a straight gradient axis (the trail wraps 300 degrees, so its tail lands next to its head and a linear gradient collapsed to a sliver of violet).
  - **Note on nano banana.** The ask was to generate the icon with nano banana (`gemini-2.5-flash-image`). The supplied AI Studio key is valid (it lists 56 models) but every image model returns `429 limit: 0` on the free tier, so image generation needs billing enabled on that project. The generation script is written and ready; the shipped icon is the vector one.
  - **Verified** over an isolated web edition: the + menu shows `Skill 11`, the page lists ★ /goal first through /pr, picking `/review` closes the menu and arms the chip ("Runs on send · shown in context"), **Add skill** lands on the Skills tab, and Escape steps back rather than closing. Icon assets checked entry by entry (declared size == actual for all seven .ico entries) and eyeballed as a contact sheet from 16 to 256. Typecheck x8 + 130 core / 7 desktop tests + desktop/web build green. · Done: 2026-07-26

### Phase 20, Chat surface polish, markdown both ways + a legible instrument strip (109)
- [x] **T109**, **Eight pieces of chat-surface feedback: markdown in your own messages, a two-row instrument strip, live draft accounting, and right-clickable images.**
  - **(1) Hover bleed in Command Center.** `PanelList` is a `.card` (16px radius) with `divide-y` rows, but the rows' `hover:bg-surface-2` fill painted square into the rounded top/bottom corners. Added `overflow-hidden` to the list so rows are clipped to the card's radius. Verified by hit-testing rather than by eye: `elementFromPoint` two pixels inside the card's top-left bounding-box corner now lands on the enclosing `<section>` (clipped away) while the straight mid-edge still lands on the row `<button>`.
  - **(2) Markdown on both sides.** User messages rendered as `whitespace-pre-wrap` text, so a typed dash list stayed dashes. They now go through `Markdown` too. The renderer was also rebuilt from "split on blank lines, classify the whole block" to a **line scanner**, because the old version only made a list when *every* line of a block was a bullet, exactly the wrong rule for how people type ("kotrain project." followed by dashes with no blank line). It now handles bullet/numbered runs **nested by indentation**, a bullet↔number switch at the same level starting a new list, headings, block quotes, rules, pipe tables, fenced code, and inline bold / *italic* / ~~strike~~ / code / links / bare-URL autolinking, with single newlines kept as `<br>`. Italics are stars-only and both delimiters must hug their text, so `some_long_name` and `2 * 3 * 4` don't become emphasis. New `Markdown.test.tsx` (11 cases) pins all of it, including fenced code not being parsed as markdown.
  - **(3) Context that moves as you type.** The composer gauge and the Context Inspector both only counted *sent* messages. `ContextGauge` gained a `draftTokens` prop, and the pane mirrors its draft into a new `store.draftBySession` (undebounced, so the right-hand panel and the gauge move in the same frame) which the Inspector reads. Both surfaces show the draft as a renderer-only **"Your message"** provenance source (accent-colored, in `SOURCE_META`), and the panel's caption switched from "updates every reply" to "updates as you type" / "includes what you're typing".
  - **(4) Attachments inside the composer.** Pending image thumbnails were `absolute bottom-full` above the composer, i.e. floating over the instrument strip. They now render as the first child *inside* the `.composer` card, a horizontally scrolling thumbnail strip with a `border-b` hairline above the textarea, thumbnails 64px with the existing hover ✕.
  - **(5+6+7) The instrument strip: two rows, three shapes.** Effort wrapped because its wrapper `div` could shrink (only the inner button had `shrink-0`), and a single row of seven identical `.chip` pills gave menus, toggles, and actions the same weight. The strip is now **row one, how this agent runs** (Mode, Tools left; a hairline divider then Offline, Incognito pushed right) and **row two, which brain it uses** (model, Thinking, Effort left; Automate right). Three new control families in `styles.css` replace `.chip` here: `.ctl-menu` (bordered transparent field, muted label, caret) for value pickers, `.ctl-toggle` (no background until hover; state carried by color plus a `.ctl-dot` that lights up) for switches, and plain icon buttons for actions. Offline lights accent, Incognito uses `.ctl-toggle-neutral` (plain ink) so the strip has exactly one accent state, and the passive "streamed reasoning" indicator uses `.ctl-toggle-on` since a `<span>` can't carry `aria-pressed`.
  - **(8) Right-click a chat image.** New `ImageMenu`, portalled to `body` at the pointer (flipped when it would run off an edge), with **Copy image** and **Save image…**, wired to composer thumbnails, sent-message thumbnails, and the full-size lightbox. Two traps found in verification, both fixed: the renderer's CSP has no `data:` in `connect-src`, so `fetch(dataURL)` fails with "Failed to fetch" (the data URL is now decoded by hand into a Blob, and the PNG re-encode draws straight from the source URL, no object URL); and closing the menu on any document `mousedown` unmounted the item before its click could fire, since a portalled menu's press reaches the document listener anyway (now tested for containment against the menu element). Escape is captured on `window`, one step ahead of the lightbox's document-capture handler, so the first Escape closes the menu and the second closes the preview.
  - **Verified** over an isolated web edition (own port 1499 + throwaway data dir, seeded with a markdown-heavy transcript): the user bubble renders `<p>` + nested `<ul>`(disc)/`<ul>`(circle) + `<br>`, the assistant bubble exercises em/strong/s/code/a/ol/ul/blockquote/hr/table/pre; typing 142 characters moved the gauge 424 → 460 tok and added "Your message 36 tok" to the panel's breakdown; the attachment strip measures 517-602 with the textarea starting at 602 and the control rows ending at 478 (no overlap); the strip holds two rows with no wrap and no overflow at 1280, 900, and 640 wide; probes of each control state confirm transparent backgrounds at rest with accent (#6d5efc) for Offline-on, plain ink for Incognito-on, and lit dots; the image menu opens over the lightbox (z 60 vs 50), reaches `navigator.clipboard.write` with a 77-byte `image/png` ClipboardItem (the write itself only fails on `Document is not focused`, since the verification pane isn't focused; permission is `granted`), and Save produces a `blob:` href named `kotrain-image.png`. Full typecheck x8 + 130 core / **18** desktop / 33 cloud tests + desktop/web build green. · Done: 2026-07-27

### Phase 21, The chat's brain sticks, the model menu stays in the pane, the Spec panel follows the folders (110)
- [x] **T110**, **Three workbench bugs: a chat that couldn't send after a tab switch, a model menu hanging outside its pane, and a Spec panel showing the wrong project.**
  - **(1) The reported bug, sending did nothing after switching tabs.** The model picker's choice lived only in `ChatPane`'s local state (`onModel` persisted `autoModel` and nothing else). The workbench mounts only the pane you're looking at, so a tab switch tore the pane down and the rebuilt one re-derived provider/model from `session.providerId`/`modelId`, which are only written when a reply is actually sent. Pick LM Studio in a fresh chat, switch tabs, and you came back on the chat's old provider, and if that provider exposes no models (a local server that isn't running) `modelId` resolved to `null`, the chip read **"No model"**, and `send()` hit a bare `if (!useModel) return;`: a lit send button that did nothing, no toast, no log. Two fixes. `onModel` now parks the pick on the chat itself (`setSessionOptions` gained `providerId` + `modelId` through the whole chain: `shared/ipc.ts`, `host.ts`, `sessions.ts`), so provider and model survive a tab switch, a closed pane, and a restart. And the silent guards are gone: a new `requireBrain(text)` returns `{ providerId, modelId }` or explains what's missing, "Add a model provider in Models first" with no providers, "still loading its model" during the async window right after a mount, and "No models available from `<provider>`. Start it, or pick another model below the chat." when the provider lists none. Used by `send`, `/goal`, and edit-and-resend (which had the same silent return), and it keeps the draft and attachments intact so nothing is lost to a failed send.
  - **(2) The model menu hung outside the pane.** The picker's panel was `absolute bottom-8 right-0` at `w-80` (320px) under a `max-w-[240px]` chip that sits at the **left** edge of its row, so right-anchoring pushed ~80px of menu past the pane's left edge, over the sidebar. It now opens `bottom-full left-0` (the file's own convention for the `@`/`/`/`+` menus, and no magic 32px offset) with `max-w-[calc(100vw-2rem)]`, so it grows rightwards into the pane it belongs to.
  - **(3) The Spec panel didn't follow the folder you picked.** `SpecPanel` reset its workspace selection only when the *set* of the chat's folders changed, keeping the current pick whenever it was still a member. Making a different folder primary in the Folders section directly above (the pane's own affordance for switching project) left the panel on the old project: it read "Primary · kotrain" while the row above said mynichi was primary, and Build/Update wrote to the wrong repo. It now snaps to `session.workspaceId` whenever the primary (or the folder set) changes; its own select still overrides, until the choice above moves again.
  - **Verified** over the web edition against a throwaway scratch chat in an isolated data dir, then cleaned up (chat deleted, temp provider removed): the Spec panel showed `…\kotrain\spec.md` + `tasks.md`, clicking the **mynichi** folder row flipped the badges *and* moved the panel to `…\mynichi\spec.md` + `tasks.md` in the same beat, and picking "Supporting · kotrain" from the select overrode it back. With the chat pinned to a provider exposing no models, the chip read "No model · Scratch (dead)" and clicking send produced **"No models available from Scratch (dead). Start it, or pick another model below the chat."** with the typed draft still in the box (previously: nothing at all). Picking `google/gemma-4-12b` then opening a second tab and switching back kept the chip on that model, and `session:get` confirmed `providerId: lmstudio-x` / `modelId: google/gemma-4-12b` on disk. Menu geometry measured open: card `left 416 → right 736` inside a pane whose content starts at 416 (previously `left 297`, i.e. 119px into the sidebar), bottom 8px clear of the chip. Full typecheck x8 + 18 desktop tests + web build green, no console errors. · Done: 2026-07-28

### Phase 22, Auto profiles + a real markdown/editing pane + the folders-first right panel (111)
- [x] **T111**, **Eight pieces of chat + models feedback: Auto that says what it will do, markdown that reads like a document, a file pane you can actually edit in, and a folders-first right panel.**
  - **(1) A chat with no model asks for one.** The store's `selectProvider` auto-picked `models[0]` whenever the current model wasn't in a provider's list, so "no model selected" never happened, and the guess was often wrong: LM Studio serves speech and embedding models from the same endpoint, and its first entry here is `whisper-large-v3`. Verification caught exactly that (a fresh chat sitting on Whisper). It now leaves `activeModelId` unset instead, and `ChatPane` stops falling back to `m[0]` too, so a chat without a model **says so** in three places: the empty state ("Pick a model to get started" + a button that opens the picker), a nudge above the composer, and the model chip itself reading "Choose a model" in accent. `requireBrain` also opens the picker rather than only toasting. The picker's open state moved up into the pane so all three affordances drive the same menu.
  - **(2) Auto profiles + a visible pick.** `pickAutoModel` replaces `recommendModel` as the primary API (the old function stays as a thin id-only wrapper) and returns the **reasoning** with the choice: `{ modelId, name, tier, complex, reason }`. Three per-chat profiles (`session.autoQuality`, new through the `setSessionOptions` chain): **Cheap** always takes the smallest capable model, **Quality** always the strongest, **Normal** reads the prompt (the previous behavior). Cheap and Quality deliberately ignore the prompt, a spend instruction that silently changes its mind is worse than one that doesn't. New `isChatModel` filters candidates so Auto never routes a reply to Whisper, an embedding model, a reranker, OCR, or an image model (falling back to the raw list only if nothing survives). The strip gained an **Auto** profile chip beside the model chip, shown only while Auto is selected, and the "→ <model>" preview now renders with an empty draft too, titled with the reason. 6 new shared tests (14 in `model-select.test.ts`).
  - **(3) Markdown that reads like a document.** `.md` files did render through `Markdown`, but that renderer is tuned for chat bubbles: headings were `<div class="font-semibold">` barely louder than body text, every hard-wrapped line became a `<br>`, and embedded HTML (`<div align="center">`, `<table>`, `<img>`) was dumped as literal text, which is why a README read as a wall of plain text. New opt-in **`doc` mode**: real `h1`–`h6` with a size scale, reflowed paragraphs, roomier lists/tables/quotes, task-list checkboxes, HTML comments hidden, block-level HTML reduced to its text (erased, never mounted, markdown from anywhere is untrusted), and `<img>` folded into the same handling as `![]()`. Images and relative links become chips/buttons that **open the real file** via a new `basePath` prop, because the renderer's CSP allows only same-origin and `data:` images so a remote or on-disk one can't be shown inline. The link matcher accepts any path-or-anchor target but still refuses to read `rows[0](x)` as a link. Chat mode is byte-identical; 8 new cases (19 in `Markdown.test.tsx`).
  - **(4) The file pane edits like an editor.** Save was manual-only and a controlled textarea has no working native undo. Added **auto-save** (debounced on an idle pause, remembered in `localStorage`, flushed on unmount and `beforeunload`, off falls back to the Save button), an **own undo/redo history** (Ctrl+Z / Ctrl+Shift+Z / Ctrl+Y, snapshots carry the selection, a typing run inside 600ms coalesces into one step, capped at 200), **copy / cut / paste** toolbar actions (copy falls back to the whole file with no selection), Tab-to-indent, save-on-blur, and a save-state readout (Saving shortly → Saved) replacing the bare dirty dot.
  - **(5+6) Folders first, then context, with a resizable split.** The right panel's Folders section was a list of buttons and the file tree lived in a separate dockable **Files** side-pane, one pane over from the folder list it duplicated. The tree moved **into** the Folders section: each folder is an accordion whose row still carries primary/supporting/exclude/remove and whose body is its lazy `DirTree`. Below it, the **Context** section, split by a draggable divider, VS Code style: with every tree collapsed Folders is content-height and Context owns the panel; opening one gives Folders the dragged height, capped so Context keeps a usable minimum. The asked-for height is kept unclamped and only capped for display, so freeing space returns it rather than stranding the split where a cramped panel left it. `FilesSidePane` and the store's whole `filesPane*` slice are gone.
  - **(7) The panel belongs to the workbench, not to a chat pane.** Found during verification: with `ContextInspector` rendered inside `ChatPane`, clicking a file in the new explorer opened the file pane and **took the explorer with it**, since the chat pane was no longer active. It now renders in `WorkbenchView` beside `<main>`, following `activeSessionId` (it already had an empty state for "no chat"), and the workbench sidebar's panel button, freed up by the Files pane's removal, toggles it. The **Resources** dock is collapsible for the same space reason, keeping CPU/VRAM in its header when closed, and it publishes its rect either way so the panel re-measures the split when it opens or closes.
  - **(8) Models page.** **Stop server** now renders only for a local provider whose connection actually tested `ok` (it was always rendered and merely disabled when offline, a greyed button inviting a click that can't work). The **load** chip gained a real hover state (new `.chip-action`: accent tint + inset ring), and a **loaded** chip now reads "loaded" at rest and "unload" under the cursor with a danger fill, so one chip carries the state and the action.
  - **Verified** over the web edition against this machine's real LM Studio + Anthropic providers: all three Auto profiles resolve live and differently (Cheap/Normal `qwen/qwen3.6-27b`, Quality `nvidia/nemotron-3-nano-omni`, Normal on "Refactor and debug the distributed lock" moving up to Nemotron) after the non-chat filter took Whisper and `unlimited-ocr` out of contention; clearing the default model (then restoring it) produced all three "choose a model" affordances and the CTA opened the picker; `README.md` rendered `H1 🐾 Kotrain / H2 Overview / H2 Download / H3 macOS…` with four image chips and **zero** leftover HTML; a scratch file typed into showed "Saving shortly" → "Saved" and the text **on disk**, with undo restoring the previous content and redo putting it back; the split measured 143/176 with the dock open, 260/379 with it collapsed, and restored on reopen; opening a file from the explorer kept the panel mounted. One harness limitation worth recording: the preview pane runs `document.hidden === true`, so it fires **no** `requestAnimationFrame` and **no** `ResizeObserver` callbacks, hence the dock's rect doubling as an explicit dependency of the measuring effect. Full typecheck x8 + 136 core / 26 desktop tests + web build green, no console errors. · Done: 2026-07-28

### Phase 29, Marketing site: stop pinning stale CSS and JS (120)
- [x] **T120**, **The site's own CSS and JS were cached `immutable` for a year, so shipped changes never reached returning visitors.** `vercel.json` sent `Cache-Control: public, max-age=31536000, immutable` for `css|js|png|jpg|jpeg|svg|webp|ico|woff2`. That header is correct for content-hashed bundles (hypergate's site is a Vite build, so its `index-DXJ-KgGb.js` genuinely never changes), but this site is hand-written: `styles.css` and `site.js` keep their names forever. Caught while verifying T119 on the live site: the deployed `styles.css` contained the new tokens (`curl` confirmed) while the browser rendered the **old** button styling from a `transferSize: 0` disk-cache hit, i.e. new `index.html` against a year-old stylesheet, a mix that can look arbitrarily broken.
  - **Split the header rule**: images and fonts keep `immutable`; `css|js` now get `public, max-age=0, must-revalidate`, so each load revalidates against the ETag and a 304 costs nothing on a 15 KB file.
  - **One-time cache-buster**: a header change can't release copies already pinned, since a pinned response is never revalidated. `index.html` (which was never `immutable`) now references `styles.css?v=2` and `site.js?v=2`, which is a new URL to every cache. With `must-revalidate` in place, future edits need no further bump.
  - **Verified**: `vercel.json` parses; both assets return the new header on the deployed site and revalidate rather than replay from disk; a browser holding the pinned pre-fix copies renders T118 + T119 correctly after the `?v=2` reference lands. · [spec](SPEC.md#distribution--platforms) · Done: 2026-08-03

### Phase 28, Marketing site: one button shape, shared with hypergate (119)
- [x] **T119**, **The hero CTA pair now wears hypergate.app's button style, dropdown intact.** The two hero buttons disagreed on shape: T118's split download button kept the old 14px corners at 68px tall (two lines of text) while the new GitHub pill was `border-radius: 99px`, so a rounded rectangle sat next to a lozenge, and the caret's own `background: var(--grad)` restarted the gradient at the seam, leaving a visibly lighter cyan block on the right end.
  - **One pill shape site-wide, copied from `hypergate/apps/site`.** `.btn` is now `display: inline-flex`, `padding: 13px 26px`, `border-radius: 999px`, 16px/600, with hover lift (`translateY(-1px)`) and a cyan glow; `.btn-primary` paints the gradient with **dark ink** (new `--on-grad: #04060e`) the way hypergate does; `.btn-ghost` is a bare hairline-bordered pill that tints violet on hover. The now-unused `.btn-glass` block (dead since T118 replaced the hero's glass button) is gone, and the hero's GitHub link picks up `.btn` instead of restating padding and radius of its own.
  - **The picker became one `.btn`-shaped pill.** The gradient and the drop shadow moved from the two children onto `.dl-pick`, so it runs unbroken from the label through the caret; `.dl-main` and `.dl-caret` are transparent, share the dark ink, and only round their outer ends (`999px 0 0 999px` / `0 999px 999px 0`), each tinting on hover instead of brightening the whole pill. The hover lift is suppressed while the menu is open so the panel doesn't bob with it.
  - **Label is one line with a version chip.** `.dl-main-s` ("`.exe · x64 installer · v0.1.5`", the second line that made the button 68px tall) is replaced by hypergate's `<small>` chip: mono 11px, `padding-left: 10px`, `border-left: 1px solid rgba(4, 6, 14, 0.3)`, carrying just `v0.1.5` and `display: none` while empty (offline or rate-limited). The format and architecture aren't lost: they became the link's `title` and the first half of the caption under the download-section button.
  - **Dropdown untouched in behaviour**, only in trim: same caret button, same `aria-expanded`/`aria-controls`, same arrow-key menu, same flip-up placement, same "yours" chip. Mobile keeps the full-width pill, with the label centred against the caret that sits inside its right end.
  - **Verified** in real Chrome: hero pills both 53px tall, both `999px`, 14px apart, same top; nav CTA and download-section button pick up the same shape; version chip reads `v0.1.5` from the live release API and the tooltip reads `.exe · x64 installer`; caret opens/closes, opening one menu closes the other, an outside click closes both; 390px layout stacks two equal 338px pills; no console errors. · [spec](SPEC.md#distribution--platforms) · Done: 2026-08-03

### Phase 27, Marketing site: the hero downloads, the menu behaves, the shell gets a one-liner (118)
- [x] **T118**, **The hero button downloads, the dropdown stops colliding with the page, and terminal people get a copyable one-liner.** Four changes to `apps/website`, all in the download path.
  - **The hero CTA is the real download button.** It was an anchor to `#download`, so the first click only scrolled you to a second button. The smart picker markup (primary + caret + menu) is now duplicated in the hero and driven by the same code, so a hero click starts the actual `.exe`/`.dmg`/`.AppImage` download for the detected machine, subtitle and version included. `site.js`'s single-instance globals (`dlPick`/`dlMain`/`dlMainTitle`/…) became `setupPick(root)` over `[data-dl-pick]`, one closure per instance, with `closeOtherMenus()` so opening one closes the other. Ids stay only where ARIA needs them (`aria-controls`, `aria-labelledby`); everything else moved to data attributes. Without JS both instances are still plain links to `/releases/latest`.
  - **"Star on GitHub" became a quiet outline pill.** New `.btn-ghost`: fully rounded, hairline border, near-transparent fill, trailing `↗`, reading "View GitHub repo". `.cta-row` got `align-items: stretch` so the pill matches the split button's height instead of floating short beside it.
  - **The dropdown was painting under the footer.** `main`/`header`/`footer` all shared `z-index: 1`, and `main` creates a stacking context, so the menu (`z-index: 20` inside `.dl-pick`) could never rise above the later-in-DOM footer: footer text ran straight across the menu items. `main` is now `z-index: 2`, `.dl-pick` trades `isolation: isolate` for `z-index: 30` (isolation trapped the menu below any transformed sibling that followed it), the panel background went near-opaque (`rgba(3,11,24,0.97)`) with a stronger shadow, and the menu **flips above the button** when the room below is smaller than the menu and worse than the room above, capping `max-height` to whatever side it lands on and re-placing on scroll/resize. Also: `min-width: 100%` so the panel is never narrower than the button it hangs off, no dead top padding on the first group label, and the "yours" marker on your build is a cyan outline chip instead of `' ·  yours'` glued onto the hint text. The pop-in animates **transform only**: it used to fade from `opacity: 0`, which leaves the panel invisible wherever animations never advance (hidden tabs, embedded webviews, the same throttling the scroll reveals already work around).
  - **One-liner for people who'd rather stay in a shell.** Under the button, a mono command box with a `❯` prompt and a Copy button on its right (stacked underneath at ≤560px): PowerShell `iwr <asset> -OutFile kotrain-setup.exe; .\kotrain-setup.exe` on Windows, `curl -L <asset> -o Kotrain.dmg && open Kotrain.dmg` on macOS, `curl -L … && chmod +x … && ./Kotrain.AppImage` on Linux. It uses the exact asset URL the button resolved, so it is hidden until the Releases API answers rather than shipping a guessed URL, and hidden entirely for phones and unknown platforms. The command wraps instead of scrolling (nobody should paste what they can't read), and Copy uses `navigator.clipboard` with an `execCommand` fallback and a text-selection last resort, flipping to a green "Copied" for 1.9s.
  - **Verified** in real Chrome against the static server: two pickers both resolving to the live v0.1.5 `-x64.exe` asset URL; menu opaque and flipping up in both hero and download section with no footer bleed; a real click on Copy putting the full 138-character command on the clipboard and the button going to "Copied"; the command's download half run for real in PowerShell (82.7 MB, `MZ` header, valid PE); 390px-wide layout stacking the pill under the button and Copy under the command; no console errors. · [spec](SPEC.md#distribution--platforms) · Done: 2026-08-03

### Phase 27, Border beam (118)
- [x] **T118**, **Border-beam animation, in brand colors.** The border-beam pattern (a beam of light lapping an element's border, from Jakub Antalik's Border Beam UI demo on recent.design), recolored violet→cyan to match the theme instead of the reference's rainbow. Two homes: the **chat composer** gains `.composer-beam` while a turn is streaming (a conic-gradient ring rotating via a registered `--beam-angle` custom property, masked to a 1.5px border with the content-box XOR trick, plus a faint `beam-wash` gradient drifting across the surface, background-position so it needs no registration); and the **marketing site**, where the hero window mock gets a slow quiet lap and the download pill a brighter one that speeds up on hover and hides while its menu is open. Both degrade to a static gradient ring without `@property` support and freeze under `prefers-reduced-motion`. Evidence in `docs/screenshots/border-beam-*`. · Done: 2026-08-05

### Phase 26, Back to Kotrain, and the mascot gets its own name (116–117)
- [x] **T116**, **Rebrand: Nekkos → Kotrain (kotrain.com).** The T113 rename lasted a day; the product is **Kotrain** again, so this reverses it by the same scripted sweep rather than by `git revert` (T114–T115 built on top of T113 and are being kept). Ordered rules, mascot and domain first so the blanket pass can't clobber them: `@nekkos/*` → `@kotrain/*`, product name **Nekkos** → **Kotrain**, appId `dev.nekkolabs.nekkos` → `dev.nekkolabs.kotrain`, env `KOTRAIN_*` primary, `nekkos.app`/`nekkos.dev` → **kotrain.com** (lowercase only, so the macOS bundle `Nekkos.app` correctly became `Kotrain.app` instead of a URL), MCP tools `nekkos_*` → `kotrain_*`, GHCR image, GitHub repo `nekko-labs/nekkos` → `nekko-labs/kotrain`, Fly relay back to `kotrain-relay`, `nekkos-designs/` → `kotrain-designs/`. 198 files rewritten. **Back-compat**, each chain now reads new-first then every older brand: env `KOTRAIN_* ?? NEKKOS_* ?? OPENPAW_*`; data dir `~/.kotrain` but a `~/.nekkos` or `~/.open-paw` keeps being used if present; desktop main migrates a `Nekkos/nekkos` or `Open Paw/open-paw` userData dir on first run (a pre-Nekkos `Kotrain/kotrain` dir needs no migration, it *is* the target again); CLI bin `kotrain` with a legacy `nekkos` alias; the design folder prefers `kotrain-designs` but keeps filling a `nekkos-designs` one that already exists. **Not touched**: the `nekko-relay:` PBKDF2 salt in `packages/shared/src/e2e.ts` (frozen protocol constant, asserted by the sweep script), the `nekko-labs` org, the `dev.nekkolabs` appId prefix, and Nekko Labs the company. Also fixed a typo T113 introduced, the Docker workflow was pushing to `ghcr.io/nekkos-labs/…` (an org that doesn't exist). Shipped build-log entries T88/T113/T114 were restored verbatim from git afterwards, since history should keep the names that were true when it shipped. · Done: 2026-08-03
- [x] **T117**, **The mascot is named Aphelion.** The astronaut cat from T114 stays exactly as drawn; only its name changes, so the mascot stops being a moving target every time the product is renamed. `NekkosAvatar` → `AphelionAvatar`, `MiniNekkos` → `MiniAphelion`, and the 14 animation tokens in `styles.css` (`float/drift/spin/orbit/comet(-pass)/bat(-swipe)/twinkle(-k,-slow)/puff(-k)/mini-float`) moved from the `nekkos-` prefix to `aphelion-`, matched longest-first so `comet-pass` didn't get eaten by `comet`. The composer status line reads "Aphelion is working", the website hero title "Aphelion is in orbit", and `settings.mascot` was retranslated in all 7 locales (en/es/fr/de/pt/ja/zh). Aphelion is the far point of an orbit, which is where the cat spends its time. · Done: 2026-08-03

### Phase 25, Marketing site: the harness pitch + smart download (115)
- [x] **T115**, **The site leads with local models, then argues the harness, and the download button knows your machine.** Three changes to `apps/website`, plus the GitHub Releases cleanup the rebrand missed.
  - **Chapter order.** "Local models, finally first-class" was chapter 02, behind a generic feature tour. It is now **01**, the first thing after the hero and window mock, and the nav leads with it.
  - **The harness chapter (02).** The old "Everything in one calm window" card grid (8 boxes, one of them the mascot) became **"The harness is the other half of the model"**: the argument that frontier labs ship raw capability and everything else you feel as "this model is good in my codebase" is scaffolding. Rebuilt as a **spec-sheet manifest instead of cards**, three groups (`.mf-group`) with a sticky label column and hairline-separated rows, auto-numbered by a CSS counter, accent bar on hover: **what it sees** (Context Inspector, grounded retrieval, guidelines + memory, living spec, prompt analyzer), **what it can reach** (sub-agents and swarms, skills, MCP both directions, connectors, terminating goal loops), **when it's wrong** (guardrails, sandbox, diff review, recovery). Every row is a shipped capability from `SPEC.md`, not aspiration. The mascot line moved to a one-line footnote and the JSON-LD `featureList` + `llms.txt` were rewritten to match, so answer engines get the same pitch.
  - **Smart download.** The three static platform cards (all three pointing at `/releases/latest`) are replaced by a **primary button that resolves the visitor's actual build** plus a caret menu of everything else. OS from `userAgentData.platform` with a UA fallback; Mac architecture from Chromium high-entropy hints, falling back to the WebGL renderer string, defaulting to Apple Silicon; iPadOS caught via `maxTouchPoints` (it claims to be a Mac), and phones get "install on your computer, then pair this one" instead of a useless installer link. `BUILDS` declares each published artifact with the regex that finds it, and one call to the **GitHub Releases API** turns those into direct asset URLs plus the version, shown in the button subtitle. Builds with no matching asset drop out of the menu on their own (v0.1.5 ships no Intel `.dmg`, so it isn't offered), and an offline or rate-limited fetch leaves every link pointing at the releases page. Menu is `role="menu"`, arrow-key navigable with wraparound, closes on Escape (focus back to the caret) or an outside click, and marks the build we picked for you.
  - **GitHub Releases.** Every published release was still titled **"Open Paw v0.1.x"** with Open-Paw prose in the notes. Retitled v0.1.0 through v0.1.5 to **Kotrain v0.1.x** and rewrote each body, keeping the `xattr -cr "/Applications/Open Paw.app"` instruction accurate for those builds and adding a one-line historical note about the earlier name. Asset filenames are left alone on purpose: `latest.yml` references them, so renaming would break electron-updater for installed clients. · [spec](SPEC.md#distribution--platforms) · Done: 2026-08-03

### Phase 24, Kotrain rebrand + the astronaut mascot (113–114)
- [x] **T113**, **Rebrand: Kotrain → Nekkos (nekkos.app, with nekkos.dev redirecting).** Same scripted sweep as T87, new target: npm packages `@kotrain/*` → `@nekkos/*`, product name **Kotrain** → **Nekkos**, appId `dev.nekkolabs.kotrain` → `dev.nekkolabs.nekkos`, env vars `NEKKOS_*` (with `KOTRAIN_*` and `OPENPAW_*` fallbacks kept), kotrain.com → nekkos.app, GitHub repo `nekko-labs/kotrain` → `nekko-labs/nekkos`, GHCR image, workflows, installer art regenerated, website naming, `kotrain-designs/` → `nekkos-designs/`. **Back-compat**: default data dir is `~/.nekkos` but a pre-rebrand `~/.kotrain` or `~/.open-paw` keeps being used if present; desktop main migrates the legacy "Kotrain" or "Open Paw" userData dir on first run; CLI bin `nekkos` with a legacy `kotrain` alias (`opaw` dropped). Fly app still runs as `kotrain-relay` until recreated (see Now). Lockfile regenerated; build + typecheck across all workspaces + tests green. · Done: 2026-08-03
- [x] **T114**, **The mascot becomes an astronaut: hand-drawn space Nekkos.** `components/Mascot.tsx` fully redrawn from the 8-bit kendo sprite to a stroke-based, pencil-sketch astronaut cat: big glass helmet (off-round on purpose) with ginger-tipped **ear pods**, cream suit, star chest patch, violet belt, whiskers, blush, and the ginger tail curling out of the suit, space first, cat second. `useTrainingMove` → `useSpaceMove`: while the model thinks Nekkos cycles **drift (4s) → orbit (4.2s) → comet-batting (4.8s)**; the orbit is a spark lapping a tilted dashed ring (an ellipse faked with a `scale(1, 0.34)` wrapper around a CSS rotation), the comet pass and the paw swipe share one 2.4s clock so the swat connects mid-fly-by. Idle floats in zero-g, greeting still waves, clicking fires a boost hop with a thruster puff, twinkling sparkles sit behind everything. `NekkosAvatar` is now the helmet portrait, `MiniNekkos` the helmet with the spark running laps (the "working" spinner). Old kendo keyframes (`punch/jab/kick/swing/guard/knead`) replaced by `float/drift/spin/comet-pass/bat-swipe/twinkle/puff` in `styles.css`. The website hero mascot got the same astronaut treatment (static float + orbit, reduced-motion safe) and its feature card now reads "A copilot in orbit". · Done: 2026-08-03

### Phase 23, Marketplace source rename (112)
- [x] **T112**, **The skills shelf follows Vaizer.** The public marketplace the shelf reads moved out of its own repo: `nekko-labs/nekko-dojo-skills` was folded into `nekko-labs/vaizer` (the site now ships the plugins it lists) and deleted, so every URL Kotrain held pointed at a repo that no longer exists. Renamed the integration end to end rather than leaving a dead brand in the code: `shared/dojo.ts` → `shared/vaizer.ts` (`VaizerCatalog`/`VaizerCatalogSkill`/`VaizerTier`, `VAIZER_REPO`/`VAIZER_REPO_URL`/`VAIZER_SITE_URL`/`VAIZER_CATALOG_URL`, `VAIZER_SNAPSHOT`, `vaizerSkillMdUrl`, `vaizerCategory`, `vaizerToMarketSkill`, ids now `vaizer-<slug>`, `SkillSource` value `'vaizer'`), host `dojo.ts` → `vaizer.ts` (`getVaizerCatalog`/`getVaizerSkillMd`, cache file `vaizer.json`, so a stale `dojo.json` pointing at the dead repo is simply ignored), channels `dojo:catalog`/`dojo:skillMd` → `vaizer:catalog`/`vaizer:skillMd` through the five-touch chain, and the renderer shelf renamed **Kotrain Dojo** → **Vaizer** with **Browse ↗** now opening vaizer.app/skills instead of the repo (the repo is the app's source now, not a skills index). The bundled snapshot was also a catalog behind: it carried 2 skills and now carries all 3 (`resume-checker` added), and `vaizerCategory` maps Vaizer's new `career` category onto Research & planning instead of silently defaulting to Automation. Safe to rename outright because the shelf has never been in a published release (latest is v0.1.5; the shelf landed in T85 after it), so no installed record carries the old `dojo-` id. 136 core tests + 26 desktop tests green, typecheck across all 8 workspaces. · Done: 2026-08-02

### Release
- [x] **T119**, **Released v0.4.0, the first release actually named Kotrain.** Bumped root/desktop/server to 0.4.0 and tagged. Housekeeping the rebrands never finished: deleted the stale v0.2.0/v0.3.0 drafts (their assets were still `Open-Paw-0.3.0-*`, which is why a fresh download installed "Open Paw"), retitled the six published releases from "Nekkos v0.1.x" back to "Kotrain 0.1.x", and swept Nekkos out of the v0.1.0/v0.1.5 bodies (the Open Paw historical notes stay, they describe those builds accurately). One CI casualty of the deps bump: electron-builder 26 started deriving `executableName` from the npm package name, sanitized `@kotrain/desktop` to `@kotraindesktop`, and refused the `@` — pinned `executableName: Kotrain` (the pre-26 default) so the Linux AppImage builds again. First tag run shipped win+mac but failed linux, so the partial drafts were deleted and the tag moved to the fix commit for one clean 13-asset draft, published as latest. Carries everything since v0.1.5, including T118's border beam, the orbit icon, Aphelion, and the Tailwind v4 bump. · Done: 2026-08-05
- [x] **T78**, **Released v0.3.0.** Bumped root/desktop/server to 0.3.0, pushed the backlog to `nekko-labs/kotrain` main, tagged `v0.3.0` → Release workflow (win/mac/linux installers → draft GitHub Release) + Docker workflow (GHCR image). First release carrying T66 (IDE surfaces), T72 (inline comments), T73 (design board), T74–77 (Command Center rework, cost, tasks, skills), and connector official icons + validate-on-connect. Pending creds (non-blocking): npm publish of `kotrain`, code-signing/notarization, one-click Gmail/Drive OAuth.
