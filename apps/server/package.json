{
  "name": "@open-paw/server",
  "version": "0.1.5",
  "private": true,
  "type": "module",
  "description": "Open Paw self-hosted web server — same engine + UI as the desktop app.",
  "main": "./dist/index.js",
  "bin": {
    "open-paw": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "bundle": "node scripts/bundle.mjs"
  },
  "dependencies": {
    "@fastify/static": "^8.0.3",
    "@fastify/websocket": "^11.0.1",
    "@open-paw/cli": "*",
    "@open-paw/host": "*",
    "@open-paw/shared": "*",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
